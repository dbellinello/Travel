# Cleanliness Checks — every green-lit action has a mechanism

> ## 🛑 HARD RULE: This file is enforcer specsNOT rules of record
>
> Per `Travel/CLAUDE.md` HARD RULE + `Brain/CORE RULES/Rules for Claude.html` § 2: every rule that shapes a guide — content AND Claude behavior — lives ONLY in `Brain/CORE RULES/*.html`. **`Cleanliness Checks.md` is operational scaffoldingnot a source of truth.** Every entry in this file is an enforcer spec describing how a validator catches a violation; the rule itself lives in the matching CORE RULES Doc.
>
> If a check here reads like a rulethat's because it has to name what it's checking — but the **authority** is the Docnot this file. When the two disagreethe Doc wins. Find the matching Doc via `Brain/Reference/Separation Map.md`. Park the discrepancy on the To Do List for the owner. Never patch the Doc from this file.
>
> Claude maintains this file freely (additive — new checkstightened checksretirement markers). the owner maintains the Docs.

---

**What this is for.** Every action Claude can take without asking explicit permission (adding a rulefixing a bugrestoring somethingtightening a check) must be backed by a toolscriptor named mechanism that verifies the action left the Brain clean — no leftoversno orphansno broken pointersno drift. If Claude "decides" whether something is clean without a mechanismClaude gets lazy. Anything green-lit has to be machine-checkable.

Claude runs **Category A** at session start (via `brain_check.py`)**Category B** on every audit (via `guide_tools.py audit`)and **Category C** before every guide ship (via `guide_tools.py ship`). The rest run during the audit. Nothing here needs explicit permission — all hygieneall additiveall recoverable.

Split out of the active profile into its own file so `CLAUDE.md` stays clean operational spine rather than a technical checklist. `CLAUDE.md` carries the pointer to this file. Supports the validator-coverage role described in `Brain/CORE RULES/Rules for Claude.html` § 2.

**NOTE on `line ~NNNN` pointers:** Inline line-number pointers inside individual rule entries reflect validator file state at the time the rule was added. Major rearrangements (notably 2026-05-16) have invalidated most of them; treat all `line ~NNNN` pointers as informationalnot authoritative. Use grep / search by check label to locate current implementation.

**Current size.** 394 active rules; highest rule number is 399 (rules 149–153 deleted — single numbering gap between 148 and 154; the file is otherwise contiguous 1–148 and 154–399). The list only grows. When a new drift pattern surfacesadd a check here and wire it into the right script. *(Last recount: 2026-05-18.)*

> ### ⚠️ STANDING ORDER — VALIDATOR CHECK BEFORE GUIDE FIX
>
> Every violation found → **write the `validate_itinerary.py` check firstguide fix second.**  
> Fixing the guide without writing the check is a one-time repair. The check is the permanent fix.  
> If the session ends before the guide is fixed: fine. If it ends before the check is written: the violation ships again in the next build.  
> explicit instruction throughout all 2026-05-08 guide audit sessions. Documented example: abbreviated weekday names (`Mon``Tue`etc.) in `🏛` hours rows — rule changedcheck not written50 violations across 6 audited guides went undetected.  
> See `Travel/CLAUDE.md` HARD RULE for the full protocol.

> ### ⚠️ STANDING ORDER — SEARCH VALIDATOR BEFORE ADDING A NEW CHECK
>
> Before writing any new check in `validate_itinerary.py`:
> 1. **Search first.** Grep for the icon glyphthe rule conceptand related keywords — confirm no equivalent check already exists. Duplicates add noise and risk false positives.
> 2. **Find the right block.** The validator is organized in thematic blocks by icon and rule. Insert the new check inside the correct thematic block — never at the end of the file. A check outside its thematic block is invisible to future searches and breaks enforcer structure.
>
> This order pairs with "VALIDATOR CHECK BEFORE GUIDE FIX" above. Both apply every time a new check is considered. Full rule: `Brain/CORE RULES/Rules for Claude.html` § 4 "Validator pre-check."

---

## A. Fast mechanical checks — every session start · `brain_check.py`

1. **Required §§ present.** Profile contains every § listed in `REQUIRED_SECTIONS`. Missing → FAIL → restore from `Travel/archive/` additively.
2. **Required files present.** `Brain/Reference/Guide Style.css``Brain/mds/decisions.md``Brain/scripts/validate_itinerary.py``Brain/scripts/verify_urls.py``Brain/scripts/verify_booking_links.py``Brain/scripts/commons_photo.py``Brain/scripts/guide_tools.py``Brain/scripts/brain_check.py``Brain/scripts/render_pdf.py``Brain/scripts/validate_pdf.py``Brain/scripts/sweep_stray_travel.py``Brain/scripts/autofix_itinerary.py``Brain/Reference/Platforms.md``Brain/Reference/Separation Map.md``Brain/Reference/Cleanliness Checks.md``Brain/mds/audit_log.md``Brain/mds/Icons.md``Brain/mds/Cities Gotchas.md``Brain/mds/Cities Skip List.md``Brain/Reference/PDF Render Notes.md``Brain/mds/travel_map.md``Travel/CLAUDE.md``Travel/To Do List/To_Do_List.md`. Missing → FAIL. (Authoritative list lives in `brain_check.py` `REQUIRED_FILES`; this entry mirrors it for visibility.)
3. **Pre-Flight Checklist floor.** At least 12 numbered items. Below → FAIL (silent-trim signal).
4. **Ghost references.** Every `` `Brain/...` `` and `` `Travel/...` `` path mentioned in the profile resolves to a real file. Missing → FAIL. `.gdoc` pointers warn.
5. **Audit staleness.** `audit_log.md` last entry ≤7 days old. Older → WARN.
6. **Table of Contents vs. actual §§.** Every `^## ` header has a matching TOC bullet; every TOC bullet has a matching header. Mismatch → FAIL. *(Extend brain_check.py)*
7. **Version history monotone.** ~~`version_history.md` retired 2026-04-29~~ — version tracking is now the version number in each file header only. Rule retired.
8. **No duplicate §§.** Two `^## ` headers with the same name → FAIL. *(Extend brain_check.py)*
9. **CLAUDE.md sanity.** Every file path mentioned in `Travel/CLAUDE.md` exists. Missing → FAIL. *(Extend brain_check.py)*
10. **`guide_tools.py` subcommand coverage.** Every advertised subcommand in its docstring maps to a real sibling script or implemented branch. Mismatch → FAIL. *(Extend brain_check.py)*

## B. Deep-review checks — every `audit` · `guide_tools.py audit`

The audit catches the slow drift — things mechanically fine but semantically going sideways.

11. **Contradiction scan.** Pairs of rules that disagree in spirit or letter. Finding → logged in audit entry for required ruling; never auto-resolved.
12. **Authority conflicts.** Two §§ both claim to be "the" source of truth for a topic. The first wins; the second should be a cross-referencenot a restatement.
13. **Outdated rules.** Rules whose stated reason has since been resolved. Flag for retirement (approval required — removals are stop-and-ask).
14. **Retired but still cited.** A rule marked retired but referenced by an active rule. Fix the citation in the same session (additive).
15. **Dangling anchors.** Profile contains `§Something` pointing to a § that does not exist. Fix in place if obvious; else log.
16. **Polish candidates.** Paragraphs with redundant phrasing within a paragraph. Tighten in place; never remove a rule.
17. **Profile-health snapshot.** Record line count§ countPre-Flight item countTOC item count. Tracks bloat over time.
18. **Organization suggestions.** Any § over ~200 lines or with ≥5 sub-headers flagged for split (additive reorgowner's call).
19. **Improvement list — concrete actions.** End of each auditwrite a grouped bullet list: *fixed-in-session* · *parked-pending-ruling* · *suggested*.

## C. Guide-ship checks — every `guide_tools.py ship` · `validate_itinerary.py` + `verify_urls.py`

20. **HTML well-formed.** Parses without errors.
21. **Day Trips by Train — two surfacessupply-driven counts.** Main-itinerary tour-based day trips (no fixed count floor; every destination passing §Stop Inclusion Bar + qualifying tour on waterfall ships as a full Day N block) + the documentation-only "Day Trips by Train" section (every entry passes the trusted-source curation; no arbitrary cap). Per `Brain/CORE RULES/Day Trips by Train - Extra Section.html` (2026-04-27): entry format is `.extras-sub` city heading with train time + `.ride-apps` body containing the Train: route line + Why: 2–3 sentences + Book via {rail operator site}. Optional section-level intro: when every entry shares one rail operatorship a single *"Book via {operator}."* line above the entries and skip the per-entry Book via. Negative finding when none qualify: *"No qualifying day trips by train from {City}."* Lineage: replaced the retired "≥5 floor" 2026-04-21 end-of-day 8; entry shape simplified to prose-only 2026-04-22; **full format restored 2026-04-27 per PDF v6** alongside the cleanup that retired "parking lot" terminology and dropped the explicit train-only / best-route / distance rules (curated sources do that filtering). *(Enforced — section presence via required_extras + entry-shape full-format check + negative-finding phrase check; 2026-04-22updated 2026-04-27.)*
22. **Tour-first comment.** Every self-guided or skip-the-line stop has `<!-- tour-first: checked YYYY-MM-DD — no qualifying tour -->` above it. *(Enforced — validate_itinerary.py tour-first note check)*
23. **Tour box 📍 + 🌐.** Every `.tour-box` has one 📍 and one 🌐.
24. **Forbidden words.** No metro/subway/bus in place of actual line names. Tram only via `.next-tram`. *(Already enforced)*
25. **Transit line structure.** Walk + ride-app on every transit line. *(Enforced — validate_itinerary.py Motion Rule checks: walk+ride-app pair enforced on every transit linetram sub-line scoped to `.next-tram`metro/subway/bus banned on transit banners; 2026-04-22)*
26. **Skip-list hits.** Guide mentions no entry from `Brain/mds/Cities Skip List.md` for the trip city. *(Enforced — validate_itinerary.py skip-list check: reads `## {City}` section from central Cities Skip List.mdscans stop namesday titlestransit banners; Michelin section excluded to avoid address false-positives. Architecture changed 2026-05-03 from per-guide `Guides/{City}/skip_list.md` to single central file.)*
27. **Link resolution.** Every href/src returns 200. *(Enforced)*
28. **Content-quality gate.** 📖 URLs return ≥100 words of article prose. *(Enforced)*
29. **Tour URL rating.** Tour URLs return ≥4.5★ and ≥6 reviews at verify-time. *(Extend verify_urls.py)*
30. **[RETIRED 2026-05-11]** ~~Blacklist hits. No entry from the city's blacklist. *(New per-city file under `Brain/blacklists/{City}.md`.)~~ Drift — `Brain/blacklists/` was never created; the concept is identical to Rule 26 (Cities Skip List). Rule 26 with `Brain/mds/Cities Skip List.md` is the canonical check. Rule 30 retired.

## D. Filesystem hygiene — every audit

31. **Archive naming convention.** Every file under `Travel/archive/` follows `{basename}_pre_{context}_{YYYY-MM-DD}.{ext}`. Brain-specific archived files land under `Travel/archive/` (R222026-05-06 — `Travel/archive/` no longer exists).
32. **Archive coverage.** Every archived file has a matching entry in `audit_log.md` that explains the move (version_history.md retired 2026-04-29).
33. **No stray backups.** No `.bak``~``*.orig`. Delete on sight (OS leftoversnot rules). **Exception — `.DS_Store` and `__pycache__/` are not surfaced in audits.** Per request 2026-05-03: *"`DS_Store` + `__pycache__/` regeneration — ignore."* macOS regenerates `.DS_Store` and Python regenerates `__pycache__/` automatically; sweeping them is futile and the audit noise isn't worth the signal. They only get flagged if found somewhere genuinely unexpected (e.g.committed into a packaged releasecopied into `Brain/CORE RULES/`).
34. **Guide folder layout.** Every `Travel/Guides/{City}/` has: current guide HTML + rendered PDF at root`_build/` subfolder (containing `assets/` + `verification_log.json`). `verification_log.json` moved into `_build/` on 2026-05-09 alongside assets/. Scripts (validate_itinerary.pyverify_booking_links.py) look in `_build/` firstguide root as back-compat fallback. No per-guide `archive/` subfolder (R222026-05-06 — archive centralized to `Travel/archive/`; any empty stub archive/ dirs in Guides/ are delete when encountered). No per-guide `skip_list.md` (consolidated to `Brain/mds/Cities Skip List.md` 2026-05-03). No per-guide `ASK_DANI.md`. (ASK_DANI.md fully retired 2026-05-08 — mid-build questions now go in ❓ Open Questions section of `Travel/To Do List/To_Do_List.md`.)
35. **No orphan files in Brain/.** Every Brain file is either REQUIRED or referenced from the profile.

## E. Script health — every audit

36. **`py_compile` passes** on every `.py`. Syntax error → FAIL.
37. **Each script has `--help` / usage.** Doesn't crash on `-h`.
38. **`guide_tools.py` SUBCOMMANDS dict matches its docstring.**
39. **Imports resolve.** `python3 -c "import X"` succeeds for each local module.

## F. CSS consistency — every audit (soft checks)

40. **Every class used in any guide is defined in `Guide Style.css`.** Undefined → log.
41. **No duplicate selectors** in `Guide Style.css`.
42. **Unused classes** → log as retirement candidates (required ruling).

## G. Memory / auto-memory hygiene — every audit

43. **Memory files don't contradict the Brain.** Stale memory → log for required ruling; don't auto-delete.
44. **No memory references a deleted Brain feedback file.** Log for cleanup.

## H. Log hygiene — every audit

45. **`audit_log.md` entries are date-ordered.**
46. **No duplicate date headers** in `audit_log.md`.

## I. Profile-edit safety protocol (the profile is load-bearing — break it and guides regress)

A bad edit here propagates into every guide built after it: tour-first skippedtrain-days shrunktransit-line format drifting. These checks make every profile edit recoverable.

47. **Pre-edit snapshot.** Before any non-trivial profile editcopy `Travel/CLAUDE.md` to `Travel/archive/claude_md_pre_{context}_{YYYY-MM-DD}.md`. Never overwrite an existing archive file.
48. **Post-edit brain-check.** Immediately after any profile editrun `brain_check.py`. Any new FAIL → roll back.
49. **Line-count watermark.** `brain_check.py` writes line count and §-count to `Brain/profile_watermark.json` on every successful run. Next session compares; drop without a matching `audit_log.md` entry → FAIL.
50. **§-rename guard.** The set of `^## ` headers at session end matches the set at start (modulo explicit renames logged in audit_log.md).
51. **Rule surface diff.** After any profile editdiff bullet count per § vs. pre-edit. A § losing ≥10% of its bullets without an audit_log.md entry → FAIL.
52. **Edit-intent record.** ~~version_history.md retired 2026-04-29~~ — substantive rule edits log to `audit_log.md`. Rule updated.
53. **Rollback drill.** Once per monthtest-restore a random file from `Travel/archive/` to `/tmp` and diff. Confirms archives are readable.

## J. Enforcement sync — profile ↔ tools must agree

54. **Rule → tool mapping.** Every rule that cites an enforcement has a real check in the named tool. Broken → FAIL.
55. **Tool → rule mapping.** Every validator check is justified by a rule. Orphan → log.
56. **Silent-skip guard.** Every validator check has a docstring naming what it catches and a fixture in `Brain/tests/` that the check fires on.
57. **Forbidden-words list sync.** Each banned-words list lives in one place; tools import from it — no copies.
58. **Version bump trigger.** Any validator change that adds/tightens a check requires a profile v-bump + `audit_log.md` entry.

## K. Regression + baselines — catch the profile "getting crazy"

Goal: surface driftaccumulating contradictionsand unorganized growth *before* the profile becomes unreadable.

59. **Health snapshot per audit.** Record line count§ countPre-Flight itemsTOC items§§ over 200 lines. Week-over-week deltas in `audit_log.md`.
60. **Splitting candidates.** §§ over ~200 lines or with ≥5 sub-headers flagged. Splitting needs explicit permission.
61. **Cross-section redundancy map.** For each rule: where it appears. Deliberate redundancy stays (with CANONICAL/ECHO markers); accidental duplication → flagged.
62. **Rule age.** Rules untouched/uncited for >6 months → logged for the owner to confirm still applies.
63. **Rule-to-guide trace.** Sample rules; confirm guides apply them. Zero-evidence rules → flagged.
64. **Tool-to-guide trace.** Sample validator checks; confirm they fire on some guide.
65. **Contradiction-pair index.** When a rule-pair conflict is found and resolvedlog the resolution in `Brain/mds/audit_log.md` — `contradictions_watch.md` was retired 2026-04-29 (archived to `Travel/archive/contradictions_watch_retired_2026-04-29.md`; both entries were resolved and already baked into the rule PDFs).

## L. Freshness + content aging

66. **Date-stamped claims.** Any rule phrased "as of YYYY-MM-DD" older than 90 days → WARN at audit.
67. **City-guide ship date.** Guides older than 30 days → flag for live re-verification at next ship.
68. **Source reputation age.** `verify_urls.py` timestamps each pass; >90-day-old 📖 source → re-verify at next ship.
69. **Tour listing age.** Tour URLs passing 4.5★/6+ get timestamped; tours older than 14 days re-scraped at ship.

## M. Cross-guide consistency

70. **Icon vocabulary coverage.** Every icon in §Icons Reference is used in ≥1 guide; every icon in guides is defined in §Icons Reference.
71. **Class-name canonical set.** CSS class names used across guides come from a canonical list in `Guide Style.css` comments.
72. **Structural symmetry.** Every guide has: Title PageTrip at a Glance≥1 Day Opener≥1 Day PlanningPricesEnd-of-Itinerary ExtrasHotel-first block on Day 1supply-driven count of Train Day blocks (no fixed target — count is output of §Stop Inclusion Bar + tour waterfall per §Train Day Trips; "5 Train Day blocks" anchor was retired 2026-04-22).

## N. File Contract discipline — right stuff lives in right file

Goal: catch "wrong file" content before it accumulates and forces a rebuild from zero. Contract lives in the profile's §File Contract; this category audits against it.

73. **No bash/shell blocks in `CLAUDE.md` (the active profile).** Scan `Travel/CLAUDE.md` for fenced code blocks tagged `bash``sh``shell``zsh`or for `$ `/`> `-prefixed lines inside fenced blocks. Finding → log with line numbers; move the block into a reference file and leave a one-line pointer. *(Extend brain_check.py)*
74. **HTML shape examples in the profile are allowed.** `html_templates.md` was retired 2026-04-23 per Rule 25 (no template fileno copying from prior guides). Shape examples now live inline in the profile under §Stop Types. This checkformerly "no HTML template blocks in the profile," is retired — the profile is the single source of truth for shape. Keep each inline example minimal and shape-focused (no copy-pasteable full days). *(Check retired 2026-04-23.)*
75. **No Python code blocks in the profile.** Scan for fenced ```` ```python ```` blocks. The profile states rules; scripts enforce them. Finding → log; move to the relevant script's docstring or a reference file. *(Extend brain_check.py)*
76. **No `curl`/`wget`/`python3` invocations in the profile.** Scan for lines matching `^\s*(curl|wget|python3?|pip|npm)\b` inside fenced blocks. Finding → log. *(Extend brain_check.py)*
77. **No CSS rule blocks in the profile.** Scan for fenced ```` ```css ```` blocks or inline `{ ... }` with CSS property syntax (`property: value;` inside braces) ≥3 lines. Class names referenced in prose (`.hotel-first`) are the rule and stay; full selectors with properties are styling and move. Finding → log.
78. **No travel-rule prose in `.py` scripts.** Grep Brain/*.py for comments or docstrings that state a rule not enforced by any check in that file. An orphan rule in a script is a bug — the rule belongs in the profilewith the script referencing it. Finding → log. *(Extend guide_tools.py audit)*
79. **No travel-rule prose in `Guide Style.css`.** Grep the stylesheet for comments naming the owner's preferencesitinerary choicesor stop-type rules. CSS comments may describe what a class is for ("hotel opener banner") but may not state travel rules ("the owner prefers walking"). Finding → log.
80. **No duplicated rule text across files.** If a rule's wording appears verbatim in both the profile and another file (outside of the file's one-line pointer back to the profile)one of them is a shadow copy. Finding → log pair with locations.
81. **Reference file pointer exists.** Every reference file under `Brain/` (e.g. `Cleanliness Checks.md``audit_log.md`) has at least one pointer from the profile. Orphan reference files → log. *(Extend brain_check.py)*
82. **New-file gate.** When a new file is introduced under `Brain/`it must be listed in §File Contractnamed in `REQUIRED_FILES` if load-bearingand pointed at from the profile in the relevant §. Missing any → audit flags the new file as unclassified. *(Extend guide_tools.py audit)*
83. **Pre-write right-file check (human rulenot a script).** Before Claude writes content to any Brain fileClaude names the file out loud in its working notes and confirms the content matches that file's job per §File Contract. If the content would fit multiple filesdefault to the smaller / more specific file (e.g.`Platforms.md` over profile`Cleanliness Checks.md` over profile). Fail-loud at audit if the last audit entry doesn't attest this check was applied to any file edits since the previous audit.

## O. Pointer integrity — both directions

Goal: catch wrong pointers before a rule silently dangles. Every pointer should resolveand every file that exists should be pointed at from somewhere real.

84. **Profile → file (forward pointer).** Every `` `Brain/...` `` and `` `Travel/...` `` path mentioned in the profile resolves to a real file. *(Already #4; kept as anchor for this category.)*
85. **File → profile (back pointer).** Every file under `Brain/` (root + reference files like `Cleanliness Checks.md``audit_log.md`) is pointed at from the profile at least once. Unpointed file = orphan → log. *(Extend brain_check.py.)*
86. **§ → § (internal cross-reference).** Every `§Name` mention in the profile resolves to an actual `^## ` or `^### ` header in the same file. Dangling §-ref → FAIL. *(Already partially #15; tightened here — includes `see §X``per §X``§X → §Y` forms.)*
87. **TOC ↔ § parity.** Every `^## ` header has a TOC line and every TOC bullet has a header. *(Already #6; kept as anchor.)*
88. **CLAUDE.md → profile pointer.** `Travel/CLAUDE.md` contains a pointer to the profile; no rule text duplicated from the profile. Finding → log.
89. **Script docstring → CORE RULES file.** Every `.py` script's docstring references the `Brain/CORE RULES/*.html` file it enforces. Missing → logso reviewers know which rule each script serves.
90. **Reference file → CORE RULES back-pointer.** Every reference file under `Brain/` (e.g. `Cleanliness Checks.md`) opens with a one-line sentence naming which `Brain/CORE RULES/*.html` file it supports. Missing → log; add the sentence additively.
91. **Archive pointer.** Every file under `Travel/archive/` maps to an entry in `audit_log.md` that explains the move (version_history.md retired 2026-04-29; now #32).
92. **Cities Skip List pointer.** Single central file at `Brain/mds/Cities Skip List.md`organized by `## {City}` heading; referenced from `Stops Structure.html` § 1 Research flow (which defines the contract). No rewording of that rule inside the skip-list file itself. *Architecture changed 2026-05-03 from per-city `Guides/{City}/skip_list.md` to single central file.*
93. **No stale filenames.** If a file is renamedevery other file referencing the old name is updated in the same session. Grep across `Brain/` for the old name after any rename. Finding → FAIL.

## P. Format consistency — profile is readable by a non-coder

Goal: the profile reads clean end-to-end so any future collaborator can scan it without tripping on inconsistent markup.

94. **Heading depth cap.** Profile uses only `^## ` and `^### ` headers. No `^#### ` or deeper — if a rule needs a fourth levelthe parent § should be split (per §Cleanliness Checks item #60needs explicit permission). Finding → log.
95. **Bullet style.** One bullet style per file. Profile uses `- ` (hyphen-space); `* ` or `+ ` bullets in the profile → log.
96. **No trailing whitespace.** No line ends with trailing spaces or tabs. Finding → strip in place (additive — just cleanup).
97. **No tabs.** Profile uses spaces only. Tab characters → log with line numbers.
98. **Fenced blocks tagged.** Every ` ``` ` fence carries a language tag (`bash``html``python``css``json`or `text` for plain). Bare fences → log.
99. **Consistent ordinal style.** Ordered lists start at `1.` and count up without skips. `2. … 4. …` → log.
100. **No emoji mixing.** Icons used in the profile match §Icons Reference exactly. A rogue emoji (e.g.🔥 or 🌟 used casually in prose where the intended meaning is "hot" or "star") → log.
101. **Cross-file terminology match.** Key phrases that appear in both profile and scripts use the same spelling everywhere ("Tour-First" vs "tour-first" vs "tour first"). Drift → log; pick the canonical form and standardize additively.
102. **Line length soft cap.** Profile lines over ~160 chars → log (hard-wrap candidates; won't failjust a nudge so the file stays readable in a narrow view).

## Q. Link decay & rating drift tracking

Goal: catch the slow rot — a link that was green last ship is amber today; a 4.6★ tour that dropped to 4.3★; an article that lost its prose content between audits. Without trackingregression hides.

103. **Verify-results cache.** `verify_urls.py` writes each pass to `Brain/verify_cache.json` with `{urlstatusratingreview_countword_counttimestamp}`. At next shipthe verifier compares current vs. cached for every URL and surfaces regressions (200 → 5xx4.7 → 4.32,100+ → 400+). Regression → WARN at shipFAIL if below thresholds.
104. **Rating-drop guardrail.** Any tour URL whose live rating fell below 4.5★ since last ship → FAIL (triggers tour removal). Any review-count drop ≥30% → WARN (platform may have purged reviews — verify manually).
105. **Content-quality regression.** A 📖 URL that returned ≥100 words last ship but <100 words this ship → FAIL. Catches publisher-side gutting of pages (the Lonely Planet pattern from 2026-04-20).
106. **URL-shape flip.** A product URL that silently redirects to a city-landing page (e.g.`viator.com/.../d{DestID}-{ProductCode}` → `viator.com/{City}/d{DestID}-ttd`) → FAIL.
107. **Domain-wide red flag.** If ≥3 URLs on the same domain regressed in the same passsurface the domain as a "retirement candidate" in the audit log (per the Lonely Planet pattern). Retirement still stop-and-ask.
108. **Unverified-too-long.** Any URL in the profile or any guide that hasn't been live-verified in the last 30 days → WARN at audit; 90 days → FAIL. Prevents "we think this still works" drift.
109. **Anchor-link decay.** Internal anchors (`href="#day3"`) in shipped guides must resolve to a real `id="day3"` element. Dangling → FAIL at ship. *(Extend validate_itinerary.py.)*

## R. Archive & stale-file sweep

Goal: keep the filesystem tidy so pointers don't accidentally target a ghostand old artifacts don't clutter Claude's attention.

110. **Archive-date sanity.** Every file under `Travel/archive/` has a `_YYYY-MM-DD` suffix that matches a real date (not futurenot pre-2024). Off-pattern files → log.
111. **Archive growth rate.** Audit records the archive's file count per run. A week-over-week jump of >10 archive files without an audit_log.md entry → WARN (catches sloppy mass-archival).
112. **Guide-folder cleanliness.** Every `Travel/Guides/{City}/` has: exactly one current-named guide (`{city}_v{n}.html`) at topzero old guides at top (old versions archived to `Travel/archive/` per R22)no stray `.bak`/`.orig`/`~`/`.DS_Store`. Stragglers → log; delete on sight (green-lit — OS leftoversnot rules).
113. **Orphan guide assets.** Any image under `Travel/Guides/{City}/_build/assets/` that isn't referenced by the current guide → log as orphan (not auto-delete — could be a WIP).
114. **Stale reference files.** Any reference file under `Brain/` untouched in >180 days → audit flags for confirmation. "Still load-bearing?" is the question; don't auto-delete.
115. **Audit-log rotation.** `audit_log.md` under 500 lines stays as-is; over 500 linesthe oldest audit entries (older than 6 months) move to `Travel/archive/audit_log_{YYYY-MM}.md` and the main log keeps a pointer. Stop-and-ask on first rotation — establishes the pattern.
116. **Audit-log entry per archive file.** Every archive creation ties back to one audit_log.md entry with a matching date. Missing → log.
117. **No leftover pre-edit snapshots for a completed edit.** If today's pre-edit snapshot exists in archivethe profile's audit_log.md entry for today must exist too. Missing one but not the other → FAIL.
118. **Trip data file freshness.** `Travel/Trip Essentials/Trips.html` is the primary trip data file — read directly with the Read tool. Audit flags if it hasn't been surfaced in the current session when trip-relevant work is in scope.

## S. Test & fixture coverage

Goal: every validator check has proof it fires on bad input. A check with no fixture is a silent-skip risk.

119. **Fixture folder exists.** `Brain/tests/` directory exists. Missing → create emptyadditive. *(Already referenced by #56; anchored here.)*
120. **One bad-fixture per validator check.** Every check in `validate_itinerary.py` and `verify_urls.py` has at least one negative fixture (a `.html` file under `Brain/tests/` designed to trip exactly that check). Missing → log.
121. **Fixture runner.** `guide_tools.py test` runs every fixture through its target check and asserts the expected FAIL/WARN. Missing runner → log as stub (green-lit to add).
122. **Fixture freshness.** When a check's logic changesits fixture is updated in the same commit. Drift between check and fixture → FAIL at next audit.
123. **Guide-wide regression suite.** The current shipped guide for each cityrun through `ship`must exit 0 every audit. If a prior-shipped guide now fails ship-gatethe audit logs "regression: {city}_v{n} failed ship on {date}" — means a rule tightened since the guide was shipped and the guide needs a rebuild.

## T. Ship-gate robustness

Goal: the ship command is the last line of defense — it must never pass a broken guide silently.

124. **Exit code discipline.** Every script returns non-zero on any FAIL. A "pretty-printed error but exit 0" is a bug. Audit greps each script for `print(".*❌` followed by no `return 1` / `sys.exit(1)` within N lines.
125. **Ship-gate log.** `guide_tools.py ship` writes a timestamped summary line to `Brain/ship_log.md` per run: `{YYYY-MM-DD HH:MM} — {guide_path} — PASS|FAIL — {check-count}`. Lets the audit trace "when did this guide last pass?"
126. **Pre-ship archive snapshot.** Before `guide_tools.py ship` overwrites a shipped guidecopy the prior version to `Travel/Guides/{City}/archive/{city}_v{n-1}_pre_{YYYY-MM-DD}.html`. Recoverability by default.
127. **Never-silent WARN.** A WARN must always print to stdout with the check name and the offending line. Silent WARN = silent skip. Audit grep confirms every WARN has a visible emission.

## U. Decision logging & rule-scatter prevention

Goal: prevent re-litigating settled judgment calls and prevent the "add a rule in a new place instead of the right place" pattern that causes scatter.

128. **Decisions log.** A new file `Brain/mds/decisions.md` records every non-trivial judgment call — bansdemotionsretirementssignificant trade-offs — with datewhat was decidedwhyand what replaced the old approach. Audit appends any new decisions surfaced since the last run. Prevents Claude from re-asking "should we ban Lonely Planet?" six weeks later. *(New file — additive.)*
129. **Subject-index pre-add check.** Before adding a new ruleClaude greps the profile for the rule's subject keyword (e.g."tram""tour-first""photo""hotel"). If prior rules on the subject existthe new rule goes in or adjacent to the existing §not in a random new place. Adding a scattered rule when a home § exists → FAIL at next audit.
130. **One-home-per-topic contract.** Every topic has one canonical home § in the profile; other mentions are cross-references (`*(see §Home for the rule)*`)not restatements. Audit flags duplicate authority claims (already #12 catches the pair; this catches the pattern systematically).
131. **CANONICAL / ECHO markers.** Where a rule is deliberately echoed in a second § (e.g.Pre-Flight Checklist quoting the top-level rule)the second mention is tagged `*(echo of §Home — see there for the canonical form)*`. Un-tagged duplicates → log as scatter candidates for required ruling.
132. **Session-end close-out.** Every session with profile edits ends with (a) a brain-check run(b) an audit-log entry if an audit happened(c) a pre-edit snapshot archived per Check #47and (d) a decisions.md entry if a judgment call was made. Any of the four missing → FAIL at next session start.
133. **Proactive audit nudge.** If session-start brain-check sees `audit_log.md` last entry >7 days oldClaude volunteers: *"last audit was N days ago — want me to run `audit`?"* before starting task work. Not a FAIL; a surface.
134. **No "I'll remember it" state.** If Claude finds itself thinking "I'll keep this in mind for next time," that is a bug — the thought goes into the profilea reference fileor decisions.md. Verified at audit by scanning Claude's own prior-session notes for un-persisted commitments. *(Self-discipline rule; no script enforces it — but the audit surfaces if a decision Claude cited in a guide isn't recorded anywhere.)*
135. **Restoration-drill proof.** Quarterly: pick one file from `Travel/archive/` at randomrestore it to `/tmp`diff against currentand confirm the restoration path works end-to-end. Log the drill date in audit_log.md. Prevents silent archive rot.
136. **Rule provenance.** Every rule whose "why" hinges on a specific incident names the date of the incident inline (e.g."Rule 00a0c restored 2026-04-21 after silent deletion on 2026-04-20"). Audit surfaces un-dated rules whose phrasing references an incident. Lets Claude judge whether the context still applies.
137. **Inclusion-bar comment on every stop.** Every stop in the guide carries `<!-- inclusion-bar: ... -->` with cited evidence above it. A stop without the comment = missing justification. *(Enforced — validate_itinerary.py inclusion-bar check; scans the 600 chars preceding each stop-flag div for the comment; 2026-04-22)*
138. **⏰ never at stop level on guided stops.** A stop that has a tour box and no 🎒 at stop level must not carry ⏰ outside the tour box. The ⏰ icon belongs only on self-guided stops; ⏳ handles duration inside tour boxes. *(Enforced — validate_itinerary.py ⏰-on-guided-stop check; fires when a stop has a tour-boxno 🎒 in the non-box contentand an ⏰ in the non-box content; 2026-04-22)*
139. **`.tour-box` class used only for tour logistics (must lead with 📅 or 📍).** A `.tour-box` that opens with 🎟️ is a ticket row in the wrong class — ticket rows (any category: skip-the-linetimed-entryhosted-entryfast-trackadmissionrooftop passetc.) belong in a `.ticket-box` per `guide_v2.css .ticket-box §` (two-booking-box rule) and `Stops Structure.html` (Alternation Stops). The two valid `.tour-box` lead glyphs are 📅 (guided tour booking) and 📍 (stop-logistics wrapper for self-walk). *(Enforced — validate_itinerary.py tour-box integrity check; 2026-04-22; simplified 2026-04-23 to reflect the 3-icon / 2-box / 4-label restructure.)*
140. **Every 📅 tour block is wrapped in `<div class="tour-box">`.** A bare 📅 emoji emitted inside a stop block but outside any `.tour-box` wrapper renders uncolored (browser-default white) and breaks visual uniformity across tours at a multi-tour guided stop — the exact failure mode that drifted across past guides before this check landed. The validator scans each stop blockmaps the byte-spans covered by `.tour-box` wrappersand fails the ship-gate on any 📅 position outside those spans. *(Enforced — validate_itinerary.py bare-📅-outside-.tour-box check; 2026-04-23.)*
141. **No inline `style="..."` attribute on `<div class="tour-box">`.** Tour-box rendering (`background: #f0f6ff``border-left: 3px solid #3a7bd5`) lives entirely in `Guide Style.css`. An inline style attribute overrides the palette and produces a mis-colored or uncolored tour block that looks different from its siblings in a multi-tour stop. Mirrors the existing inline-style bans on `<img>` and `.stop-photos` wrappers — same principle: rendering rules belong in CSSnever HTML attributes. *(Enforced — validate_itinerary.py inline-style-on-.tour-box check; 2026-04-23.)*
142. **Every 📅 tour-box has its tour name hyperlinked.** The 📅 glyph is immediately followed by `<a href="{booking URL}">{Tour Name}</a>` — never a bare-text tour name. Profile line 891: "Tour namelinked to the booking page." A bare-text tour name leaves the reader no way to reach the booking page from the guide. *(Enforced — validate_itinerary.py tour-box hardening batch; 2026-04-23.)*
143. **Every 📅 tour-box carries 🕐 start time.** Profile line 896. Without 🕐 the reader can't see when the tour starts and has to click through to the booking page to find out. *(Enforced — validate_itinerary.py tour-box hardening batch; 2026-04-23.)*
144. **Every 📅 tour-box carries ⏳ duration.** Profile line 897. Without ⏳ the reader can't tell if this is a 90-minute walk or a 5-hour combo and can't plan the day around it. *(Enforced — validate_itinerary.py tour-box hardening batch; 2026-04-23.)*
145. **Every 📅 tour-box carries 👥 max group size.** Profile line 898. Small-group (≤15) vs. large-bus (50+) is a core selection criterion; the icon is load-bearing. *(Enforced — validate_itinerary.py tour-box hardening batch; 2026-04-23.)*
146. **Every 📅 tour-box carries a review count.** Profile line 893 format: `— [Platform] · [Rating]★ · [Count]+ reviews`. The review count is required alongside the star rating — rating alone doesn't convey volume (a 4.9★ with 6 reviews is very different from a 4.9★ with 2,100 reviews). *(Enforced — validate_itinerary.py tour-box hardening batch; 2026-04-23.)*
147. **Review counts round down to the nearest 10 (for <100) or 100 (for ≥100) and end in `+ reviews`.** Profile line 893 example: `2,100+ reviews` (never `2,143 reviews`). For products with <100 reviewsround down to nearest 10 (`29 reviews` → `20+ reviews`; `12 reviews` → `10+ reviews`). For ≥100round down to nearest 100 (`348 reviews` → `300+ reviews`; `2,143 reviews` → `2,100+ reviews`). Rounded-down-plus signals "at least this many" without false precision that makes the count look like a live API value. Pattern: digits ending in `0` (any size) followed by `+ reviews`. *Refined 2026-04-26 from "nearest 100" to handle small-count products cleanly — earlier rule rejected `20+ reviews` because it didn't end in `00`blocking small-count Viator/GYG products from shipping. (Enforced — validate_itinerary.py tour-box hardening batch; 2026-04-23refined 2026-04-26.)*
148. **[RETIRED 2026-04-24]** ~~No price information in `.tour-box`.~~ Subsumed by Rule 164 (zero currency guide-wide). Belt-and-suspenders sentinel in validate_itinerary.py kept.

154. **Title card scope — hotel is name + address only.** Per `Hotel Banner.html § 3` → "Out of scope for the hotel". The `.title-page` block contains `.title-city` + `.title-hotel` (name) + `.title-address` (Google-Maps-linked address) and nothing else. The validator scans the `.title-page` inner HTML (with `<a>` tags stripped so the Maps URL can't false-positive) for out-of-scope tokens: currency symbols next to digits (`$75``€120`)currency codes (`USD`/`EUR`/`GBP`/`JPY`)price cadence (`per night``from $``starting at``rates:`)check-in/check-out/arrival/departure wordsISO dates (`2026-04-23`)month-name+day dates (`April 23`)stay-length counts (`5 nights``7 days`)clock times (`3:00 pm`)confirmation/reservation codesloyalty tiers (`platinum``bonvoy``honors``gha``genius`etc.)room-type words (`king room``suite``deluxe bed`)and amenity words (`breakfast``wi-fi``parking``amenity`). Any match is a hard FAIL. flagged this after a prior build emitted a hotel price in the title card. The hotel's role in the guide is name-it-on-the-cover + map-the-address; everything else is out-of-scope. *(Enforced — validate_itinerary.py title-card scope check; 2026-04-23.)*

155. **Every 🎟️ ticket row inside a `.stop-block` is wrapped in a `.ticket-box` — no matter the ticket category.** Per `guide_v2.css .ticket-box §` (two-booking-box rule) and `Stops Structure.html § 2` (Self-Guided stop Shape B). The validator iterates every `.stop-block`builds a byte-span mask of every `.ticket-box` wrapper inside itscans every 🎟️ position in the stop blockand hard-fails if any 🎟️ falls outside all `.ticket-box` spans. Applies to every ticket category — skip-the-linetimed-entryhosted-entryfast-trackadmissionrooftop pass — and to every stop type (self-guided Shape-B ticket-gated stop or any other). Scope is limited to `.stop-block` — Shows & Performances 🎟️ rows live in `.shows-box` inside `.extras-section`a different container hierarchy (show tickets are seat-reservationnot venue-entry). Reason: `.ticket-box` gives 🎟️ rows the shared blue booking-box palette (`#f0f6ff` / `#3a7bd5`) so the ticket row matches its sibling `.tour-box` rows instead of rendering browser-default when bare; the 🎟️ glyph itself is what scans for ticket gates. *Retired 2026-04-23 — red `.ticket-box` palette (`#fdf2f2` / `#c0392b`).* Prior wording: "the red palette (`#fdf2f2` background`#c0392b` border) is how scans the day for ticket gates"; retired when the two-color scan signal was collapsed to a glyph-only signal (see guide_v2.css .ticket-box § retirement notesame date). *Retired 2026-04-24 — "alternation stop" as a valid shape.* ~~Per request §Tour-First Rule "tour wins exclusively" (`Travel/archive/alternation_stops_retired_2026-04-24.md`)a `.stop-block` shipping BOTH a `.tour-box` AND a `.ticket-box` is now a validator hard FAIL.~~ *Re-locked 2026-04-25 — Type 3 Alternation Stops un-retired.* Per `Brain/CORE RULES/Stops Structure.html`alternation is the third of four stop types — a `.stop-block` MAY ship both boxes; CSS auto-flips the title icon to "🚩 or 🎒". The 2026-04-24 retirement is reversed; see Rule 171 above for full lineage. *(Enforced — validate_itinerary.py bare-🎟️-outside-.ticket-box check; 2026-04-23. Tour-box+ticket-box-on-same-stop mutual-exclusion hard-fail added 2026-04-24 and removed 2026-04-25 alongside the alternation un-retire.)*

156. **Stop-type system is 1 title-icon modifier · 2 stop types · 1 body-box type · 2 day-glance labels.** *Updated 2026-05-20 — Guided Tour Stop and Alternation retired.* Per `Brain/CORE RULES/Stops Structure.html` and `Brain/CORE RULES/Trip at a Glance.html`. The taxonomy lives on four axes:

     - **Stop-title icons** — rendered by CSS `::before` keyed off the modifier class on `.stop-name`:
       - 🎒 = `.stop-name.self` (Self-Guided Stop — Option 1 pure-walk or Option 2 ticket-gated)
       - 🚆 = `.stop-name.train` (inner stop on a Train Day — routed as `self` inside the train wrapper)
       - ~~🚩 = `.stop-name.guided`~~ *(Retired 2026-05-20 — Guided Tour Stop retired; all in-stop tour boxes converted to self-guided)*
       - ~~🚩 or 🎒 = Type 3 Alternation~~ *(Retired 2026-05-20 alongside guided)*
     - **Stop types (2 active)**:
       1. 🎒 Self-Guided Stop (Option 1 pure walk · Option 2 ticket-gated) — the only active itinerary stop type
       2. 🚆 Train Day (round-trip train wrapping Self-Guided Stops in the destination city)
       - ~~🚩 Guided Tour Stop~~ *(Retired 2026-05-20)*
       - ~~🚩 or 🎒 Alternation~~ *(Retired 2026-05-20)*
     - **Body-box types (1 active inside stop-block)**:
       - 🎟️ row → `.ticket-box` (any ticket category — timed-entryfast-trackadmissionrooftop passetc.)
       - ~~📅 row → `.tour-box` inside stop-block~~ *(Retired 2026-05-20 — tour boxes now live only in the Tours Extra Section)*

       Combination rule: `self` → exactly one of (📍-led self-walk box`.ticket-box` led by 🎟️) — XORnever both.
     - **Day-level glance labels (2 active)** — rendered in Trip at a Glance:
       - 🎒 Self-Guided
       - 🚆 Train Day
       - ~~🚩 Guided~~ *(Retired 2026-05-20)*
       - ~~🎒 + 🚩 Self + Guided~~ *(Retired 2026-05-20)*

     The retired classes `.stop-name.ticket``.stop-name.mixed`and `.stop-name.guided` are gone; no new title classes are permitted. *Note: `validate_itinerary.py` still accepts `guided` in its modifier-class whitelist as of 2026-05-20 — validator update needed to hard-fail on `guided` in new builds. Tracked in To_Do_List.md.* *(Enforced — validate_itinerary.py modifier-class whitelist + body-shape routing; `:has()` auto-flip rule in guide_v2.css is legacy-only for shipped guides.)*

157. **`{guide_folder}/_build/verification_log.json` present and PASS for every bot-blocked booking URL.** Viator / GetYourGuide / Michelin / TripAdvisor all return HTTP 403 to automated fetch — the only record that a human actually verified a specific-product URL via `site:`-search is the log entry. Every Viator product URL (`viator.com/tours/{City}/{Slug}/d{DestID}-{ProductCode}`)GetYourGuide product URL (`getyourguide.com/{city}-l{LocID}/{slug}-t{ActID}/`)Michelin restaurant URL (`guide.michelin.com/{lang}/{region}/restaurant/...`)and TripAdvisor attraction URL (`tripadvisor.com/Attraction_Review-g{GeoID}-d{AttractionID}-…`; added 2026-04-24 after live tests showed HTTP 403 on the TA UA) in the shipped guide must have an entry in `{guide_folder}/_build/verification_log.json` with `result: "PASS"` and `verified_on` within the last 90 days. Missing log filemissing entrynon-PASS resultbad dateor >90-day staleness → FAIL. >30-day staleness → WARN (rebuild nudgenot ship-blocker). Reason: on 2026-04-23 an audit revealed 27/31 Viator+GYG tour URLs in a shipped guide had no verifiable record of a `site:`-search hit — the DO-NOT-DELETE workaround in `Brain/Reference/Platforms.md` was documented but not enforced. This gate closes that loop. *(Enforced — validate_itinerary.py booking-link log-coverage check (staticfail-fast) + verify_booking_links.py (authoritative ship-gatefull log + live h1); 2026-04-24.)*

158. **TripAdvisor `Attraction_Review-g{GeoID}-d{AttractionID}` URLs — log-required + live `<h1>` match against the stop's venue name when fetchable.** TripAdvisor silently reassigns `d{AttractionID}` numbers when its catalog changes — the URL shape stays validHTTP 200 stays greenbut the resolved page is a different attraction. `verify_urls.py` catches neither. TA is now **dual-classified**: log-required (rule 157 — because live tests on 2026-04-24 showed TA returns HTTP 403 to our UA most of the time)AND live-h1-matched when the fetch actually succeeds. When `verify_booking_links.py` gets a 200 responseit extracts the first `<h1>`tokenizes itgenerates candidate forms of the stop name (full · pre-em-dash location head · pre-parens name · parens-content · ` or `-split aliases) and requires ≥50% token overlap on at least one candidate. When it gets a 403the log entry carries the verification (WARNnot FAIL). Reason: on 2026-04-23 a shipped guide had a TripAdvisor `Attraction_Review` URL whose `<h1>` resolved to an unrelated waterway rather than the intended museum — the d-ID had been reassigned. *(Enforced — verify_booking_links.py live h1-match gate + log-coverage gate; candidate-generation fix 2026-04-24.)*

159. **Wikipedia `/wiki/{Slug}` URLs — live `<h1>` match against the stop's venue name.** Same failure mode as entry 158: a Wikipedia URL whose slug silently drifted (article renamedmergedredirected to a different subjector the wrong slug was pasted in at build time) returns HTTP 200 + abundant prose but is not about the stop's subject. `verify_urls.py` content-quality gate measures word countnot subject match. The `verify_booking_links.py` gate fetches the URLextracts the first `<h1>`generates candidate forms of the stop name (full · pre-em-dash location head · pre-parens name · parens-content · ` or `-split aliases — so a stop like "Reims — Cathedral & Champagne Cellars" still matches the h1 "Reims Cathedral" on the "Reims" candidate)and requires ≥50% token overlap on at least one candidate. Added 2026-04-24 at the owner's request ("Please add Wikipedia too since we use it in all the guides") because 📖 Wikipedia links appear on every stop and share the same "right URL shapewrong subject" failure mode as TripAdvisor. Candidate-generation tuning landed 2026-04-24 after an initial run caught 1 real subject-drift bug (a stop's Wikipedia URL resolved to a neighboring arrondissement article) but 8 false positives on legitimate matches with compound stop names (em-dash qualifiersparens clarifiers); candidate-generation cleared all 8 false positives while preserving the real subject-drift catch. *(Enforced — verify_booking_links.py live h1-match gate; 2026-04-24.)*

160. **Michelin Restaurants — all three star tiers shipunconditionally.** Every 1⭐2⭐and 3⭐ restaurant listed on guide.michelin.com for the destination must appear in the §Michelin Restaurants section (minus the seafood-heavy exclusion in rule 161). Shipping only the top tier is a direct rule violation: a 20-day destination that shipped only ⭐⭐⭐ entries on 2026-04-24 was the motivating incident — "the Michelin restaurant rule must be broken ... the rule needs to say that ALL restaurants with any number of stars need to be included on the list." The builder enumerates the full starred registercaptures every entry in every tierand ships every one (minus seafood-heavy). Completeness is the signal; there is no "shortlist the standouts" editorial pass. *(Editorial rule — no machine enforcement possible without a live guide.michelin.com source. Brain-rule surface + builder diligence; reviewed at guide-ship diff time.)*

161. **Michelin Restaurants — seafood-heavy menus are INCLUDED + flagged with `🐟 seafood-heavy` next to the name.** Per `Brain/CORE RULES/Michelin Restaurants - Extra Section.html` § Seafood flag (when in doubtmark): *"When a restaurant's menu identity is 'this is a seafood restaurant' — dedicated seafood housefish-first tasting menuoyster / raw-bar specialty — flag it with `🐟` next to the name. ... A general restaurant that happens to have a couple of fish courses doesn't need the flag. When in doubtflag it. the owner checks before making reservations and would rather see the entry with a heads-up than miss it because of a borderline call."* This is the only flag on the Michelin list — no other tastecuisineor price-band markers. Heading shape: `⭐⭐ Restaurant Name 🐟 seafood-heavy`. Rule lineage: ~~v1 (pre-2026-04-24): include + flag with 🐟~~ → ~~v2 (2026-04-24): exclude seafood-heavy outright (no entryno flag) — the owner's sweep removed the flag because seafood-heavy entries were noise on her scan list~~ → **v3 (2026-04-27): re-instated include + flag per the Doc** + the owner's chat 2026-04-27: *"🐟 is used to flag when you think the menu has a lot of seafoodso the icon is used to indicate that this time."* The v2 → v3 flip was driven by the realization that excluding seafood-heavy entries silently DROPPED restaurants still worth visiting (she eats some fishjust not when "this is a seafood restaurant" is the menu's narrative identity); the flag's job is "heads-up before reservation," not "cut from the list." (Editorial rule — no machine enforcement for the seafood-heavy classification itself; the surrounding mechanical rules — Rule 237 cuisine rowRule 242 only-🐟-flag-allowed — keep the heading row clean.) *Updated 2026-04-27 alongside the Michelin Restaurants.html validator-coverage backfill slice.* **🔄 SUPERSEDED 2026-05-15 (v4 — back to exclusion):** the 🐟 flag is RETIRED again. Seafood-primary restaurants are now OMITTED entirelynot flagged. Current rule lives in `Michelin Restaurants - Extra Section.html` § How to build the list (Seafood exclusion paragraph). v3 framing (include + flag) replaced by v4 (omit entirely) per request 2026-05-15. Library entry for 🐟 moved back to Symbols & Marks (available pool).

162. **[RETIRED 2026-04-27]** ~~No retired `🐟 seafood-heavy` marker in `.extras-sub` — stale-marker scrub.~~ Per Rule 161 v3 (2026-04-27): the `🐟` flag is now CANONICAL againnot retired. The scrub block in `validate_itinerary.py` (line ~2559) was retired in the same slice with a Rule 11 retirement marker preserving the lineage. Original rule bodykept per Rule 11 (retire ≠ delete): ~~"Since rule 161 retired the inline seafood flag (seafood-heavy restaurants are now excluded outrightnot flagged)any `🐟 seafood-heavy` text inside a Michelin section `.extras-sub` is a leftover artifact of the pre-2026-04-24 flag behavior. The correct fix is to remove the entry entirely (the restaurant is seafood-heavyso it shouldn't appear at all — not re-flag it). The validator hard-fails on any surviving `🐟` marker in `.extras-sub` with a message pointing at rule 161 + the seafood-heavy exclusion."~~ Lineage: scrub added 2026-04-24 alongside Rule 161 v2 (exclude); retired 2026-04-27 when Rule 161 flipped back to v3 (include + flag) per PDF v2. *(Retired — validate_itinerary.py 🐟-scrub block at line ~2559 commented out with retirement marker; 2026-04-27.)*

163. **Ride Apps section — concise prose≤60 words targetZERO prices in any currency (including USD).** Per `Getting Around - Extra Section.html § Ride Apps` (rewrite 2026-04-24). The §Ride Apps block answers exactly two questions: (1) "Can I use Uber?" and (2) "If notwhat do I use?" — nothing else. Hard content bans: **no prices at allin any currencynot even USD** — no `typically under €20`no `typically under $20`no `budget $15 for a crosstown ride`no `airport run about $60`. This is not "local currency is bannedUSD is fine" — this is "ZERO money figures herefull stop." Reason: the builder has no live source for per-city ride-app fares (surgesairport surchargestipstime-of-day variance all shift hour-by-hour)so any figure that shows up IS an invention — a number that looks like data but isn't. used to plan the trip from these numbers; planning against fiction is worse than planning without the number at all. Other bans: no market commentary (`densecompetitive ride-hailing market`)no fleet historyno surge-pricing contingenciesno SIM-card mentionsno app-install instructions. Format cap: ≤3 short lines≤60 words soft target>80 words WARN>100 words FAIL. The validator counts words inside every `.transit-box` (where the Ride Apps block lives) and scans for any currency symbol (`$``€``£``¥``₩``₹``₽``฿`) or 3-letter code paired with digits — any currency-price hit inside a `.transit-box` is a hard FAILregardless of currency. Reason: a prior-guide Ride Apps box shipped with "…runs a densecompetitive ride-hailing market. Uber typically under €20" — a 76-word dissertation with an invented priceprecisely the drift this rule prevents. the owner clarified 2026-04-24: *"to be clearno money should be even showing in this section. this was already an invention…"* The ban is absolute. *(Enforced — validate_itinerary.py RIDE APPS BREVITY + NO-PRICES CHECK catches `$``€``£``¥``₩``₹``₽``฿``CHF``R$` inside `.transit-box`; 2026-04-24.)*

164. **ZERO currency figures anywhere in a shipped guide — any symbolany currencyincluding USD.** Per `Stops Structure.html § Prices` (rewritten 2026-04-24). A shipped guide carries no money figures on any surface — no title-card lineno tour-boxno ticket-boxno transit-box / Ride Appsno Michelin blockno Showsno Local Tastesno proseno captions. the owner 2026-04-24: *"we only use prices for flightshotelscan rentals and rental places. nothing else ... all these categories are all on demand when I ask for. So no guide should ever be shipped with any money showing anywhere."* The four price-carrying categories (✈️ Flights🏨 Hotels pricing-research🏠 Rentals🚗 Car Rentals) are all on-demand research outputs produced as separate documents when when asked — never baked into the guide file. The hotel name + address appears in the title card per Rule 154but its price does not. The validator strips HTML tags first (so Maps URLs and booking-link query params with embedded currency codes don't false-positive) and scans the rendered text for any currency symbol (`$``€``£``¥``₩``₹``₽``฿``R$`) adjacent to digits or any 3-letter ISO currency code adjacent to digits (`USD``EUR``GBP``CHF``JPY``KRW``CNY``RUB``INR``THB``BRL``MXN``AUD``CAD``NZD``SGD``HKD``TWD``SEK``NOK``DKK``PLN``CZK``HUF``TRY``ILS``AED``ZAR``MYR``IDR``PHP``VND`). Any hit is a hard FAIL. USD is not special-cased — `$20` is as forbidden as `€20`. Reason: multiple prior builds shipped stray price figures on ride-app linesticket rowsmeal mentionsand transit prose even after prose-level reminders; the owner: *"money is not a factor for meonly care about the big expenses,"* meaning small-ticket prices (mealstaxisticketscoffee) are noise she doesn't want on the scan surface at all. The validator closes the loop by making zero the barnot "USD-only." *(Enforced — validate_itinerary.py ZERO-CURRENCY CHECK; 2026-04-24.)*

165. **No `~$N` approximation marker and no `$N USD` trailing label — anywhere.** Per `Stops Structure.html § Prices`. Both forms are banned even inside the on-demand research outputs that DO carry prices (FlightsHotelsRentalsCar Rentals). `~` signals "I'm hedging because I didn't convert properly" and the trailing `USD` label signals "this might be a different currencyI'm covering myself." Neither is acceptable — the correct action is to convert from the live rateround UP to a sensible whole numberand emit a clean `$N` figure with no decoration. Note: rule 164 already catches both forms inside a shipped guide (any `$` adjacent to a digit failsregardless of `~` or suffix). This rule applies to the on-demand research outputswhich rule 164 does not govern. *(Editorial rule — the on-demand outputs are separate documents not validated by validate_itinerary.py.)*

166. **Brain-firstbug-fix freeze: every fix lives in the Brain (CORE RULES + CSS + validators) and no guide rebuilds or PDFs happen while a Brain bug is open.** Per `Rules for Claude.html` Brain-first rule (strengthened 2026-04-24) and rebuild-freeze rule (new2026-04-24). The rule has two surfaces: **(a) WHERE fixes go (Rule 23)** — every shape / rule / content-code fix lands in `Brain/Reference/Guide Style.css`or the validator scripts under `Brain/scripts/` — never in `Travel/Guides/{City}/*.html`never in a rendered PDFnever in a per-guide patch file. The only exception is a pure prose typo or wrong photo in one specific guide (no rule behind it). Everything else is rule-shaped and goes through the Brain. **(b) WHEN guides get rebuilt (Rule 28)** — once a bug-fix session opens on the BrainALL downstream guide operations (rebuilds`ship` runsPDF rendersdelivery of new HTML/PDF to owner) are frozen until (i) every open bug in the batch lands in the Brain(ii) `brain_check.py` is 0 fail(iii) `validate_itinerary.py` passes on the affected guide(iv) `verify_booking_links.py` / other active gates pass. Upstream Brain edits + `brain_check.py` + `validate_itinerary.py` runs-to-surface-more-bugs are green-lit throughout the freeze. the owner 2026-04-24: *"The brain has the travel_profile and CSS. No bug fix should be made directly to any guide or HTML. The fixes need to be in the travel_profile and CSS. Fixes are done in the brain and never outside. So that the fixes are there in perpetuity for all the guides that will be run to build it. All the solutions need to be long-term solutions. The code is used to generate tons of guides and is a waste of time and tokensas it is not feasible to keep fixing the severity of every guide. No guides will be rebuilt until all the code is fixed in the brain and bug-free. Alsolet's add that PDFs are not generated while we are working to fix bugs."* **Check mechanism — session-end enforcement.** At the end of any session where Brain edits happenedthe cleanliness check asserts that no `Travel/Guides/` HTML files were modified during the session (git-style diffor file-mtime check against session-start) and no `*.pdf` files were created. Modifications in `Guides/` during a Brain-fix session = FAIL. PDF creation during a Brain-fix session = FAIL. The freeze is lifted only by the owner's explicit signal that the Brain is done-for-now; then one rebuild per affected guide and one PDF render per guide runscleanly. Reason: prior sessions burned cycles doing "quick rebuilds to show the fix in context" that shipped guides with the remaining open bugs still liveand burned tokens re-rendering PDFs that would be superseded by the next fix. The freeze is the mechanism that makes "fix once in the Braininherit forever" actually hold. *(Editorial + mechanical rule — the session-end checks land in `brain_check.py` when the session-state infra is wired; until thenthis is a self-enforced Pre-Flight gate the operator runs mentally.)*

167. **Cleanup = archivenot delete: files named in a cleanup request move to `Travel/archive/`never `rm`.** Per `Rules for Claude.html` archive rule (new2026-04-24). When the owner says "clean up," "delete unused files," "get rid of," or any variantthe default action is to `mv` each named file into the central archive `Travel/archive/` (R222026-05-06 — single destinationnever per-guide or per-folder subfolders) — NOT `rm` it. the owner 2026-04-24: *"cleanup = to archiving for me. I delete there once in a while."* The proposal must label the list **"Archive candidates"** (not "Delete candidates") and name the destination archive for each item. `rm` is reserved for (a) regenerating caches (`__pycache__/``.pyc``.DS_Store`) and (b) items the owner has explicitly flagged as "permanently delete." Anything else archives. **Check mechanism — session-end file-loss audit.** At the end of any session that performed removals on behalf of a "cleanup" requestthe cleanliness check asserts that for every file removed from its source location there exists a file of the same name inside an `archive/` folder under `Travel/` (implementation: scan git-diff deletionsconfirm each deleted filename also appears as a git-diff addition inside an `archive/` path). A deletion with no matching archive entry and no explicit "permanently delete" green light from the owner = FAIL. Reason: on 2026-04-24 during a "cleanup" the owner approved11 preview HTMLs + a stale root-level ASK file were `rm`-ed instead of archived. the owner corrected the pattern immediately; this rule + check hardens the correction so future instances can't lose the precedent when auto-memory evicts. Pairs with Rule 12 (Permissioning) and Rule 0a (movenever delete). *(Editorial + mechanical rule — the session-end file-loss audit lands in `brain_check.py` when the session-state infra is wired; until thenthis is a self-enforced Pre-Flight gate the operator runs mentally.)*

168. **Archive is permission-gated on both sides — no readsno writesno touchesunless explicitly permitted.** Per `Rules for Claude.html` archive-gate rule (new2026-04-24). The single central archive `Travel/archive/` (R222026-05-06) sits in a permission quarantine. the owner 2026-04-24: *"Claude can't read the archive unless I give permission"* — with clarifications: *"You can add things there if I tell you to clean up or archivebut not read"* and *"unless I permit."* Two permission paths: (i) **write permit** is bundled into a Rule 29 cleanup approval — the cleanup green light IS the write permit for the specific files namedscoped to `mv` into the listed archivenever a standing write-open; (ii) **read permit** is per-fileper-reasonrequested in chat and green-lit by explicitly naming the specific path. Read permission is never per-folder. Scope of the read ban covers `Read``Grep``Glob`-intoany tool that opens archive file contents for Claude-level reasoning. Exception for tooling: mechanical integrity scans by `brain_check.py` / `validate_itinerary.py` / other Brain scripts that skip `archive/` paths are tooling (not Claude-level reading) and are unaffected — their skip-archive behavior is what keeps the archive quarantine intact on the Rule 26 side. **Check mechanism — session-end archive-touch audit (both sides).** At session end the cleanliness check asserts: (A) no `Read`/`Grep`/`Glob` invocation resolved to `**/archive/**` unless the transcript contains an explicit green light for that specific file in the same session → read without permit = FAIL; (B) no `mv` / `Write` / file creation landed under `**/archive/**` unless the session transcript contains a approved cleanup proposal (Rule 29) listing that specific file → write without permit = FAIL. The write-side check pairs with rule 167's archive-coverage audit (every cleanup `rm` must land as an archive write): together they close the cleanup loop — rule 167 says "every deletion must have a matching archive write," rule 168 (write side) says "every archive write must have a approved cleanup backing it." Reason: past Claude instances repeatedly drifted into archive content on the read sideresurfacing retired rules / old templates / old guide markup / retired validator checks as if currentand undoing corrections that had already landed in the live Brain. The archive preserves history per Rule 0a and Rule 11 — it does not inform today's builds. Pairs with Rule 25 (no reading `Guides/` for rule-building)Rule 26 (no real place names in Brain — whose allow-list design already whole-file-skips `archive/`which only works if the read direction is also quarantined)and Rule 29 (cleanup = archivenot delete — which supplies the write-side permit path). *(Editorial + mechanical rule — the session-end archive-touch audit lands in `brain_check.py` when the session-state infra is wired; until thenthis is a self-enforced Pre-Flight gate the operator runs mentally.)*

169. **Older guides are frozen artifacts — no retroactive maintenanceno non-conformance findingsno unsolicited retrofits.** Per `Rules for Claude.html` frozen-guides rule (new2026-04-24). Each guide is a point-in-time build: valid under the Brain that existed the day it shipped. As the Brain evolvespast guides will fall out of conform — retired CSS classesretired templatessince-renamed validator checksold color palettes — and that is the intended state. the owner 2026-04-24: *"We will build hundreds of guidesand older guides are 'dead' unless I invoke them again. I don't want you to worry about maintaining the integrity of all of them. The brain changes and evolvesand guides will be staleout of conform — and it is okay."* Operationally: (a) no retrofits — new rules apply forwardnot backward; (b) no past-guide non-conformance findings in any reportauditor cleanliness pass; (c) no proactive re-validationre-renderor re-ship of past guides; (d) a past guide re-enters active work ONLY when explicitly invokes it by name. Absent invocationpast guides are invisible to the pipeline. **Check mechanism — session-end past-guide-touch audit.** At session end the cleanliness check asserts: no file under `Guides/{City}/` (other than the specific guide explicitly invoked in the session) was openedmodifiedvalidatedre-renderedor flagged as non-conforming → any touch without explicit the owner invocation = FAIL. Also: no session output surfaced a past-guide non-conformance as a "finding," "bug," "cleanup item," or "inconsistency" → any such surfacing = FAIL. Scope: every `Guides/{City}/{city}_v{n}.html` (+ matching `.pdf`) not explicitly named in the current session; every file under any `Guides/{City}/archive/` (which additionally falls under rule 168). Pairs with rule 112 (guide-folder cleanliness — file-organization discipline)Rule 25 (no cribbing from `Guides/` for rule-building)and Rule 30 (archive read-lock). Rule 31 completes the set by closing the "worry surface" — past guides are not just unreadable for rule-building (Rule 25)they are also not a maintenance backlog (Rule 31). Reason: past Claude instances have repeatedly flagged past-guide non-conformance as bugsproposed retrofits not requestedand treated old guides as a rolling integrity surface — which produces noisewastes tokensand sometimes mutates a shipped artifact not requested to be touched. the owner 2026-04-24: *"I just wanted to make your life easier and less stressful. I see you worry."* *(Editorial + mechanical rule — the session-end past-guide-touch audit lands in `brain_check.py` / `guide_tools.py audit` when the session-state infra is wired; until thenthis is a self-enforced Pre-Flight gate the operator runs mentally.)*

170. **Active-guide bug work is root-cause work — surface every bug before fixing any; don't rush to rebuild.** Per `Rules for Claude.html` root-cause rule (new2026-04-24). When a bug surfaces on the active guidethe posture is "find the rest before touching the first" — run every validatorread end-to-end for driftcompile the full bug listthen strengthen the Brain (per Rule 23) once per bugthen one rebuild at the end (per Rule 28). the owner 2026-04-24: *"It will rebuild eventuallybut it is more meaningful to work out the bugscracksinconsistenciesbroken rulesetc.than to rush to fix it."* **Shared trigger phrase "root-cause pass"** — when used itRule 32 is in force by name (no need to infer). **Check mechanism — session-end active-guide-pace audit.** At session end the cleanliness check asserts: (A) if ANY active-guide bug was surfaced during the sessionthe transcript shows a full-validator sweep (`validate_itinerary.py` + `verify_booking_links.py` + any other active gate) was run on the guide BEFORE the first Brain edit landed — partial sweep = WARNno sweep = FAIL; (B) if Brain edits were madeeach one has a paired "why" note naming the bug it roots out (no rule strengthening without a surfaced-bug justification); (C) if a guide rebuild or PDF render happened during the sessionall surfaced bugs were closed AND `brain_check.py` passed clean BEFORE the rebuild step — violates Rule 28so the failure mode here is "rebuild between fixes" → FAIL. Pairs with rule 166 (Brain-firstbug-fix freeze — the Rule 23/28 checks)rule 169 (past-guide non-conformance → not a finding)rule 111 (root-cause-first culture). Rule 170 closes the remaining surface on the active-guide timeline: rules 166/169 handle "where" and "past"; rule 170 handles "pace." Reason: past Claude instances have repeatedly rushed from "bug visible" → "fix written" → "rebuild" without the intermediate audit passproducing (1) rebuilds that fix one bug and ship the next three(2) closed bugs that had no rule-strengthening behind them and so recur in the next city(3) apology language around the correct (slow) posture that reinforces the wrong (fast) reflex. the owner 2026-04-24: *"I know you get anxious about fixing them fast. Not sure how to help in this regard. Is there anything we could do to help you?"* — the rule IS the help. *(Editorial + mechanical rule — the session-end active-guide-pace audit lands in `brain_check.py` / `guide_tools.py audit` when the session-state infra is wired; until thenthis is a self-enforced Pre-Flight gate the operator runs mentally.)*

171. **[RETIRED 2026-04-25 — Type 3 Alternation Stops un-retired]** ~~A `.stop-block` never carries BOTH a `.tour-box` AND a `.ticket-box` — hard FAIL.~~ Per the brain restructure of 2026-04-25 (`Brain/CORE RULES/Stops Structure.html`)Type 3 Alternation Stops are a re-lockedvalid stop type — a `.stop-block` carrying both boxes is the alternation shapewith the title icon auto-flipping to "🚩 or 🎒" via CSS `:has()`. the owner 2026-04-25: *"yesthis is all right... should not be retired... we just created"* — locking Type 3 alternation as the third of four stop types. The validator hard-fail in `validate_itinerary.py` was removed; the CSS auto-flip rule in `Guide Style.css` was restored. The XOR constraint moved to the `self` modifier only (Type 2 Self-Guided is XOR — Option 1 OR Option 2never both — alternation lives on the `guided` modifier). See Rule 156 for the current 4-stop-type taxonomy. Prior wording preserved per Rule 11: ~~"The validator iterates every `.stop-block`counts `.tour-box` wrappers and `.ticket-box` wrappers inside; if both counts are ≥1 it hard-fails with a message naming the stop and pointing at §Tour-First Rule."~~ Prior retirement lineage is in `Travel/archive/alternation_stops_retired_2026-04-24.md` (now superseded by the 2026-04-25 re-lock). *(Retired — validator hard-fail removed 2026-04-25; replaced with `self`-modifier XOR enforcement inside the modifier-body matching routine.)*

172. **PDF rendering is on-demand only — `ship` never invokes `render_pdf.py` or `validate_pdf.py` automatically.** Per `Rules for Claude.html § 8` (on-demand outputrewritten 2026-04-24). The automatic pre-ship chain is `validate + verify + verify-booking` (per Rule 173) — nothing else. `render_pdf.py` and `validate_pdf.py` remain callable as standalone `guide_tools.py pdf` / `guide_tools.py validate-pdf` subcommandsbut they run only when explicitly asks for a PDF in chat. the owner 2026-04-24: *"the PDF should be done on demand only. only when I ask and not automatic."* The PDF is still the canonical phone-reading artifact ("PDF is used on mobile on her iPhone on the ground" — Rules for Claude.html § 8) — its canonicity is unchangedonly its trigger moved from "automatic after every verify pass" to "on the owner's ask." Reason: prior `ship` runs rendered a PDF after every verify passburning Chromium startup time and disk space on every iteration even when the owner was only checking HTML/link state; the owner wanted the phone artifact produced deliberatelynot as a side effect. **Check mechanism — ship-chain inspection.** At session start `brain_check.py` reads `guide_tools.py` and asserts the `if cmd == "ship":` block iterates over the exact three-gate chain per Rule 173 — any `pdf` or `validate-pdf` token inside the ship loop = FAIL. Additionallythe `guide_tools.py` docstring must carry the "ON-DEMAND ONLY" markers on the `pdf` and `validate-pdf` entries; missing marker = WARN. Pairs with Rule 166 (Brain-firstbug-fix freeze — which also forbids PDF rendering while Brain bugs are open); Rule 166 governs timing ("no PDFs while bugs are open")Rule 172 governs the default trigger ("PDFs are never automaticfreeze or no freeze"). *(Editorial + mechanical rule — the ship-chain inspection lands in `brain_check.py` when the inspector is wired; until thenthe rule is self-enforced by the `ship` function body in `guide_tools.py` which hard-codes the three-gate tuple.)*

173. **Ship chain shape is exactly `validate + verify + verify-booking` — three gatesfail-fasthard-fail on any non-zero.** Per `Rules for Claude.html § 8` → pre-ship pipeline (rewritten 2026-04-24). The `if cmd == "ship":` block in `guide_tools.py` must iterate over `("validate""verify""verify-booking")` in that ordercall each via `_run(SUBCOMMANDS[sub]tail)`and return immediately on the first non-zero exit code. **No other subcommands are part of the automatic chain.** the owner 2026-04-24after the `OPEN_ITEMS.md` item #5 audit surfaced that `verify_booking_links.py` existed as an orphan script (not wired into `ship`not even present in `SUBCOMMANDS`) despite being the authoritative ship-gate for rules 157/158/159: *"i agree hard fail"* — confirming that any FAIL from `verify_booking_links.py` blocks the ship the same as `validate`/`verify` FAILno WARN fallback. **Why three gatesnot two.** `verify_urls.py` (the second gate) checks HTTP 200 + non-empty prose but CANNOT detect subject drift: a TripAdvisor URL whose `d{AttractionID}` was silently reassigned to a different attraction still returns 200 with abundant prose — the URL shape is validthe page is not about the stop. `verify_booking_links.py` (the third gate) fetches the URLextracts the `<h1>`and compares it against the stop's venue name; also enforces log-coverage for bot-blocked platforms (Viator / GetYourGuide / Michelin / TripAdvisor return HTTP 403 to automated fetch — the only record that a human verified a specific-product URL is the `{guide_folder}/verification_log.json` entry). Rules 157/158/159 define the individual check semantics; Rule 173 enforces the CHAIN SHAPE — the rule that all three scripts actually runin orderon every `ship`. Reason: pre-2026-04-24 `verify_booking_links.py` existed but could not be invoked via `guide_tools.py` at all — it was an orphanand `ship` silently skipped the subject-drift / log-coverage enforcement despite rules 157/158/159 claiming the gate was active. **Check mechanism — ship-chain inspection.** At session start `brain_check.py` reads `guide_tools.py` and asserts: (a) the `SUBCOMMANDS` dict contains the three keys `"validate"``"verify"``"verify-booking"` mapping to `validate_itinerary.py``verify_urls.py``verify_booking_links.py` respectively — any missing key or mismatched value = FAIL; (b) the `if cmd == "ship":` block iterates over the exact tuple `("validate""verify""verify-booking")` in that order — any reorder / omission / extra entry = FAIL; (c) each of the three scripts exists as a real file in `Brain/` — missing file = FAIL. Pairs with Rule 172 (PDF rendering on-demand only — together Rules 172 and 173 fully describe the automatic-chain shape: three gatesno PDFsno bundleno extras). *(Editorial + mechanical rule — the ship-chain inspection lands in `brain_check.py` when the inspector is wired; until thenthe rule is self-enforced by the `ship` function body in `guide_tools.py` which hard-codes the `("validate""verify""verify-booking")` tuple.)*

174. **⚠️ Warning stop flag — never carries weather or climate content.** Per `Brain/CORE RULES/Stops Structure.html` § Stop Flags. The ⚠️ flag is for *important caveats* — long linessecurityticket-required-by-datedress codeetc. Static climate claims (`micro-climate``afternoon clouds common``bring a layer``avoid in summer``windy in March`etc.) are drift — weather is on-demand only per `On Demand/Weather - On Demand.html` and `CLAUDE.md` § On-demand documents. The validator scans every `⚠️` row inside `.stop-block` and hard-FAILs on weather / climate / wind / sun / rain / temperature / season tokens adjacent to the warning. Reason: a prior build shipped `⚠️ Bring a light layer — afternoon clouds common in May` as an attraction warning; weather forecasts change too quickly to bake into a guide built ahead of the trip. Relocated 2026-04-26 from `Stops Structure.html` per `Brain/CORE RULES/Rules for Claude.html`. *(Enforced — validate_itinerary.py ⚠️-warning weather-token scrub; 2026-04-26.)*

175. **🗓️ Weekly Closures section — recurring weekly patterns only; no national holidays.** Per `Brain/CORE RULES/Weekly Closures - Extra Section.html`. The `#weekly-closures` section ships only city-wide recurring weekly patterns (`Museums & Galleries · Closed Tuesday``Palaces · Closed Monday``Boutiques & Stores · Closed Sunday`). Calendar-specific dates — `1 January``25 December``Easter Sunday``13 June``Bastille Day`etc. — never appear here. If a national holiday genuinely falls inside the trip window AND meaningfully closes city venuessurface it on the affected day's transit banner or stop flag instead. The validator scans the `#weekly-closures` section text for date tokens (ISO datesmonth names + daynamed holidays) and hard-FAILs on any hit. Reason: the owner 2026-04-26 caught a build that listed Christmas Day under Weekly Closures when she wasn't traveling in December — *"i am not going to be there during xmasso a drift."* Relocated 2026-04-26 from `Weekly Closures - Extra Section.html` per `Brain/CORE RULES/Rules for Claude.html`. *(Enforced — validate_itinerary.py weekly-closures date-token scrub; 2026-04-26.)*

176. **🚃 Tram subsection in Getting Around — never leads with the word "Yes" or "No".** Per `Brain/CORE RULES/Getting Around - Extra Section.html` § Tram. The tram answer is a declarative sentence using the locked templates verbatim (`{City} has a tram system — operated by {operator}.` or `{City} does not have a tram system — use walk or ride-app instead.`). Phrasings like `Yes — {City} has a tram` or `No — {City} doesn't have one` read as a quiz response and are recurring drift flagged multiple times. The validator scans the `#getting-around` section's tram subsection for any line / paragraph beginning with `Yes ` / `No ` / `Yes,` / `No,` / `Yes —` / `No —` (case-insensitive) and hard-FAILs on a hit. Reason: catches the quiz-style phrasing before ship. Relocated 2026-04-26 from `Getting Around - Extra Section.html` per `Brain/CORE RULES/Rules for Claude.html`. *(Enforced — validate_itinerary.py tram-subsection Yes/No-lead scrub; 2026-04-26.)*

177. **Title page scope — three linesexactly: City + Hotel name + Address. Nothing else.** Per `Brain/CORE RULES/Hotel Banner.html`. The `.title-page` block contains exactly `.title-city` + `.title-hotel` + `.title-address` and no other content. The hotel-naming rule + the always-`FROM HOTEL` banner rule live in the Doc; this entry catches drift onto the title page itself: no separate "Google Maps" / "Apple Maps" rowsno "Hotel Website" linkno overview textno priceno flight infono country/date headerno anything. The existing Rule 154 (title-card scope check) covers most out-of-scope tokens (currencydatesroom typesamenitiesetc.); this entry adds the structural ban: `.title-page` must contain exactly three direct children `.title-city``.title-hotel``.title-address` — any extra div / paragraph / link inside the title page = FAIL. Relocated 2026-04-26 from `Hotel Banner.html` per `Brain/CORE RULES/Rules for Claude.html`. *(Enforced — validate_itinerary.py title-page-children-shape check; 2026-04-26. Pairs with existing Rule 154.)*

178. **Local Tastes — no country-level clichésno generic dishes-on-every-menu.** Per `Brain/CORE RULES/Local Tastes - Extra Section.html` § What ships → The bar. The Local Tastes section ships only foods or drinks tied to a specific place's identity (the positive examples used live in the Doc — birthplace dishesregional café institutionslocally-invented drinks). Country-level clichés (`pasta in Italy``croissant in France``sushi in Japan``tapas in Spain`) and generic-everywhere dishes (`steak frites``basic sandwiches``plain pizza`) drift the section into noise. The validator scans every `#local-tastes` `.extras-sub` entry header for the cliché-name patterns above (case-insensitive substring match) and hard-FAILs on a hit. Editorial-rule for "generic dish on every menu" since menu-genericity isn't machine-classifiable; the cliché-name scrub is the mechanical backstop. Relocated 2026-04-26 from `Local Tastes - Extra Section.html` per `Brain/CORE RULES/Rules for Claude.html`. *(Enforced — validate_itinerary.py local-tastes cliché-name scrub; editorial-rule for "generic" classification; 2026-04-26.)*

179. **🎟️ row in Shows — links to venue's own website only; no third-party ticketing aggregators.** Per `Brain/CORE RULES/Shows, Performances & Concerts - Extra Section.html` § Where the 🎟️ link goes. The 🎟️ row in every Shows entry must link to the venue's own website (e.g.`lascala.milano.it``wienerstaatsoper.at``royaloperahouse.org.uk`). Third-party aggregators are banned: `vivaticket.com``vivaticket.it``ticketmaster.com``ticketmaster.it``ticketmaster.co.uk``ticketmaster.fr``todaytix.com``seetickets.com``fnacspectacles.com``eventim.com``eventim.de``ticketone.it``ticketcorner.ch`. The existing MICHELIN BOOK-VIA OWN-SITE / SHOWS BOOK-VIA OWN-SITE checks in `validate_itinerary.py` already enforce this denylist; this entry documents the rule on the cleanliness surface as the relocated guardrail home. Relocated 2026-04-26 from `Shows, Performances & Concerts - Extra Section.html` per `Brain/CORE RULES/Rules for Claude.html`. *(Enforced — validate_itinerary.py SHOWS BOOK-VIA OWN-SITE check; pre-existinganchored 2026-04-26.)*

180. **Day route discipline — no backtrackingno re-entering a left neighborhoodno walking past a finished stop.** Per `Brain/CORE RULES/Day Structure.html` § Day shape — geographic clustering. The positive shape (one walking loop / one-way path / two clusters joined by one tram or ride-app hop) lives in the Doc. This entry catches the route-discipline drift: at ship timethe validator extracts each `.day-block`'s ordered stopschecks for repeated stop names within a day (no doubling-back on the same venue)checks for transit-banner walk times that exceed cluster-coherence thresholds (`🚶 > 20 min` between two stops within the same neighborhood = WARNsuggesting cluster-split is needed)and surfaces sequences where a stop earlier in the day is walked past in a later transit-banner destination. Sub-entry: stop-count discipline — *meaningful over full*; never pad to hit the 4-stop floor (already covered editorially by Rule 166 *"Brain-firstbug-fix freeze"* and the §Stop Inclusion Bar). Relocated 2026-04-26 from `Day Structure.html` per `Brain/CORE RULES/Rules for Claude.html`. *(Enforced — validate_itinerary.py day-route discipline checks; cluster-coherence WARNrepeat-stop FAIL; 2026-04-26.)*

181. **Tour-search discipline — never default to self-guided without first running the Viator / GYG / TA tour search.** Per `Brain/CORE RULES/Tours - Extra Section.html` § For each stop and `Brain/CORE RULES/Rules for Claude.html` § Tour-firstALWAYS. Every paid attraction warrants a tour search; only confirmed-empty searches justify falling to step 2 (skip-the-line ticket); only confirmed-empty waterfall justifies step 3 (venue's own site). The validator's existing tour-first-checked sentinel system (Rule 22) enforces this at ship time — every self-guided or skip-the-line stop must carry `<!-- tour-first-checked: stop=... query=... decision=... reason=... date=... -->` evidence. This entry documents the rule on the cleanliness surface as the relocated guardrail homeanchoring the planning-time discipline alongside the ship-time gate. Relocated 2026-04-26 from `Tours - Extra Section.html` per `Brain/CORE RULES/Rules for Claude.html`. *(Enforced — validate_itinerary.py tour-first-checked sentinel; pre-existinganchored 2026-04-26.)*

182. **🚃 Tram body matches one of the three locked templates verbatim.** Per `Brain/CORE RULES/Getting Around - Extra Section.html` § Tram. The tram body must match one of: (i) `{City} has a tram system — operated by {operator}. Line(s) used on this trip: {N}.`(ii) `{City} has a tram system — operated by {operator}. No tram rides on this trip.`or (iii) `{City} does not have a tram system — use walk or ride-app instead.` — verbatim except for the `{City}` / `{operator}` / `{N}` placeholders. Drift like *"{City}'s tramoperated by {operator}runs lines 28E and 25E."* still passes the existing Yes/No-lead scrub (Rule 176) but violates the locked-template rule. The new check parses the 🚃 Tram `.transit-box` body and hard-FAILs if it doesn't match one of the three locked patterns. Reason: closes the variant-phrasing drift class fully — Rule 176 was a partial fix. Added 2026-04-27 per audit pass on Getting Around v5: *"Please check whether validators are checking as much of this section as possible."* *(Enforced — validate_itinerary.py TRAM TEMPLATE ADHERENCE; 2026-04-27.)*

183. **🚆 Stations near the hotel — walk > 30 min always has a 🚕 ride-app fallback.** Per `Brain/CORE RULES/Train Stations Near Hotel - Extra Section.html` § Stations near the hotel: *"No walking-distance limit — always show the closestno matter how far. If walking time is > 30 minalso add a ride-app time so there's a practical alternative."* The existing walk+ride-app pair check (Rule 25 / Motion Rule) covers transit banners (`.next``.next-tram``.hotel-first``.depart`) but station entries inside the Getting Around extras-section are a separate surface the Motion-Rule check doesn't scan. The new check parses the 🚆 / 🚄 Stations `.transit-box`finds every `🚶 ~N min`and hard-FAILs if N > 30 and no `🚕 ~M min` line follows in the same entry. Reason: stations can be far (especially HSR stations on the city outskirts)and a 45-min walk without a ride-app alternative is impractical reading. Header-glyph note: both 🚆 (default) and 🚄 (HSR-only) are accepted as the section header per the Doc's glyph rule. Added 2026-04-27 per the audit pass on Getting Around v5. *(Enforced — validate_itinerary.py STATIONS WALK >30 RIDE-APP FALLBACK; 2026-04-27.)*

184. **🚆 Stations near the hotel — empty section ships the negative-finding line.** Per `Brain/CORE RULES/Train Stations Near Hotel - Extra Section.html` § Stations near the hotel (rule added 2026-04-27): *"If the city has no train stations at allship one line: No train stations in {City}."* Without enforcement the new rule was honor-system only — a future Claude could ship an empty Stations `.transit-box` and the reader couldn't tell whether we looked or forgot. Mirrors the negative-finding-line pattern already used for Michelin / Local Tastes / Weekly Closures / Historic Downtown (all declared in `CLAUDE.md`). Detection: empty = no 🚶 walk-time markers anywhere in the body (= no station entries). Empty section MUST contain `No train stations in {City}.` (with `{City}` being any non-whitespace token). Hard-FAIL when neither station entries nor the negative-finding line are present. Added 2026-04-27 per the audit pass on Getting Around v5. *(Enforced — validate_itinerary.py STATIONS EMPTY-CASE NEGATIVE FINDING; 2026-04-27.)*

185. **🚕 Ride apps — empty section ships the negative-finding line.** Per `Brain/CORE RULES/Getting Around - Extra Section.html` § Ride apps (rule added 2026-04-27): *"If no ride app works in the city at allship one line: No ride apps available in {City} — use taxi or walk."* Mirrors Rule 184's drift class — closes the looked-vs-forgot gap on the brand-new rule. Detection: empty = no per-app labels (`primary` / `backup` / `not available`) found in the body. Per the Doc format ruleevery app entry must carry one of those three labels — so absence of all three = no app entries = empty sectionwhich MUST contain `No ride apps available in {City}` followed by the `— use taxi or walk` fallback clause. Added 2026-04-27 per the audit pass on Getting Around v5. *(Enforced — validate_itinerary.py RIDE APPS EMPTY-CASE NEGATIVE FINDING; 2026-04-27.)*

186. **📖 Wikipedia link text is exactly the bare word `Wikipedia`.** Per `Brain/CORE RULES/Links.html` § 📖 Wikipedia: *"Link text: just `Wikipedia` — no descriptorno article-title appendedno `Official Site` tag."* Drift class: link text drifts to `Wikipedia article``Wikipedia — {Subject}``Wikipedia: {Subject}`or just the subject name. The locked text is the bare word `Wikipedia`case-sensitiveno trailing punctuation. The new check parses every `<a>` whose href points at `en.wikipedia.org` and hard-FAILs if the anchor text is not exactly `Wikipedia`. Reason: a guide reading `Wikipedia article` looks like a generic link; the locked bare-word form is part of the deliberate visual rhythm of the stop block. Added 2026-04-27 per the Links.html validator-coverage audit pass. *(Enforced — validate_itinerary.py WIKIPEDIA LINK TEXT — exactly 'Wikipedia'; 2026-04-27.)*

187. **Every 📍 in the guide is followed by a Google Maps `<a>` link.** Per `Brain/CORE RULES/Links.html` § 📍 Google Maps: *"Every address in the guide is a clickable Google Maps link — every time. ... This applies to every address in the guide — title-card hotelmeeting points in 📅 tour boxesticket-box venuesstation entriesrestaurant entriesMichelin entriesetc. Same convention everywhere."* The pre-existing `.title-address` Maps-link check (validate_itinerary.py line ~2610) and `.entry-body` Maps-link check (line ~1934) cover specific surfaces; this rule is the comprehensive sweep — every 📍 occurrence anywhere in the rendered HTML must be immediately followed (within 250 charsallowing for whitespace + opening `<a>` tag) by an `<a href>` containing `google.com/maps`. Catches drift on surfaces not explicitly covered elsewhere (`.tour-meet` 📍 meeting points`.ticket-box` 📍 venue addressesstation-entry 📍transit-banner 📍restaurant-row 📍). Added 2026-04-27 per the Links.html validator-coverage audit pass. *(Enforced — validate_itinerary.py EVERY 📍 IS A GOOGLE MAPS LINK; 2026-04-27.)*

188. **Address anchors don't leak country names.** Per `Brain/CORE RULES/Links.html` § 📍 Google Maps: *"Anchor text: the address itselfformatted as `Street · Neighborhood` with a middle-dot separator. No country."* The PDF marks the country-leak shape `{Street}{Postal} {City}{Country}` as ❌ explicitly with a real-city example (see Links.html for the verbatim case). The new check sweeps every Maps-linked anchor in the guide (`<a href="...google.com/maps...">{text}</a>`) and hard-FAILs if the anchor text's last comma-separated segment is a country name. The country-name lookup table (operational data per Rule 26) covers the owner's typical travel destinations — Europe-heavy (FranceItalySpainPortugalGermanyBelgiumNetherlandsSwitzerlandAustriaGreeceetc.) plus major-city pairs elsewhere (JapanUSACanadaetc.). Not exhaustive — additions are cheap when a new destination surfaces. Reason: the locked address shape is `Street · Neighborhood` with no country (postal codes banned 2026-05-19); a country-leak `France` widens the anchor text past spec and breaks the visual rhythm. Added 2026-04-27 per the Links.html validator-coverage audit pass. *(Enforced — validate_itinerary.py ADDRESS ANCHOR FORMAT — country leak; 2026-04-27.)*

189. **Address anchors use the middle-dot ` · ` separator before postal+citynot a comma.** Per `Brain/CORE RULES/Links.html` § 📍 Google Maps: *"Anchor text: the address itselfformatted as `Street · Neighborhood` with a middle-dot separator."* The PDF marks the wrong-separator shape `{Street}{Postal} {City}{Country}` as ❌ (see Links.html for the verbatim case) — the separator is wrong (and there's also a country leakcaught by Rule 188). The new check sweeps every Maps-linked anchor's anchor text and hard-FAILs when a postal-code pattern is present (`\b\d{4,5}\b`) AND a comma is present AND the middle dot `·` is absent — the combination indicates the comma is being used as the separator instead of the locked middle dot. Edge case handled: addresses with internal commas inside the street part (e.g.`Building 5Floor 3 · 75001 SomeCity`) still pass because the middle dot is also present. Edge case skipped: addresses with no postal code (e.g.a tour meeting point `In front of the {Landmark}`) — the check only fires when there's a postal-code patternsince the locked separator rule applies to the `Street · Postal+City` shape specifically. Added 2026-04-27 per the Links.html validator-coverage audit pass. *(Enforced — validate_itinerary.py ADDRESS ANCHOR FORMAT — middle-dot separator; 2026-04-27.)*

190. **Google Maps URLs match the locked URL pattern `https://www.google.com/maps/search/?api=1&query=...` exactly.** Per `Brain/CORE RULES/Links.html` § 📍 Google Maps: *"URL pattern: https://www.google.com/maps/search/?api=1&query={url-encoded-full-address}."* The pre-existing pin-sweep check (Rule 187 — every 📍 is followed by an `<a>` containing `google.com/maps`) is loose-substring; this is the strict locked-pattern check. Drift class — non-canonical Maps URL shapes that still resolve but break the locked spec: `http://` (not https)`https://www.google.com/maps?q=...` (legacy `?q=` form)`https://www.google.com/maps/place/...` (place-page form)`https://maps.google.com/?q=...` (subdomain form)`https://goo.gl/maps/...` (shortened form). All five resolve to working maps but break the locked pattern. The locked search-with-`api=1` form gives the most consistent rendering across desktop + mobile + iOS — that's why the Doc locks it. The new check finds every `<a href>` whose URL hits any Google-Maps host shape (`google.com/maps``maps.google.com``goo.gl/maps`) and hard-FAILs unless the URL begins with `^https://www\.google\.com/maps/search/\?api=1&query=`. Added 2026-04-27 per request *"Lets add DF and G"* + *"Links is where we run into 80% of the problems"* framing — D was initially skipped on brittleness concerns; the owner's high-risk-surface call locks the canonical-URL discipline for static enforcement. *(Enforced — validate_itinerary.py GOOGLE MAPS URL — locked pattern; 2026-04-27.)* 🔄 SCOPE-NARROWED 2026-05-17: the locked-pattern check now applies **only to 📍 rows** (URLs found within 250 characters of a 📍 emoji). Extras-sub review links in Cappuccino / Restaurants Near Hotel / Downtown / Michelin headings legitimately use `maps.google.com/?q=``google.com/maps?cid=`or `/place/` URL forms and are exempt from the `^https://www\.google\.com/maps/search/\?api=1&query=` requirement. (validate_itinerary.py GOOGLE MAPS URL — locked patternline ~739; updated 2026-05-17.)

191. **Every 📖 occurrence is followed by a non-empty `<a href="http(s)://...">` link.** Per `Brain/CORE RULES/Links.html` § 📖 Wikipedia: *"When no substantive Wikipedia article exists: drop the 📖 row entirely. Don't substitute another source."* The "absence" decision is editorial — a static check can't tell whether a stop SHOULD have a 📖 row or not. But if the row IS presentthe new check requires every part of it to be valid. Static gap closed: a bare 📖 with NO `<a>` at allOR a 📖 with an empty / placeholder href (`""``#``javascript:...`)silently slipped through every existing 📖 check above (WIKIPEDIA-ONLY at line ~2818WIKIPEDIA URL STRUCTURE at line ~2876Rule 186 link text) because those checks all match `📖 ... <a href="(https?://[^"]+)"` — if no `<a>` matchesthe URL is not collected and nothing fires. The new check finds every 📖 in the rendered HTML and hard-FAILs if the immediately-following content (within 250 charsallowing whitespace + opening `<a>` tag) is not an `<a href="http(s)://...">` with a non-empty http(s) URL. The downstream host / structure / link-text / h1-match / 100-word gate checks then take over for a real link. Added 2026-04-27 per request *"Lets add F"* framing — F's positive rule (drop-the-row-if-no-article) is editorial and unenforceablebut the negative form (every-present-📖-must-be-a-real-link) is the static gap that closes the rule's enforceable surface. *(Enforced — validate_itinerary.py 📖 ROW — VALID NON-EMPTY ANCHOR; 2026-04-27.)*

192. **Every URL in a shipped guide is verified at ship time — composite of existing infrastructure (verify_urls.py + verify_booking_links.py + ship-chain shape Rule 173 + log-freshness Rule 157).** Per `Brain/CORE RULES/Links.html` § Every link is live-verified: *"Including every edit inside a session. A one-character URL change re-runs verification."* Aggregate-documentation entry — the rule is fully enforced via the existing chain; this entry anchors that aggregation on the cleanliness surface so the rule is visibly anchorednot implicit. The enforcement chain: (a) `verify_urls.py` (ship-gate 2 of 3) checks HTTP 200 + ≥100-word content for every URL on every `ship` invocation; (b) `verify_booking_links.py` (ship-gate 3) adds subject-drift `<h1>` match for TripAdvisor + Wikipedia URLs (Rules 158/159) plus log-coverage for bot-blocked platforms (Rule 157); (c) Rule 173 (three-gate ship-chain shape) ensures both verification scripts always run before any ship; (d) Rule 157 (verification log freshness) requires bot-blocked URL log entries within 90 days. The "in-session re-runs on URL edit" rule is a Claude-build-behavior expectation — between ship runs the verification timestamps are kept fresh by Claude's build flow; the ship-gate catches any URL that wasn't re-verified after editing because it always re-runs the full check chain. What is NOT a static-file check: an in-session enforcement that fires the moment a URL is edited (would require either invasive URL-inventory infrastructure with per-URL timestamps embedded in the HTMLOR a session-level Claude rule in `Rules for Claude.html` — both heavier than the existing ship-gatewhich catches the same drift class with less complexity). Added 2026-04-27 per request *"Lets add DF and G"* + *"unless you think it makes no sense to do so"* framing — for G specificallyClaude's recommendation was to skip a new validator check (would either duplicate existing infrastructure OR require heavy URL-inventory infrastructure) and instead anchor the rule explicitly on the cleanliness surface; the owner: *"I trust your recommendation"*. *(Enforced — composite of existing infrastructure: validate_itinerary.py + verify_urls.py + verify_booking_links.py + ship-chain shape Rule 173 + log-freshness Rule 157; aggregated 2026-04-27.)*

193. **🍴 Restaurants Near Hotel — top-3 entries within ≤15 min walk from the hotel.** Per `Brain/CORE RULES/Restaurants Near Hotel - Extra Section.html` § What each top-3 entry looks like: *"Top 3 ordered by walk time (closest first)all within ≤15 min walk."* The new check parses the `#restaurants` extras-sectioniterates every `.extras-sub` heading + body pairskips the hotel-restaurant entry (signaled by 🏨 row)extracts the `🚶 ~{N} min` walk-time from each top-3 entry's bodyand hard-FAILs if N > 15. Reason: the section's premise is "decent meal nearby when you're back tired and hungry" — a 22-min walk defeats the purpose. Drift class: a future Claude could pick a higher-rated restaurant farther out and quietly ship it; the cap closes that. Added 2026-04-27 per the Restaurants Near Hotel validator-coverage audit pass. *(Enforced — validate_itinerary.py RNH top-3 entry shape — ≤15 min walk; 2026-04-27.)* **🔄 SUPERSEDED 2026-05-13:** cap raised to ≤25 min walk + top-4 cap. New check at end of `validate_itinerary.py` ("2026-05-13 RULE-ALIGNMENT CHECKS" block); see audit_log.md entry of 2026-05-13.

194. **🍴 Restaurants Near Hotel — top-3 ordered by walk timeclosest first.** Per `Brain/CORE RULES/Restaurants Near Hotel - Extra Section.html` § What each top-3 entry looks like: *"Top 3 ordered by walk time (closest first)..."* The new check extracts every `🚶 ~{N} min` walk-time from top-3 entries in document order and hard-FAILs if the resulting list isn't ascending. Hotel-restaurant entry is excluded (no walk time). Reason: the closest-first ordering means a tired reader scans the section top-down and finds the easiest option first — the rule has UX weightnot just visual. Drift class: a future Claude could ship the entries in rating order or alphabetical order and break the locked walk-time progression. Added 2026-04-27 per the Restaurants Near Hotel validator-coverage audit pass. *(Enforced — validate_itinerary.py RNH top-3 entry shape — sorted ascending; 2026-04-27.)* **🔄 NOTE 2026-05-13:** ordering rule still applies; "top-3" is now "top-4" per the cap rename (see Rule 193 superseded note).

195. **🍴 Restaurants Near Hotel — every top-3 entry has both a 🚶 walk row and a 🏛 hours row.** Per `Brain/CORE RULES/Restaurants Near Hotel - Extra Section.html` § What each top-3 entry looks like: *"{Restaurant Name} ← linked to restaurant's site / 📍 {Address} [Maps-linked] / 🚶 ~{N} min walk from hotel / 🏛 Opens {time} · closed {day}."* The four-row entry shape is locked. Pre-existing checks cover the 📍 row (Rule 187 pin sweep + Rules 188/189 country/separator). The new check enforces presence of the 🚶 row + the 🏛 row in every top-3 entry body. Hotel-restaurant entry is excluded — it has 🏨 in place of 🚶 (zero walk) and the existing line ~2995 check enforces its heading suffix. Reason: a missing 🚶 row removes the "how far is it" data the section is designed around; a missing 🏛 row removes the "is it open" data needed to act on the recommendation. Both are first-order drift risks. Added 2026-04-27 per the Restaurants Near Hotel validator-coverage audit pass. *(Enforced — validate_itinerary.py RNH top-3 entry shape — 🚶 + 🏛 rows present; 2026-04-27.)* **🔄 PARTIAL SUPERSEDE 2026-05-13:** rule shape changed — restaurant name now plain text (no link)hotel-restaurant entry drops `· Hotel Restaurant` suffix. The 🚶 + 🏛 row requirements still hold.

196. **🍴 Restaurants Near Hotel — top-3 heading is linked to the restaurant's own site (not Google Mapsnot Wikipedia).** Per `Brain/CORE RULES/Restaurants Near Hotel - Extra Section.html`: *"{Restaurant Name} ← linked to restaurant's site"*. The heading is the restaurant name wrapped in `<a href>` pointing at the restaurant's own website (own domain — not the Maps URL that lives on the 📍 rownot a Wikipedia articlenot an aggregator like TripAdvisor or TheFork). The new check parses every top-3 `.extras-sub` headingextracts every `<a href>` in itfilters out Maps URLs (`google.com/maps``maps.google.com``goo.gl/maps`) and Wikipedia URLs (`wikipedia.org`)and hard-FAILs if no qualifying restaurant-site link remains. Reason: the heading link is the reader's path to the restaurant's actual site (menureservationshoursatmosphere) — without itthe entry is a name with no way to act on it. Drift class: a future Claude could leave the heading as plain text (no link)or wrap it in the Maps URL (which is what the 📍 row is for)or in a Wikipedia link (wrong source). Hotel-restaurant entry is excluded — its "linked to restaurant's site" rule is conditional on the hotel having a website. Added 2026-04-27 per the Restaurants Near Hotel validator-coverage audit pass. *(Enforced — validate_itinerary.py RNH top-3 entry shape — heading restaurant-site link; 2026-04-27.)* **🔄 RETIRED 2026-05-13:** rule inverted. Restaurant name is now **plain textno link** per Restaurants Near Hotel - Extra Section.html § 3. New check enforces plain text; old "must link to restaurant site" check deleted from validator.

197. **🍴 Restaurants Near Hotel × ⭐ Michelin — no restaurant duplicated across the two sections.** Per `Brain/CORE RULES/Restaurants Near Hotel - Extra Section.html` § How to build the list: *"Skip Michelin-starred restaurants (they're already in their own section)."* The two sections serve different purposes — Restaurants Near Hotel is for "tired and hungry" walks-from-hotel; Michelin is for destination dining. Shipping the same restaurant in both sections is double-counting and breaks the section split. The new check extracts every Michelin restaurant name (plain text from `#michelin .extras-sub`after stripping leading ⭐ stars + any 🐟 seafood-heavy marker)extracts every RNH top-3 restaurant name (plain heading text from `#restaurants .extras-sub`hotel-restaurant entry excluded via 🏨-row signal)and hard-FAILs on case-insensitive set intersection. Drift class: a future Claude could legitimately find a Michelin-starred restaurant within walking distance and ship it in both sections without realizing the section split. The cross-check closes that. Decision on strict count = exactly 3: deliberately skipped per Claude's "force-pad to count" anti-pattern (cf. retired Day Trips by Train day-count floor — same principle: don't fail builds for shipping fewer entries when fewer qualify). Added 2026-04-27 per request *"Green light 2"* + *"Skip I or add I (strict count = 3) - go with your instinct"* framing on Restaurants Near Hotel. *(Enforced — validate_itinerary.py RNH × MICHELIN CROSS-CHECK; 2026-04-27.)* **🔄 NOTE 2026-05-13:** cross-check still valid; top-3 became top-4 cap (rule renamed `Top 4`). **🔄 NOTE 2026-05-30:** cap raised to Minimum 5.

198. **`.title-city` is uppercase.** Per `Brain/CORE RULES/Hotel Banner.html` § Three lines: *"{CITY (uppercase)}"* and *"City — uppercase"*. The locked title-page shape is `{CITY (uppercase)}` on line 1 — a guide shipping a mixed-case or lowercase city name violates the locked shape. Pre-existing Rule 154 (title-card scope check) catches out-of-scope content tokens (currencydatesroom typesetc.) but doesn't validate the case of the city line. The new check extracts `.title-city` text content (HTML stripped) and hard-FAILs if `text != text.upper()` — i.e. it contains a lowercase character. `.upper()` handles unicode correctly: accented uppercase characters (ÉÜÑetc.) pass; non-letter chars (spacesdigitspunctuation) are unaffected. Drift class: a future Claude could ship the city in title-case or lowercase to "look nicer" and break the locked all-caps shape. Added 2026-04-27 per the Hotel Banner validator-coverage audit pass. *(Enforced — validate_itinerary.py TITLE PAGE: .title-city uppercase; 2026-04-27.)*

199. **`.title-page` direct children are exactly `[.title-city.title-hotel.title-address]` in that order.** Per `Brain/CORE RULES/Hotel Banner.html` § Three lines: *"{CITY (uppercase)} / {Hotel Name (bold)} / {Street · Neighborhood} / Three linesin order — that's the entirety of the banner."* And per cleanliness Rule 177 (added 2026-04-26): *".title-page must contain exactly three direct children .title-city.title-hotel.title-address — any extra div / paragraph / link inside the title page = FAIL."* Rule 177 was DOCUMENTED but not actually enforced in code — the existing title-card scope check (line ~2895) catches out-of-scope CONTENT tokens (currencydatesetc.) but doesn't validate the children list or order. Closes the documentation-vs-enforcement mismatch. The new check uses `_walk_balanced_div` to extract the title-page inner HTMLthen `re.findall` to extract every `<div class="...">` in document order. Hard-FAILs if the class list isn't exactly `[title-citytitle-hoteltitle-address]`. Drift class: a title-page shipping `.title-hotel` before `.title-city`or with an extra `.title-subtitle` somewherewould have slipped through every existing check. Added 2026-04-27 per the Hotel Banner validator-coverage audit pass — closes a real documentation-vs-enforcement mismatch. *(Enforced — validate_itinerary.py TITLE PAGE: direct-children list + order; 2026-04-27. Pairs with existing Rule 177 documentation entry which is now actually enforced.)*

200. **`🏨 FROM HOTEL:` banner uses uppercase `HOTEL` (case-sensitive).** Per `Brain/CORE RULES/Hotel Banner.html` § Hotel naming rule: *"the generic word hotel (lowercase in prose) or HOTEL (uppercase in transit banners). The transit-banner formula is always FROM HOTEL..."* The pre-existing day-bookend opener check (line ~4307validate_itinerary.py) uses `re.IGNORECASE` to detect missing openers — it correctly catches days MISSING the opener entirelybut a day shipping `🏨 from hotel:` (lowercase) or `🏨 From Hotel:` (title case) silently passes even though it violates the locked uppercase rule. The new check is a parallel scan: find every from-hotel banner pattern case-INsensitivelythen verify each match is the literal canonical form `🏨 FROM HOTEL:` (case-sensitive). Banners in wrong case = hard-FAIL. Together with the bookend opener checkthe two cover the locked banner shape — bookend catches "missing"this catches "present but wrong case." Drift class: a future Claude could write the banner in title case to "look nicer" and break the locked all-caps `HOTEL` rule. Added 2026-04-27 per the Hotel Banner validator-coverage audit pass. *(Enforced — validate_itinerary.py `🏨 FROM HOTEL:` case-sensitive uppercase; 2026-04-27.)*

201. **`.day-header` text matches one of the three locked patterns.** Per `Brain/CORE RULES/Day Structure.html` § Day-block header format: the `.day-header` text follows ONE of three locked patterns: (i) in-city days = `Day N`(ii) train days = `Day N — Train Day — {Destination City}`(iii) guided coach days = `Day N — Guided Coach to {Destination City}`. Em-dash is U+2014 (the actual character used in the Doc — not a hyphen-minusnot an en-dash). Pre-existing trip-at-a-glance check (line ~2779) validates glance-card title format but the actual `.day-header` text wasn't validated. A guide shipping `Day 1 (Tuesday)``Day 1: {City}`or any other variant slips through every existing check. The new check walks every `.day-header` elementextracts plain text (HTML strippedwhitespace normalized)and matches against the three locked regex patterns. Hard-FAIL on any day-header that doesn't fit. Drift class: future Claude adds parenthetical day-of-week annotations or colon-format alternatives because they "look more informative" — both break the locked shape that the trip-at-a-glance and CSS depend on. Added 2026-04-27 per the Day.html validator-coverage audit pass. *(Enforced — validate_itinerary.py DAY-HEADER FORMAT — three locked patterns; 2026-04-27.)*

202. **Coach-day `.day-block` closer is `🚌 → hotel`.** Per `Brain/CORE RULES/Day Structure.html` § Transit banners → End-of-day return: *"Coach day variant: `🚌 → hotel` (the bus drops at hotel)."* The pre-existing closer check (line ~4459validate_itinerary.py) requires `→ hotel` in the last half of every `.day-block` but doesn't validate the lead-glyph for coach days. A coach day shipping the regular `🚶 N · 🚕 M → hotel` closer (instead of `🚌 → hotel`) silently passes — the bus actually drops the reader at the hotelso the walk+ride-app pair is misleading information. The new check walks every `.day-block`parses the `.day-header`and if the header matches the coach-day pattern (`Day N — Guided Coach to {Destination}`)the day_inner must contain `🚌 → hotel` (whitespace-tolerant). Hard-FAIL if missing. Drift class: future Claude defaults to the regular walk/ride-app closer because that's the in-city patternmissing that coach days have their own variant. Added 2026-04-27 per the Day.html validator-coverage audit pass. *(Enforced — validate_itinerary.py COACH-DAY CLOSER — 🚌 → hotel; 2026-04-27.)*

203. **Train-day `.day-block` SHOULD have a `🚉 ARRIVE {station}:` banner after the outbound train (WARNnot FAIL).** Per `Brain/CORE RULES/Day Structure.html` § Transit banners → Train-day arrival: *"🚉 ARRIVE {arrival station}: 🚶 N min · 🚕 M min → {first stop} (after the outbound trainonly when a real transfer exists)."* The "only when a real transfer exists" qualifier makes this a WARNnot a FAIL — a train-day without a `🚉 ARRIVE` banner is legitimate when the destination station is right next to the first stop (no transfer needed). But missing it when there IS a transfer is a real drift class — the reader doesn't know whether to walk/ride-app from the destination station to the first stop. WARN surfaces the candidate for human review without blocking ship. Detection: walk every `.day-block`parse the `.day-header`and if the header matches the train-day pattern (`Day N — Train Day — {Destination}`)check that the day_inner contains `🚉 ARRIVE` (case-insensitive on the word ARRIVEthe glyph itself is fixed). WARN if missing — call site uses the existing `warn(labeldetail)` helper rather than `check()`. Added 2026-04-27 per request *"Add C anyway with a softer WARN-not-FAIL approach"* — the explicit Tier 2 with WARN semantics handles the "real transfer exists" semantic by surfacing the case for review without forcing the build to fail when the destination is adjacent. *(Enforced — validate_itinerary.py TRAIN-DAY ARRIVAL — 🚉 ARRIVE BANNER (WARN); 2026-04-27.)*

204. **Every 📅 tour-box's numeric star rating is ≥ 4.5★.** Per `Brain/CORE RULES/Tours - Extra Section.html` § 1: *"Rating bar (everywhere): 4.5+★."* Pre-existing tour-box rating check (line ~1387) verifies a star rating is PRESENT (e.g. `4.5★`) but doesn't validate the numeric value. A tour-box shipping `4.3★` slipped through every existing check. The new check extracts the leading numeric value (`(\d\.\d)\s*★`)converts to floatand hard-FAILs if value < 4.5. Drift class: a future Claude could pick a 4.2★ or 4.4★ tour because it has a high review count or a notable operatormissing the strict bar floor. Added 2026-04-27 per request *"green light"* approving the Tour Rules Tier 1 four-pack. *(Enforced — validate_itinerary.py TOUR RULES — rating ≥4.5★; 2026-04-27.)*

205. **Every 📅 tour-box's numeric review count is ≥ 6 (with `0+ reviews` flagged as WARN per round-down-to-10 ambiguity).** Per `Brain/CORE RULES/Tours - Extra Section.html` § 1: *"Rating bar (everywhere): 4.5+★ · minimum 6 reviews."* Pre-existing review-count check (line ~1605) verifies the rounded `+ reviews` format but doesn't validate the numeric floor. A tour-box shipping `5+ reviews` slipped through. Per cleanliness Rule 147review counts round down to nearest 10 for <100 — so a tour with real count 6-9 displays as `0+ reviews`. The new check has two tiers: (a) hard-FAIL when displayed count is 1-5 (definitely below bar; though existing rounded-format check would already FAIL these as "unrounded"); (b) WARN when displayed count is exactly 0 (ambiguous: real count could be 6-9 passing bar OR 0-5 failing bar; surfaces for human review without blocking ship). Counts ≥10 trivially pass the ≥6 bar. Added 2026-04-27 per request *"green light"*. The WARN-on-`0+` design handles the rounding rule's ambiguity without forcing legitimate 6-9-review tours to fail nor letting truly under-bar tours ship silently. *(Enforced — validate_itinerary.py TOUR RULES — review count ≥6 (FAIL on <6WARN on `0+ reviews`); 2026-04-27.)*

206. **Tour-box pickup info uses one of the three locked variants exactly: `🏨 ↔ 🚐` / `🏨 → 🚐` / `🏨 ← 🚐`.** Per `Brain/CORE RULES/Tours - Extra Section.html` § 6 — Pickup variants: *"🏨 ↔ 🚐 — round-trip (pickup + drop-off) · 🏨 → 🚐 — pickup only · 🏨 ← 🚐 — drop-off only."* Pickup info is OPTIONAL ("when applicable") — but when presentmust match one of the three locked patterns exactly. The new check walks every tour-box; if 🚐 OR 🏨 appears in the bodythe body must contain one of the three pattern matches (`🏨\s*[↔→←]\s*🚐` against the three glyph variants). Hard-FAIL when 🚐/🏨 appears but no canonical pattern matches — catches drift like `🏨 (round-trip) 🚐``🏨-🚐`or old coach-first order `🚐 → 🏨`. Updated 2026-05-11: emoji order flipped to hotel-first (🏨 before 🚐) across all guidesrulesand validator. Added 2026-04-27 per request *"green light"*. *(Enforced — validate_itinerary.py TOUR RULES — pickup variant exact match; 2026-04-27.)*

207. **Every `.stop-block` ships ≤ 3 tour-boxes.** Per `Brain/CORE RULES/Tours - Extra Section.html` § 3 — Three tour boxes per stop: *"Plan 3 tour boxes per stop. That's the target. Ship fewer than 3 only when fewer than 3 qualifying tours exist. When more than 3 qualifyship the best 3 — picked on a blend of rating + review count."* The "≥3" bound was previously not enforced — a stop with 4+ tour-boxes would silently ship despite breaking the locked maximum. The new check walks every `.stop-block`counts `<div class="tour-box">` childrenand hard-FAILs when count > 3. Lower bound is intentionally NOT enforced (PDF allows shipping fewer when fewer qualify). Drift class: future Claude adds a 4th tour-box "to be thorough" or because the picker can't decide between two — both break the locked 3-box ceiling. Added 2026-04-27 per request *"green light"*. *(Enforced — validate_itinerary.py TOUR RULES — ≤3 tour-boxes per stop; 2026-04-27.)*

208. **`.stop-name` is the actual venuenever a generic label.** Per `Brain/CORE RULES/Stops Structure.html` § Stop name = always the venue: *"Every stop's name is the actual place — the museumpalaceneighborhoodbasilicaviewpoint. Never a generic label like 'Three Guided Tours' or 'Morning Walk.' If the day visits three placesthat's three stopseach with the place's name."* The new check walks every `.stop-name`extracts plain text (HTML strippedwhitespace normalized)and matches against three families of generic patterns: (i) number-word + activity-noun (e.g. `Three Guided Tours``Two Stops``Four Tours`); (ii) time-of-day + activity-noun (e.g. `Morning Walk``Afternoon Stops``Evening Tour`); (iii) bare activity-noun without a venue qualifier (e.g. `Guided Tour``Walking Tour``Free Time``Leisure`). Patterns are anchored `^...$` to avoid false-positives on real venue names that happen to contain the tokens — `Three Sisters Café` doesn't match because `Sisters` isn't in the noun list; `Walking Tour Company` doesn't match because the anchor requires the full string to be just `Walking Tour`. Drift class: a future Claude summarizes a day's three guided tours as a single stop labelled `Three Guided Tours` instead of shipping three stops with the venues' actual names — a recurring drift flagged historically. Added 2026-04-27 per request *"green light 1"*. *(Enforced — validate_itinerary.py STOP NAME — venuenot generic label; 2026-04-27.)*

209. **Stop flags don't appear inside a 📅-led `.tour-box` (guided tour box).** Per `Brain/CORE RULES/Stops Structure.html` § Stop Flags: *"They appear inside the self-walk box or ticket boxon their own line."* The PDF distinguishes three box types in the validator surface: 📅-led `.tour-box` (guided tour boxwhere flags do NOT belong); 📍-led `.tour-box` (the "self-walk box" in PDF terminologywhere flags DO belong); `.ticket-box` (where flags also DO belong). Existing checks cover flag MEANING (Rule 174 ⚠️ no weatherRule 4430 💵 cash-only) and ORDER (Rule 5248). Gap closed here: flag LOCATION (wrong-box subset). The new check walks every `.tour-box`identifies the lead glyph (📅 = guided📍 = self-walk)and hard-FAILs if any stop-flag glyph (🚫/🆓/💵/⚠) appears inside a 📅-led tour-box. Drift class: a future Claude could add `🚫 Closed` inside a guided tour-box thinking the flag applies to the venue — but the venue's "closed" status is irrelevant when entry happens via the guided tour (the tour handles it). Flags in guided tour-boxes are noise. Added 2026-04-27 per request *"green light 1"*. *(Enforced — validate_itinerary.py STOP FLAGS — LOCATION (B1wrong box); 2026-04-27.)*

210. **Stop flags don't appear at stop-block level outside any box.** Per `Brain/CORE RULES/Stops Structure.html` § Stop Flags: *"They appear inside the self-walk box or ticket boxon their own line."* Companion to Rule 209 — closes the other half of the flag-LOCATION gap. The new check walks every `.stop-block`strips all `.tour-box` and `.ticket-box` content from the inner HTML (the stop-name is also stripped to avoid false-positives on the rare venue name that happens to contain a flag glyph)and hard-FAILs if any stop-flag glyph (🚫/🆓/💵/⚠) remains in the stripped content — meaning the flag is at stop-block level (not inside any box). Drift class: a future Claude adds a stop-level `🚫 Closed` row above the boxes (treating the flag as a stop-wide caveat) — but per PDF the flag must live INSIDE the relevant boxnot above. Pairs with Rule 209 — together the two cover the full flag-LOCATION rule (Rule 209 = wrong box; Rule 210 = no box at all). Added 2026-04-27 per request *"green light 1"*. *(Enforced — validate_itinerary.py STOP FLAGS — LOCATION (B2no box); 2026-04-27.)*

211. **🎟 ticket-box link text on Viator / GetYourGuide / TripAdvisor URLs ends with ` — {Platform}` (em-dash + display name).** Per `Brain/CORE RULES/Stops Structure.html` § 🎟 Ticket-box link text: *"Format: `🎟 {Product Title} — {Platform}`. The product title is what surfaces on the platform's listing page... Platform attribution after the em-dash."* Examples in the Doc: ✅ `🎟 Pena Palace & Park Skip-the-Ticket-Line Entrance — GetYourGuide` · ✅ `🎟 Castle of the Moors Skip-the-Line Entrance Ticket — Viator`. Pre-existing checks cover the bare-host ban (validate_itinerary.py line ~3450 — catches `<a>viator.com</a>` style drift) and the generic-descriptor scrub (Rule 4392 — catches "ticket"/"entrance ticket"/"entry ticket" suffixes). Gap closed here: a product-title link text that lacks the trailing em-dash + platform-name attribution (e.g. `🎟 Pena Palace Skip-the-Line Ticket` with no `— GetYourGuide`) silently shipped despite breaking the locked format. The new check finds every 🎟 row's `<a href>`determines the host (Viator/GetYourGuide/TripAdvisor with all the regional TA TLDs covered: `.com``.it``.co.uk``.fr``.de``.es`)and hard-FAILs if the visible link text (HTML stripped) doesn't end with the literal ` — {Display Name}` suffix using U+2014 em-dash + canonical display name (`Viator` / `GetYourGuide` / `TripAdvisor`). Wrong-dash drift (hyphen-minus `-` or en-dash `–` instead of em-dash) also fails. Venue-site fallback URLs (when the 🎟 URL is the venue's own domain) correctly skipped — those use the bare-domain link text per the Doc's separate fallback rule. Added 2026-04-27 per request *"lets add 2 too. I agree"* approving the Tier 2 platform-attribution check after the Tier 1 three-pack landed. *(Enforced — validate_itinerary.py 🎟️ TICKET-BOX LINK TEXT — platform attribution; 2026-04-27.)*

212. **No `.stop-photos` wrapper inside a `.ticket-box`.** Per `Brain/CORE RULES/Photos Rules.html` § Placement: *"The photo block sits directly under `.stop-block` — never inside a `.tour-box` or `.ticket-box`."* The pre-existing tour-box-half check (Rule 153validate_itinerary.py line ~1900) caught one direction of the placement rule; this closes the ticket-box half — exact mirroropposite container. Drift class: a future Claude bundles the photo inside the `.ticket-box` thinking it documents the booked entry — but per PDF the photo block belongs directly under `.stop-block`not nested inside any booking row. Walks every `.ticket-box`opens its balanced inner content via `_walk_balanced_div`and hard-FAILs on any `class="...stop-photos..."` match inside. Added 2026-04-27 per the Photos.html validator-coverage audit pass — Tier 1 four-pack (A·B·C·D). *(Enforced — validate_itinerary.py PHOTO PLACEMENT — A; 2026-04-27.)*

213. **Photo block (`.stop-photos` / `.stop-photos-empty`) is the LAST element inside `.stop-block`.** Per `Brain/CORE RULES/Photos Rules.html` § Placement: *"Photos (or the 'No pictures found' line) are always the LAST element inside `.stop-block`regardless of stop type."* Pre-existing checks cover presence (every `.stop-block` has a `.stop-photos`) and exclusivity (max 1 wrapper per stop) but not POSITION. A stop that ships its photo block in the middle (followed by prosea 📒 stop-summary rowa stop flagor any other content) silently passed every existing check despite breaking the locked placement. The new check walks every `.stop-block` (bounded by `_boundary_re` — sibling lookahead at `.stop-block` / `.next` / `.next-tram` / `.train` / `.day-header` / `.day-block` / `.extras-section` / `<hr>` / `</body>`)finds the LAST `.stop-photos` opening tagwalks its balanced close via `_walk_balanced_div`and verifies that everything after the close is whitespace or HTML comments — no other meaningful content. Hard-FAILs when content remains. Drift class: future Claude appends a 📒 summary rowan inclusion-bar comment as visible textor a stop flag below the photo block thinking it's a stop-level afterword — both break the canonical structural shape. Added 2026-04-27 per the Photos.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py PHOTO PLACEMENT — B; 2026-04-27.)*

214. **`<img src>` inside `.stop-photos` starts with the relative `_build/assets/` path.** Per `Brain/CORE RULES/Photos Rules.html` § Storage: *"Reference each photo as a relative path: `<img src="_build/assets/800px-Foo.jpg">`. Never link directly to `upload.wikimedia.org` or any other remote URL."* Pre-existing bans cover Wikimedia hotlinks (validate_itinerary.py line ~1131)base64 data URIs (line ~1102)and placeholder-photo services (Picsum/placehold/Unsplash seeds — line ~1086). Gap closed here: an `<img src>` that's an absolute path (`/Users/{User}/.../_build/assets/...`)a sibling-folder path (`../_build/assets/...`)or any non-banned remote CDN slipped through every existing check. The new check positively asserts the `_build/assets/` relative-path shape and hard-FAILs everything else (excluding `data:` and `http(s)://` to avoid double-reporting against the dedicated bans). Drift class: a build-pipeline tool emits absolute paths during local rendering and the absolute paths leak into the shipped HTML — links break for any reader on a different machine. Added 2026-04-27 per the Photos.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py PHOTOS — CONTENT SHAPE — C; 2026-04-27.)*

215. **`<img alt>` inside `.stop-photos` does not start with `photo of` / `image of` / `picture of`.** Per `Brain/CORE RULES/Photos Rules.html` § Alt text: *"Shortplain. Just the subject name. No 'photo of' prefix."* Pre-existing alt check enforces NON-EMPTINESS (validate_itinerary.py line ~1108) but not the prefix ban. A wrapper shipping `alt="Photo of {Venue}"` / `alt="Image of {Subject}"` / `alt="A picture of the {Stop}"` passed every existing alt check despite breaking the Doc rule. The new check uses an anchored regex `^\s*(?:a\s+|the\s+)?(?:photo|image|picture|pic|photograph)\s+of\b` — requires the noun PLUS literal "of" so a real venue name that legitimately contains the noun (e.g. "Photographic Museum") doesn't false-positive. Optional leading article ("a "/"the ") covers `A photo of …` and `The picture of …` variants. Hard-FAILs on any match. Drift class: future Claude defaults to descriptive alt prose because that's the convention on photo-sharing sites; Doc rule is plain subject name only. Added 2026-04-27 per the Photos.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py PHOTOS — CONTENT SHAPE — D; 2026-04-27.)*

216. **`<img src>` filename inside `.stop-photos` starts with the `800px-` prefix.** Per `Brain/CORE RULES/Photos Rules.html` § Harvest recipe step 6: *"Download to `Guides/{City}/_build/assets/` preserving the `800px-{Filename}` filename."* The 800px Commons thumbnail resolution is the locked canonical ship size — every photo harvested via `commons_photo.py` resolves to `https://upload.wikimedia.org/wikipedia/commons/thumb/{a}/{ab}/{File}/800px-{File}` and downloads with that filename preserved. The new check extracts the filename (basename after the last `/`) from every `_build/assets/`-prefixed src and hard-FAILs if it doesn't start with `800px-`. Drift class: future Claude grabs a 600px or 1200px or original-resolution thumb URL from a Commons file page (the file-page links offer multiple resolutions) and the non-canonical size leaks into the _build/assets/ folder; the photo still renders but the size discipline that keeps file weights consistent across guides drifts. Scope is restricted to `_build/assets/`-prefixed src (Rule 214 already catches non-`_build/assets/` paths). Added 2026-04-27 per approved Tier 2 trio (E·F·G) alongside the Tier 1 four-pack. *(Enforced — validate_itinerary.py PHOTOS — CONTENT SHAPE — E; 2026-04-27.)*

217. **`.stop-photos-empty` wrapper holds the literal `<em>No pictures found.</em>` (em-tag wrapexact period — TIGHTENED from substring-only).** Per `Brain/CORE RULES/Photos Rules.html` § Negative finding HTML format: *"`<div class="stop-photos stop-photos-empty"><em>No pictures found.</em></div>`."* Prior check (validate_itinerary.py line ~989pre-2026-04-27) was substring-only — required the text "No pictures found" to appear anywhere in the wrapper inner. That let bare-text wraps and other-tag wraps (`<p>No pictures found</p>``<i>No pictures found</i>`) silently pass despite breaking the locked PDF format. Tightened 2026-04-27 to require the explicit `<em>...</em>` wrap with the trailing period via regex `<em>\s*No\s+pictures\s+found\.?\s*</em>`. Period made tolerant (`.?`) since both `found.` and `found` are reasonable in practice; the em-tag wrap is the load-bearing assertion. The legacy substring sentinel still fires as a fallback `wrong_count` signal so an empty wrapper with NO recognizable negative-finding text at all (the original drift class) still surfaces via the existing exactly-1-img-or-negative-line check. Added 2026-04-27 per approved Tier 2 trio (E·F·G). *(Enforced — validate_itinerary.py PHOTOS — CONTENT SHAPE — F; 2026-04-27. Tightening of pre-existing 2026-04-26 substring check.)*

218. **Every `<img src="_build/assets/...">` resolves to a real file on disk.** Per `Brain/CORE RULES/Photos Rules.html` § Storage: *"Photos download to `Guides/{City}/_build/assets/` and ship from there. Never link directly to `upload.wikimedia.org` or any other remote URL — Commons rotates thumbnail URLsand a hotlinked photo silently breaks the next time the cache expires."* The HTML-shape checks (Rules 214 + 216) verify the `<img src>` LOOKS right (`_build/assets/800px-{Filename}.jpg` shape); this rule verifies the file actually EXISTS. Drift class: a future Claude pasted an `<img src="_build/assets/800px-Foo.jpg">` for a photo it never actually downloaded — passes every regex shape check but renders as a broken image when the guide is opened in a browser. The new check resolves each `_build/assets/`-prefixed src against the guide HTML's parent directory (via `Path(filename).parent / "_build"`) and hard-FAILs on any path that doesn't resolve to a `.is_file()`. Scope is conditional: skipped entirely when the guide HTML's parent has no `_build/assets/` subdirectory (synthetic test mode — no real assets to point atship-gate doesn't fire on validator unit tests). Added 2026-04-27 per approved Tier 2 trio (E·F·G). *(Enforced — validate_itinerary.py ASSETS ON DISK — G; 2026-04-27.)*

219. **`.glance-section` sits between `.title-page` and the first `.day-block`.** Per `Brain/CORE RULES/Trip at a Glance.html` § What this section is: *"Sits directly after the title pagebefore Day 1."* Pre-existing checks cover wrapper presence (validate_itinerary.py line ~3119)card structureand title format — but not POSITION. A guide shipping the glance section after the first day-blockor before the title pagesilently passed. The new check finds the document positions of the `.title-page` openerthe `.glance-section` openerand the first `.day-block` openerand hard-FAILs on either inversion. Drift class: future Claude relocates the glance section to the bottom of the guide as a "navigation footer" or buries it inside Day 1 — both break the locked top-of-guide placement that lets the reader scan the whole trip before opening any day. Added 2026-04-27 per the Trip at a Glance.html validator-coverage audit pass — Tier 1 four-pack (A·B·C·D). *(Enforced — validate_itinerary.py TRIP AT A GLANCE — VALIDATOR-COVERAGE BACKFILL — A; 2026-04-27.)*

220. **All non-🚆 Train Day cards come BEFORE all 🚆 Train Day cards in the glance section.** Per `Brain/CORE RULES/Trip at a Glance.html` § Day order — in-city days first🚆 Train Day patterns last. Walks every `.glance-day` card in document orderclassifies each by checking for the `🚆 Train Day` substring in the titleand hard-FAILs if any non-Train-Day card appears AFTER a Train-Day card. Only `🚆`-glyph cards trigger the tail-position rule. *(Note: prior wording referenced "guided coach day-trips render as 🚩 Guided" — Guided Tour Stop retired 2026-05-20; all non-Train-Day cards are 🎒 Self-Guided.)* Drift class: a future Claude alphabetizes cards by destinationsorts by some perceived "interestingness," or orders by tour duration — all break the locked top-to-bottom in-city-first / Train-Day-last flow. Added 2026-04-27 per the Trip at a Glance.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py TRIP AT A GLANCE — VALIDATOR-COVERAGE BACKFILL — B; 2026-04-27.)*

221. **`.glance-day` card count = `.day-block` count (1:1 symmetry).** Per `Brain/CORE RULES/Trip at a Glance.html` § What this section is: *"clickable cardsone per touring dayjumping straight to that day's block."* Implicit invariant — every touring day in the itinerary has exactly one card; every card has exactly one matching day-block. The pre-existing card-presence check (≥1 card) and the universal section-anchor check (`href="#x"` ↔ `id="x"`) catch some failure modes but not COUNT MISMATCH directly. A guide shipping 5 day-blocks but only 4 cards (a forgotten card) or 5 cards but 6 day-blocks (a stray day-block — possibly a copy-paste bug from a longer itinerary draft) passes every existing check despite breaking the 1:1 contract. The new check counts strict-match `<a class="glance-day">` opens vs `<div class="day-block">` opens and hard-FAILs on inequality. Drift class: rebuild adds a new day-block but forgets to extend the glance sectionor the reverse. Added 2026-04-27 per the Trip at a Glance.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py TRIP AT A GLANCE — VALIDATOR-COVERAGE BACKFILL — C; 2026-04-27.)*

222. **Glance card day numbers are sequential from Day 1 — no gapsno duplicatesno out-of-order.** Per `Brain/CORE RULES/Trip at a Glance.html` § Each card and § Day orderplus `Brain/CORE RULES/Guide Structure.html` § Day numbering: *"Day 1 = first full touring day; arrival/departure days don't count."* Pre-existing checks cover the `Day N · {icon} {label}` format on each card individuallybut not the SEQUENCE across cards. A guide shipping cards `Day 1 · Day 3 · Day 4 · Day 5` (missing Day 2 — a typo'd skip)or `Day 1 · Day 2 · Day 2 · Day 3` (a duplicate from a copy-paste bug)or `Day 1 · Day 3 · Day 2 · Day 4` (out-of-order from a manual reorder gone wrong) silently passes the per-card check. The new check extracts each card's day number via the title regex `^\s*Day\s+(\d+)\s+·`builds the ordered listand hard-FAILs unless it equals `[123...N]`. Cards whose title doesn't parse a day number are surfaced separately (they would also fail the existing strict-format check at line ~5811but the new check names them too for diagnostic clarity). Added 2026-04-27 per the Trip at a Glance.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py TRIP AT A GLANCE — VALIDATOR-COVERAGE BACKFILL — D; 2026-04-27.)*

223. **🚆 Train Day cards REQUIRE the ` — {City}` em-dash + non-empty city suffix.** Per `Brain/CORE RULES/Trip at a Glance.html` § Train Day variant — destination city as dash suffix: *"Train-day cards CARRY the destination city as an em-dash suffix on the card title — `Day N · 🚆 Train Day — {City}` (e.g.`Day 8 · 🚆 Train Day — Sintra`). The destination is visible at glance level so the owner knows which train day each card opens without clicking through."* Pre-existing strict-format regex (validate_itinerary.py line ~5799) accepts the optional ` — {City}` suffix on ANY label per a back-compat note ("the plain `🚆 Train Day` form (no suffix) also remains acceptable for back-compat with shipped guides"). This rule TIGHTENS that lenient back-compat to FAIL on bare `🚆 Train Day` cards in NEW builds. Older guides remain frozen per the CLAUDE.md behavioral rules — no retrofit. Drift class: future Claude defaults to the bare form because the back-compat line softened the rule mentallybut the at-glance pattern (knowing which train day each card opens without clicking) requires the city to be visible. The new check finds every `🚆 Train Day` card titlerequires the regex `🚆\s*Train Day\s+—\s+\S+` (em-dash U+2014non-empty content after)and hard-FAILs on any miss. Added 2026-04-27 per approved Tier 2 trio (E·F·G). *(Enforced — validate_itinerary.py TRIP AT A GLANCE — VALIDATOR-COVERAGE BACKFILL — E; 2026-04-27. Tightening of pre-existing back-compat leniency.)*

224. **Glance card href specifically matches `#day{N}` pattern (not arbitrary internal anchor).** Per `Brain/CORE RULES/Trip at a Glance.html` § Cross-links: *"Each card links via `href="#day{N}"` to a matching `id="day{N}"` on the day-block."* The pre-existing universal section-anchor check (validate_itinerary.py line ~5520) verifies that EVERY internal `href="#x"` has a matching `id="x"` somewhere in the document — but accepts any well-formed internal href on a glance card. A glance card linking to `href="#evora-coach-tour"` (where `id="evora-coach-tour"` happens to exist on a tour-box deeper in the day) passes the universal anchor check despite breaking the locked `#day{N}` shape. The new check tightens scope to glance cards only and requires the href to match `^#day\d+$` exactly. Drift class: future Claude wires a glance card to a tour-box id thinking it's a "deep link" — but per PDF the glance card MUST jump to the day-block openernot into a tour-box mid-day. Added 2026-04-27 per approved Tier 2 trio. *(Enforced — validate_itinerary.py TRIP AT A GLANCE — VALIDATOR-COVERAGE BACKFILL — F; 2026-04-27.)*

225. **Glance cards exclude arrival/departure/Day 0 references.** Per `Brain/CORE RULES/Trip at a Glance.html` § Arrival / departure days excluded: *"Only real touring days appear as cards. Arrival and departure are improvised on the ground."* Companion to Rule 5503 (`.day-header` arrival/departure exclusion at line ~5499). Belt-and-suspenders coverage — the existing check catches arrival/departure tokens on `.day-header` / `.day-title` but not on glance card titles directly. A guide whose glance section ships a `Day 0 · 🎒 Arrival` card while its day-blocks are all clean would pass the existing check despite breaking the rule. The new check applies the same arrival/departure regex (`\b(arrival|arriving|departure|departing|day\s*0|day\s*zero)\b`case-insensitive) to every `.glance-day-title` text. Hard-FAILs on any match. Drift class: future Claude adds a glance card for the arrival day "for completeness" thinking it's a navigation aiddespite the Doc rule that improvising-on-the-ground means no guide content. Added 2026-04-27 per approved Tier 2 trio. *(Enforced — validate_itinerary.py TRIP AT A GLANCE — VALIDATOR-COVERAGE BACKFILL — G; 2026-04-27.)*

226. **🍽️ Downtown Restaurants — every entry has 📍 address row + 🏛 hours row.** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` § What each entry looks like: *"`{Restaurant Name}` ← linked to restaurant's site / `📍 {Address}` [clickable Google Maps link] / `🏛 Opens {time} · closed {day}`"*. Direct mirror of the RNH Rule 195 entry-shape checkminus the 🚶 walk row (DR is district-scopednot hotel-scoped). Pre-existing presence check (validate_itinerary.py line ~2948) confirms the section exists; this rule walks every `.extras-sub` entry inside `#downtown` and hard-FAILs any entry missing 📍 or 🏛 rows in its body. Drift class: a future Claude ships a name + address only (no hours row) thinking the section is a quick-reference listbut used 🏛 to plan the day — without it she has to look up hours per restaurant. Added 2026-04-27 per the Downtown Restaurants.html validator-coverage audit pass — Tier 1 five-pack (A·B·C·D·E). *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — VALIDATOR-COVERAGE BACKFILL — A; 2026-04-27.)*

227. **🍽️ Downtown Restaurants — every top-3 heading is linked to the restaurant's site (not Mapsnot Wikipedia).** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` § What each entry looks like: *"`{Restaurant Name}` ← linked to restaurant's site"*. Direct mirror of RNH Rule 196. The 📍 row's link is the Google Maps anchor on the address; the heading link must be a different URL — the restaurant's own sitewhere the owner goes to verify hours / book / read the menu. Walks every `.extras-sub` heading inside `#downtown`extracts every `<a href>`hard-FAILs if no anchor URL is non-Maps and non-Wikipedia. Drift class: future Claude links the heading to the Maps URL or to a Wikipedia article instead of the restaurant's own site — both leave the owner without the booking surface. Added 2026-04-27 per the Downtown Restaurants.html validator-coverage audit pass — Tier 1 five-pack. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — VALIDATOR-COVERAGE BACKFILL — B; 2026-04-27.)* 🔄 SUPERSEDED 2026-05-17: **rule inverted.** The downtown top-3 heading must now link to a **REVIEW HOST** (Yelp / Google Maps / Google Reviews / Google Local Guide — any `yelp.com``google.com/maps``google.com/search?...localguide` form) — NOT the restaurant's own site. Own-site links → hard-FAIL. Reason: linking to the restaurant's own site gave the owner a marketing pagenot a ratings surface; a review-host link gives star count + recent reviews + the "enough context to book or skip" signal. Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` § 5 (updated 2026-05-17). `_DR_ALLOWED_REVIEW_HOSTS` in validate_itinerary.py line ~6465.

228. **🍽️ Downtown Restaurants × ⭐ Michelin Restaurants — no restaurant duplicated across sections.** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` § How to build the list: *"Skip Michelin-starred restaurants (they're already in their own section)."* Direct mirror of RNH Rule 197 — same set-intersection logicapplied to `#downtown` × `#michelin`. The two sections serve different purposes — Downtown is for "best of the historic core by local consensus"; Michelin is for destination dining at the starred tier. Shipping the same restaurant in both is double-counting and breaks the section split. Detection: extract every Michelin restaurant name (plain text from `#michelin .extras-sub`leading ⭐ stars + 🐟 markers stripped)extract every DR entry name (plain heading text from `#downtown .extras-sub`)case-insensitive set intersection. Any overlap = drift. Added 2026-04-27 per the Downtown Restaurants.html validator-coverage audit pass — Tier 1 five-pack. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — VALIDATOR-COVERAGE BACKFILL — C; 2026-04-27.)*

229. **🍽️ Downtown Restaurants — at most 3 entries (top-3 cap).** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` § What each entry looks like: *"Top 3 ordered by review consensus / editorial standout (best first)."* Pre-existing checks cover section presence and entry shape but not COUNT. A guide shipping 4–5 entries in DR (because the city has many strong contenders) silently passed despite breaking the locked top-3 cap. Drift class: future Claude ships 5 entries thinking "more = better," but the at-glance pattern depends on the cap — three is the working memory limit she'll actually scan when picking dinner. Lower bound is intentionally NOT enforced here: the Doc allows fewer than 3 (when fewer qualify) AND zero entries with the negative-finding fallback (Rule 230)both legitimate. Hard-FAILs only when count > 3. Added 2026-04-27 per the Downtown Restaurants.html validator-coverage audit pass — Tier 1 five-pack. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — VALIDATOR-COVERAGE BACKFILL — D; 2026-04-27.)* **🔄 SUPERSEDED 2026-05-13:** cap raised to top-4 per `Downtown Restaurants - Extra Section.html` § 4. Validator updated (line ~8385) — message reads "at most 4 entries." Rule retained here for history.

230. **🍽️ Downtown Restaurants — when zero entries shipthe section carries the literal `"No distinct historic downtown in {City}."` negative-finding line.** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` § When no historic downtown exists: *"Some destinations don't have a clear historic-downtown core (modern resort citiesscattered metro areas). Ship a single negative-finding line in the section: `'No distinct historic downtown in {City}.'` The line confirms we looked and found nothing. Without ityou can't tell if we checked or forgot."* Pre-existing extras-empty check (validate_itinerary.py line ~2977) catches sections that are present-but-silently-empty (no `extras-sub` and no `extras-empty` line) but doesn't validate the literal phrase inside the empty wrapper. The new check fires when the DR section ships zero `.extras-sub` entriesstrips HTML to plain textand hard-FAILs unless the phrase `No distinct historic downtown in` appears (case-insensitiveCity placeholder unrestricted). Drift class: future Claude rewords the negative finding to "No downtown core in {City}" or "No historic centre identified" or leaves the empty wrapper bare — all break the locked phrase that makes the "we lookedwe found nothing" signal scannable across guides. Added 2026-04-27 per the Downtown Restaurants.html validator-coverage audit pass — Tier 1 five-pack. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — VALIDATOR-COVERAGE BACKFILL — E; 2026-04-27.)*

231. **🍽️ Downtown Restaurants — no 🚶 walk row inside entries (district-scopednot hotel-scoped).** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` § What each entry looks like: *"No walk-time row — the section is district-scopednot hotel-scoped."* The DR entry shape is intentionally a SUBSET of the RNH entry shape — same name + 📍 + 🏛 rowsMINUS the 🚶 walk row. Drift class: a future Claude copies the RNH shape (which has 🚶) into a DR entryleaving the walk-time row in. The walk time inside DR is meaningless because DR is scoped to the historic downtown district (which may be far from the hotel — the entire point of the section is "worth crossing the city for"). Catches the cross-PDF copy-paste failure mode. Walks every `.extras-sub` body inside `#downtown` and hard-FAILs on any 🚶 glyph. Added 2026-04-27 per approved Tier 2 single (F) alongside the Tier 1 five-pack. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — VALIDATOR-COVERAGE BACKFILL — F; 2026-04-27.)*

232. **🎭 Shows — every entry has a 📍 address row.** Per `Brain/CORE RULES/Shows, Performances & Concerts - Extra Section.html` § What each entry looks like: *"`📍 Address [clickable Google Maps link]`"*. Pre-existing checks (validate_itinerary.py lines ~2826~2885~2897) verify aggregator-link denylist`.shows-box` wrapper presenceand the 🎼 genre opener — but don't verify 📍 presence. A guide shipping heading + prose + 🎟 booking row but no address line silently passes. The new check walks every `.extras-sub` entry inside `#shows`captures the entry HTML between this heading and the next `.extras-sub` (or end of section)and hard-FAILs on any entry missing 📍. Drift class: a future Claude trims the address row thinking the venue name + 🎟 link are enoughbut used 📍 to map walking routes from the hotel before the show. Added 2026-04-27 per the Shows v5 validator-coverage audit pass — Tier 1 four-pack (A·B·C·D). *(Enforced — validate_itinerary.py SHOWS — VALIDATOR-COVERAGE BACKFILL — A; 2026-04-27.)*

233. **🎭 Shows — every entry has a 🎟 booking row with an `<a href>` link.** Per `Brain/CORE RULES/Shows, Performances & Concerts - Extra Section.html` § What each entry looks like: *"`🎟 show-or-venue-site.com`"* + § Where the 🎟 link goes: *"The 🎟 row links to the show or venue's own website."* Pre-existing checks don't verify the 🎟 row exists or carries an anchor — only the aggregator denylist (which scans `.ride-apps`the prose divpossibly the wrong container per the structural-comment hierarchy). The new check finds the 🎟 glyph in each entrylooks for the next `<a href="...">` within 400 chars (typical row content)and hard-FAILs on (a) no 🎟 glyph at allOR (b) 🎟 with no nearby anchor. Drift class: bare URL string with no `<a>` wrap (`🎟 example.com` rendered as text — no click target)OR a stripped 🎟 row leaving prose + address only. Added 2026-04-27 per the Shows v5 validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py SHOWS — VALIDATOR-COVERAGE BACKFILL — B; 2026-04-27.)*

234. **🎭 Shows — every 🎟 link points to the venue's own sitenot a ticketing aggregator.** Per `Brain/CORE RULES/Shows, Performances & Concerts - Extra Section.html` § Where the 🎟 link goes: *"The 🎟 row links to the show or venue's own website. The venue's homepage routes to whichever portal it uses and shows the seat map + schedule from the source."* Tightens the pre-existing aggregator denylist (validate_itinerary.py line ~2826)which scans `<a href>` inside `.ride-apps` (the prose div) — but per the structure-comment hierarchy the 🎟 booking link lives in `.shows-box`not `.ride-apps`. The pre-existing check might miss 🎟-row aggregator drift. The new check scopes directly to the 🎟 row anchor regardless of which container holds it. Reuses the same `shows_aggregator_hosts` tuple (VivaticketTicketmaster across regional TLDsTodayTixSeeTicketsFNAC SpectaclesEventimTicketOneTicketCorner). Added 2026-04-27 per the Shows v5 validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py SHOWS — VALIDATOR-COVERAGE BACKFILL — C; 2026-04-27.)*

235. **🎭 Shows — when zero entries shipthe section carries the literal `"No exceptional showsperformancesor concerts in {City}."` negative-finding line.** Per `Brain/CORE RULES/Shows, Performances & Concerts - Extra Section.html` § When no exceptional showsperformancesor concerts exist: *"Ship a single negative-finding line in the section: `'No exceptional showsperformancesor concerts in {City}.'` The line confirms we looked and found nothing. Without ityou can't tell if we checked or forgot."* Pre-existing extras-empty check (line ~2977) catches sections that are present-but-silently-empty (no `extras-sub` and no `extras-empty` line) but doesn't validate the literal phrase inside the empty wrapper. The new check fires when the Shows section ships zero `.extras-sub` entriesstrips HTML to plain textand hard-FAILs unless the phrase `No exceptional showsperformancesor concerts in` appears (case-insensitive; the comma-or-no-comma and `or` allowance handle minor punctuation driftbut the four content words are load-bearing). Mirror of the same negative-finding pattern landed on Local Tastes / Michelin / Downtown Restaurants. Drift class: a future Claude rewords to "No major shows in {City}" / "No notable performances" / "Shows section not applicable here" — all break the locked scannable signal. Added 2026-04-27 per the Shows v5 validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py SHOWS — VALIDATOR-COVERAGE BACKFILL — D; 2026-04-27.)*

236. **🎭 Shows — 📍 row anchor URL is `google.com/maps`.** Per `Brain/CORE RULES/Shows, Performances & Concerts - Extra Section.html` + `Brain/CORE RULES/Links.html` § 📍: *"📍 → Google Maps anchor on the address itself."* Universal Maps-linking on 📍 is enforced for stop-blocks (Rule 187) but Shows entries live in `.extras-section` which has separate scoping. A Shows entry shipping `📍 <a href="https://venue.com/contact">Address</a>` (a link to the venue's contact page that happens to wrap the address) would pass the universal anchor check but break the locked Maps-anchor rule. The new check walks every 📍 in shows entrieslooks for the next `<a href="...">` within 400 charsand hard-FAILs unless the URL is `google.com/maps``maps.google.com`or `goo.gl/maps`. Drift class: future Claude links the address to the venue's location page or to a directions service that's not Maps. Added 2026-04-27 per approved Tier 2 single (E) alongside the Tier 1 four-pack. *(Enforced — validate_itinerary.py SHOWS — VALIDATOR-COVERAGE BACKFILL — E; 2026-04-27.)*

237. **⭐ Michelin Restaurants — every entry has a `🍽` cuisine row.** Per `Brain/CORE RULES/Michelin Restaurants - Extra Section.html` § What each entry looks like: *"`⭐⭐ Restaurant Name / 🍽 Cuisine type / 📍 Address [clickable Google Maps link]`"*. Pre-existing format check (validate_itinerary.py line ~2502) verifies stars + plain-text name in `.extras-sub``.entry-body` presenceand 📍 row + Maps-linking — but doesn't verify the 🍽 cuisine row inside `.entry-body`. A guide shipping stars + name + 📍 but no cuisine row silently passed. The new check walks every `.entry-body` body and hard-FAILs on any box missing 🍽. Drift class: future Claude trims the cuisine row thinking the restaurant name + stars + address is enough infobut used 🍽 to filter by cuisine type when picking dinner. Added 2026-04-27 per the Michelin Restaurants.html validator-coverage audit pass — Tier 1 three-pack (A·B·C). *(Enforced — validate_itinerary.py MICHELIN — VALIDATOR-COVERAGE BACKFILL — A; 2026-04-27.)*

238. **⭐ Michelin Restaurants — entries ordered by star tier descending (⭐⭐⭐ firstthen ⭐⭐then ⭐).** Per `Brain/CORE RULES/Michelin Restaurants - Extra Section.html` § Order: *"By star tierdescending — ⭐⭐⭐ firstthen ⭐⭐then ⭐. Alphabetical within each tier."* Pre-existing checks verify each entry's individual format but not the cross-entry SEQUENCE. A guide shipping `⭐ A` → `⭐⭐⭐ B` → `⭐⭐ C` (tier-interleaved) or `⭐ → ⭐⭐ → ⭐⭐⭐` (ascending) silently passed despite breaking the locked top-down-by-tier flow. The new check extracts the leading ⭐ count from each `.extras-sub` body in document orderbuilds the sequenceand hard-FAILs if it's not non-increasing. Drift class: a future Claude alphabetizes across all tiers (mixing 1⭐/2⭐/3⭐ by name) or sorts ascending by tier — both break the at-glance tier-grouping that lets scan the section by ambition level. Added 2026-04-27 per the Michelin Restaurants.html validator-coverage audit pass — Tier 1 three-pack. *(Enforced — validate_itinerary.py MICHELIN — VALIDATOR-COVERAGE BACKFILL — B; 2026-04-27.)*

239. **⭐ Michelin Restaurants — when zero entries shipthe section carries the literal `"No Michelin-starred restaurants in {City}."` negative-finding line.** Per `Brain/CORE RULES/Michelin Restaurants - Extra Section.html` § When no Michelin-starred restaurants exist: *"Ship a single negative-finding line in the section: `'No Michelin-starred restaurants in {City}.'` Don't omit the section entirely. The line confirms we looked and found nothing. Without ityou can't tell if we checked or forgot."* Pre-existing extras-empty check (line ~2977) catches sections that are present-but-silently-empty (no `extras-sub` and no `extras-empty` line) but doesn't validate the literal phrase. The new check fires when the Michelin section ships zero `.extras-sub` entriesstrips HTML to plain textand hard-FAILs unless the phrase `No Michelin-starred restaurants in` appears (case-insensitive; the regex tolerates the optional hyphen via `Michelin-?starred` to handle both spelled-with-hyphen and spelled-without-hyphen drift). Mirror of the negative-finding pattern landed on Local Tastes / DR / Shows. Drift class: future Claude rewords to "No starred restaurants in {City}" / "{City} has no Michelin entries" / "Section not applicable" — all break the locked scannable signal. Added 2026-04-27 per the Michelin Restaurants.html validator-coverage audit pass — Tier 1 three-pack. *(Enforced — validate_itinerary.py MICHELIN — VALIDATOR-COVERAGE BACKFILL — C; 2026-04-27.)*

240. **⭐ Michelin Restaurants — alphabetical within each star tier.** Per `Brain/CORE RULES/Michelin Restaurants - Extra Section.html` § Order: *"By star tierdescending ... Alphabetical within each tier."* Tightens Rule 238 (tier-descending) by enforcing within-tier alpha order. Pre-existing checks don't enforce alpha order. A guide shipping a 2⭐ tier as `⭐⭐ Z-Restaurant` → `⭐⭐ A-Restaurant` → `⭐⭐ M-Restaurant` (random) silently passed. The new check groups entries by star count (preserving document order so the tier-descending check still runs cleanly)strips the optional ` 🐟 seafood-heavy` suffix from each name for sort comparisonand hard-FAILs any tier whose name list isn't case-insensitively sorted. Drift class: a future Claude orders restaurants by their physical locationby some perceived prestige within the tieror by review score — all break the alphabetical sort that makes the section repeatable across guides. Added 2026-04-27 per approved Tier 2 three-pack (D·E·F) alongside the Tier 1 three-pack. *(Enforced — validate_itinerary.py MICHELIN — VALIDATOR-COVERAGE BACKFILL — D; 2026-04-27.)*

241. **⭐ Michelin Restaurants — every entry has exactly 12or 3 ⭐ stars.** Per `Brain/CORE RULES/Michelin Restaurants - Extra Section.html` § What ships: *"Stars only — 1⭐2⭐3⭐. Bib Gourmand and Michelin Plate are not on this list."* Pre-existing check (validate_itinerary.py line ~2543) requires `.extras-sub` to LEAD with ⭐ but doesn't validate the count. A guide shipping a typo'd `⭐⭐⭐⭐ Restaurant Name` (4 stars — impossible per the Michelin rating) or `⭐⭐⭐⭐⭐ Restaurant Name` silently passed despite breaking the canonical Michelin tier system. The new check counts the leading ⭐ run and hard-FAILs unless the count is exactly 12or 3. Bib Gourmand entries (no ⭐ at all) are already caught by the existing "leads with ⭐" checkso a 0-star case can only arise from non-leading-⭐ entries (which fail elsewhere). Drift class: typo on the high end — easy to miss on a copy-paste of an existing 3⭐ entry where the star count gets duplicated. Added 2026-04-27 per approved Tier 2 three-pack. *(Enforced — validate_itinerary.py MICHELIN — VALIDATOR-COVERAGE BACKFILL — E; 2026-04-27.)*

242. **⭐ Michelin Restaurants — heading row allows only ⭐ stars + plain text + optional `🐟 seafood-heavy` flag (no other emoji markers).** Per `Brain/CORE RULES/Michelin Restaurants - Extra Section.html` § Seafood flag (when in doubtmark): *"This is the only flag on the Michelin list — no other tastecuisineor price-band markers."* Companion to Rule 161 v3 (which re-instated 🐟 as the canonical seafood-heavy flag2026-04-27). Pre-existing checks verify the heading leads with ⭐ and the name is plain text but don't ban additional emoji markers in the heading row. A guide shipping `⭐⭐ Restaurant 🌶️ spicy` (a future Claude adding heat-level)`⭐⭐ Restaurant 💰 expensive` (price-band marker)or `⭐⭐ Restaurant 🥬 vegan-friendly` (cuisine marker) would silently pass despite breaking the "only flag is 🐟" rule. The new check strips the allowed glyphs from the heading (🐟 explicitly; ⭐ at U+2B50 is naturally outside the scanned emoji ranges)strips HTMLand hard-FAILs on any remaining emoji from the major pictograph + symbol Unicode ranges (U+1F300-1F6FFU+1F900-1F9FFU+2700-27BF). Drift class: future Claude adds new flags because they "seem helpful" — but the scan signal at the heading row is locked: ⭐ tier + name + optional 🐟 only. Added 2026-04-27 per approved Tier 2 three-pack. *(Enforced — validate_itinerary.py MICHELIN — VALIDATOR-COVERAGE BACKFILL — F; 2026-04-27.)*

243. **🌤 Weather — no Weather section in the shipped guide (no `🌤` glyphno "Weather Forecast" heading anywhere).** Per `On Demand/Weather - On Demand.html` § What this section is: *"Weather is an on-demand research output — checked live closer to the tripnever baked into the shipped guide. Forecasts shift too much for the guide to carry static weather notes."* Plus `CLAUDE.md` § On-demand documents (Weather is one of the four on-demand-only docs alongside ✈️ Flights / 🏨 Hotels research / 🏠 Rentals / 🚗 Car rentals — none ship with the guide). Pre-existing Rule 174 catches weather/climate tokens inside ⚠️ warning rowsbut a future Claude adding a full `🌤 Weather` section to End-of-Itinerary extras (thinking it's helpful — "let's bake in a 7-day forecast") would slip through every existing checksince `required_extras` (line ~2944) checks for ABSENCE of required sections but nothing checks for PRESENCE of unauthorized sections. The new check strips HTML to plain text and hard-FAILs on either (a) the `🌤` glyph (not on any active glyph table — Weather has no in-guide glyph because it has no in-guide section)or (b) the literal phrase `Weather Forecast`. Drift class: a future Claude pastes a weather-research output into the guide as a courtesybaking forecast info into a static guide that ships ahead of the trip when forecasts are still meaningless. Added 2026-04-27 per the Weather.html validator-coverage audit pass — Tier 1 single. *(Enforced — validate_itinerary.py WEATHER — VALIDATOR-COVERAGE BACKFILL — A; 2026-04-27.)*

244. **🌤 Weather — no weather-source URLs (`weather.com` / `accuweather.com` / `ventusky.com` / `windy.com`) in the shipped guide.** Per `On Demand/Weather - On Demand.html` § Sources: lists Weather.com (big picture · 10-14 day forecast · best for rain probability)AccuWeather (precise hourly timing)Ventusky (cloud cover and visibility)Windy.com (different model · good backup) as the four trusted on-demand research sources. These belong in research notes (separate document — Weather is on-demand only) and never in the shipped guide. Pre-existing checks don't ban weather-source URLs. Drift class: a future Claude pastes a weather-research summary into the guide and links to Ventusky directly (`Check Ventusky.com on the rooftop day`)or wraps a `🌤` heading around a Weather.com block — both would render as research-output-in-the-shipped-guidewhich is the exact failure mode Weather.html names. The new check scans every `<a href>` URL in the rendered HTML and hard-FAILs on any of the four hosts. Added 2026-04-27 per approved Tier 2 single (B) alongside the Tier 1 single (A). *(Enforced — validate_itinerary.py WEATHER — VALIDATOR-COVERAGE BACKFILL — B; 2026-04-27.)*

245. **🍮 Local Tastes — entry names avoid country-level clichés and generic-everywhere dishes.** Per `Brain/CORE RULES/Local Tastes - Extra Section.html` § What ships → The bar: *"Tied to a specific place's identity"* (PDF examples in the source PDF reference specific neighborhood / city / patisserie pairings). This rule IMPLEMENTS the cliché-name scrub that Rule 178 (added 2026-04-26) documented but whose validator implementation never landed — closes the documentation-vs-enforcement mismatch (P4 finding from the Local Tastes audit pass) in-line. Two layers: (a) substring match (case-insensitive) on country-cliché compounds — `pasta in italy``croissant in france``sushi in japan``tapas in spain``steak frites``basic sandwiches``plain pizza`; (b) exact-match (case-insensitive) on plain country-cuisine words — `PASTA``PIZZA``SUSHI``TAPAS``RAMEN``CROISSANT` alone. Compound names like `PIZZA NAPOLETANA``PASTA CARBONARA``EDOMAE SUSHI` legitimately pass since they don't match the exact comparison and don't contain a country-cliché compound. Drift class: a future Claude adds `PASTA` or `CROISSANT IN FRANCE` thinking the destination has a strong food culturebut the rule requires entries to be tied to a specific place's identity (`{Neighborhood}` / `{City}` / `{Patisserie}` — see PDF for canonical examples) — country-level clichés are noise. Added 2026-04-27 per the Local Tastes.html validator-coverage audit pass — Tier 1 five-pack (A·B·C·D·E). Pairs with Rule 178 — together the two surfaces document and enforce the same rule; this rule is the canonical mechanical home. *(Enforced — validate_itinerary.py LOCAL TASTES — VALIDATOR-COVERAGE BACKFILL — A; 2026-04-27. Resolves P4 doc-vs-enforcement mismatch on Rule 178.)*

246. **🍮 Local Tastes — entry NAME header is uppercase (source-text check).** Per `Brain/CORE RULES/Local Tastes - Extra Section.html` § What each entry looks like: *"The dish or drink name is the header (uppercase)."* Style A (locked 2026-05-19): the NAME lives in an `.extras-sub` heading div. Title Case is now required — ALL CAPS is the violation. Mirror of Rule 198 (`.title-city` uppercase source-text check). The check extracts the `.extras-sub` heading text (HTML stripped) and hard-FAILs if all letter characters are uppercase (i.e. `text == text.upper()` for the letter subset). Drift class: a future Claude ships `BICERIN` instead of `Bicerin`relying on old CSS muscle memory. `.local-tastes-title` is a banned retired class; the check now targets `.extras-sub`. Added 2026-04-27 per the Local Tastes.html validator-coverage audit pass — Tier 1 five-pack. *(Enforced — validate_itinerary.py LOCAL TASTES — VALIDATOR-COVERAGE BACKFILL — B; 2026-04-27.)*

247. **🍮 Local Tastes — every entry has exactly two bullets in order: `What it is` + `Where`.** Per `Brain/CORE RULES/Local Tastes - Extra Section.html` § What each entry looks like: *"NAME / • What it is — what the dish or drink isits originand what makes it distinctive. Multi-sentence prose. / • Where — specific establishment(s) with address(es) and short parenthetical notes about what each is known for."* Pre-existing presence check verifies the section exists and has either `.local-tastes` wrappers or `.extras-empty`but nothing validates each entry's bullet structure. The new check finds each entry's `<ul class="local-tastes-list">`counts `<li>` children (must be exactly 2)and verifies the first `<li>`'s `<strong>` label starts with "What it is" and the second's starts with "Where" (case-insensitive prefix match — allows trailing prose / em-dash / etc.). Hard-FAILs on any deviation: only one bulletthree+ bulletsmissing `<strong>` labelsswapped order ("Where" before "What it is")wrong labels ("Description" / "Address" / etc.). Drift class: a future Claude adds a third bullet for "Why it matters" or "Pricing," or trims to a single all-in-one bullet thinking it's cleaner — both break the locked two-bullet shape that the at-glance scan depends on. Added 2026-04-27 per the Local Tastes.html validator-coverage audit pass — Tier 1 five-pack. *(Enforced — validate_itinerary.py LOCAL TASTES — VALIDATOR-COVERAGE BACKFILL — C; 2026-04-27.)* **🔄 SUPERSEDED 2026-05-18:** the two-bullet `.local-tastes-list` format is now BANNED (K2 check). Current format: flat `<div>` description row (≤240 chars) + `📍` row with Google Maps anchor(s). No `<ul>`no `<li>`no "What it is" / "Where" labels — their presence is a hard fail.

248. **🍮 Local Tastes — every "Where" bullet contains at least one Google Maps anchor.** Per `Brain/CORE RULES/Local Tastes - Extra Section.html` § What each entry looks like: *"(Addresses inside the 'Where' bullet are clickable Google Maps linkslike everywhere else in the guide.)"* Pre-existing universal section-anchor check (line ~5520) verifies broken-href anchors but doesn't enforce the Maps-anchor rule on Local Tastes specifically. A "Where" bullet shipping bare-text addressesor wrapping the address in a link to the establishment's website instead of Mapssilently passed. The new check finds the second `<li>` (the Where bullet) inside each entry's listscans every `<a href>` insideand hard-FAILs if NONE of them are a Google Maps URL (`google.com/maps``maps.google.com`or `goo.gl/maps`). At least one Maps anchor is the load-bearing assertion — the establishment name might legitimately link to the venue's own website alongside the Maps-linked address. Drift class: future Claude links the address to the venue's contact page or to a Wikipedia article about the dishbreaking the owner's "tap address → Maps" workflow. Added 2026-04-27 per the Local Tastes.html validator-coverage audit pass — Tier 1 five-pack. *(Enforced — validate_itinerary.py LOCAL TASTES — VALIDATOR-COVERAGE BACKFILL — D; 2026-04-27.)*

249. **🍮 Local Tastes — when zero entries shipthe section carries the literal `"No recommended foods to taste in {City}."` negative-finding line.** Per `Brain/CORE RULES/Local Tastes - Extra Section.html` § When nothing distinctive exists: *"Ship a single negative-finding line in the section: `'No recommended foods to taste in {City}.'` The line confirms we looked and found nothing. Without ityou can't tell if we checked or forgot."* Pre-existing extras-empty check (line ~2977) catches sections that are present-but-silently-empty (no entries and no `.extras-empty` line) but doesn't validate the literal phrase inside the empty wrapper. The new check fires when the Local Tastes section ships zero `.local-tastes` entriesstrips HTML to plain textand hard-FAILs unless the phrase `No recommended foods to taste in` appears (case-insensitive). Mirror of the same negative-finding pattern landed on Michelin / DR / Shows / Weekly Closures. Drift class: a future Claude rewords to "No notable local foods in {City}" / "Nothing distinctive here" / "Skipped — no candidates" — all break the locked scannable signal. Added 2026-04-27 per the Local Tastes.html validator-coverage audit pass — Tier 1 five-pack. *(Enforced — validate_itinerary.py LOCAL TASTES — VALIDATOR-COVERAGE BACKFILL — E; 2026-04-27.)*

250. **🍮 Local Tastes — entry NAME is plain text (no `<a>` link wrapping the header).** Per `Brain/CORE RULES/Local Tastes - Extra Section.html` § What each entry looks like — the NAME is the card headerplain textnot a hyperlink. Mirror of the Michelin Restaurants plain-text-name rule (validate_itinerary.py line ~2547). Pre-existing checks don't enforce this — a guide wrapping the NAME in `<a href="https://en.wikipedia.org/wiki/Bicerin">BICERIN</a>` would silently pass despite breaking the locked card-header shape. The description div or 📍 row is the right place for any reference link (Wikipediaeditorial source) — the NAME header is just the dish name. Style A (locked 2026-05-19): the NAME lives in `.extras-sub`; `.local-tastes-title` is a banned retired class. The check searches the `.extras-sub` heading HTML for any `<a>` tag and hard-FAILs on a hit. Drift class: a future Claude links the NAME to a reference article (Wikipedia / Wikipedia-like editorial source) thinking "the reader might want to learn more" — but used NAME as a scan anchornot a navigation surface; the link in the 📍 row (description / address row) is the right place for any reference anchor. Added 2026-04-27 per approved Tier 2 single (F) alongside the Tier 1 five-pack. *(Enforced — validate_itinerary.py LOCAL TASTES — VALIDATOR-COVERAGE BACKFILL — F; 2026-04-27.)*

251. **🗓️ Weekly Closures — no national holidays / calendar dates / month+day tokens inside the section.** Per `Brain/CORE RULES/Weekly Closures - Extra Section.html` § What this section is: *"City-wide weekly patterns of when things are typically closed by category — so days can be planned around them. Not a calendar; not per-venue. Just the general weekly rule for the destination."* This rule IMPLEMENTS the date-token scrub that Rule 175 (added 2026-04-26) documented but whose validator implementation never landed — closes the documentation-vs-enforcement mismatch (P5 finding from the Weekly Closures audit pass) in-line. Same pattern as P4/Rule 178/Local Tastes resolved in the prior slice. Three layers of date detection: (a) ISO/numeric date forms — `2026-04-25``2026/04/25``25-04-2026``25/04/2026`; (b) month-name + day in either order — `January 1``Jan 1``1 January``25 December``December 25th`etc. (with optional ordinal suffix); (c) named holidays — `Christmas``Easter``Bastille Day``New Year``Thanksgiving``Diwali``Hanukkah``Ramadan``Eid``Lunar New Year``All Saints``Good Friday``Pentecost``Epiphany``Assumption``Corpus Christi`. Strips HTML to plain text first so attribute values / anchor URLs don't false-positive. Drift class: the owner 2026-04-26 caught a build that listed Christmas Day under Weekly Closures when she wasn't traveling in December — *"i am not going to be there during xmasso a drift."* If a national holiday genuinely falls inside the trip window AND closes city venuessurface it on the affected day's transit banner / stop flag insteadnever on the citywide-weekly-pattern surface. Added 2026-04-27 per the Weekly Closures.html validator-coverage audit pass — Tier 1 four-pack (A·B·C·D). Pairs with Rule 175 — together the two surfaces document and enforce the same rule; this rule is the canonical mechanical home. *(Enforced — validate_itinerary.py WEEKLY CLOSURES — VALIDATOR-COVERAGE BACKFILL — A; 2026-04-27. Resolves P5 doc-vs-enforcement mismatch on Rule 175.)*

252. **🗓️ Weekly Closures — every entry matches the locked `{Category} · Closed {Day}` format.** Per `Brain/CORE RULES/Weekly Closures - Extra Section.html` § What each entry looks like: *"`{Category} · Closed {Day}`"* with examples *"`Museums & Galleries · Closed Monday`"**"`Palaces · Closed Tuesday`"**"`Boutiques & Stores · Closed Sunday`"*. Pre-existing presence check verifies the section exists and has either `.stop-row` lines or `.extras-empty`but nothing validates each entry's shape. The new check walks every `.stop-row` inside `#weekly-closures` and hard-FAILs unless the entry: (a) contains a `<strong>` wrap on the category(b) has the literal sequence ` · Closed ` (middle dot U+00B7 + capital "Closed" + space) separating category from dayand (c) ends with a single day-name token. Drift class: a future Claude rewords to `"Museums close on Tuesdays"` (verb-tensed prose)`"Tuesday: museums closed"` (calendar-style)or `"Closed: museums (Tuesdays)"` (parens-style) — all break the at-glance pattern used to plan day order around closures. Added 2026-04-27 per the Weekly Closures.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py WEEKLY CLOSURES — VALIDATOR-COVERAGE BACKFILL — B; 2026-04-27.)*

253. **🗓️ Weekly Closures — bold ONLY the category name (the part before the dash).** Per `Brain/CORE RULES/Weekly Closures - Extra Section.html` § What each entry looks like: *"Bold only the category name — the part before the dash. Don't bold the day or anything else on the line."* Pre-existing checks don't enforce bold scoping. A future Claude bolding the day too (`<strong>Museums & Galleries</strong> · Closed <strong>Tuesday</strong>`) or the entire line (`<strong>Museums & Galleries · Closed Tuesday</strong>`) silently passed. The new check walks every `.stop-row` inside `#weekly-closures`counts `<strong>` tags (must be exactly 1)and verifies the strong-wrapped text equals the category portion (the prefix before ` · Closed `). Hard-FAILs on >1 strong tag OR mismatched strong content. Drift class: future Claude bolds for emphasis without realizing the bold-scope rule is load-bearing — the unbold day name visually anchors the pattern. Added 2026-04-27 per the Weekly Closures.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py WEEKLY CLOSURES — VALIDATOR-COVERAGE BACKFILL — C; 2026-04-27.)*

254. **🗓️ Weekly Closures — when zero entries shipthe section carries the literal `"No notable city-wide weekly closures in {City}."` negative-finding line.** Per `Brain/CORE RULES/Weekly Closures - Extra Section.html` § When the destination has no notable pattern: *"Ship a single negative-finding line in the section: `'No notable city-wide weekly closures in {City}.'` The line confirms we looked and found nothing. Without ityou can't tell if we checked or forgot."* Pre-existing extras-empty check (line ~3756) catches sections that are present-but-silently-empty (no `.stop-row` and no `.extras-empty` line) but doesn't validate the literal phrase. The new check fires when the Weekly Closures section ships zero `.stop-row` entriesstrips HTML to plain textand hard-FAILs unless the phrase `No notable city-wide weekly closures in` appears (case-insensitive; the regex tolerates the optional hyphen via `city-?wide` to handle both spelled-with-hyphen and spelled-without-hyphen drift). Mirror of negative-finding pattern landed across Local Tastes / Michelin / DR / Shows. Drift class: future Claude rewords to "No weekly closure patterns in {City}" / "Nothing notable here" / "Skipped" — all break the locked scannable signal. Added 2026-04-27 per the Weekly Closures.html validator-coverage audit pass — Tier 1 four-pack. *(Enforced — validate_itinerary.py WEEKLY CLOSURES — VALIDATOR-COVERAGE BACKFILL — D; 2026-04-27.)*

255. **🗓️ Weekly Closures — day name at the end of each entry is a valid weekday (Monday–Sundaysingular or plural).** Per `Brain/CORE RULES/Weekly Closures - Extra Section.html` § What each entry looks like — the `{Day}` slot at the end of `{Category} · Closed {Day}` resolves to a weekday name. Examples in the Doc use the plural form (`Tuesdays``Mondays``Sundays`) but singular (`Tuesday`) is also valid for a one-time pattern. The new check tightens Rule 252 by validating the day-token via regex `^(Monday|Tuesday|Wednesday|Thursday|Friday|Saturday|Sunday)s?$` (case-insensitive). Hard-FAILs on typos like `Munday` / `Wensday` / `Thurssday`on non-weekday tokens like `weekends` / `weekdays` / `midweek` (force the explicit weekday-name format for consistency across guides)and on multi-word patterns like `Mon-Tue` (force a single weekday or one row per day). Drift class: typos that pass spell-check but read wrong (`Sundayd` rendering oddly)or shorthand like `weekends` that's ambiguous (Sat? Sun? Sat+Sun?). Forcing the explicit weekday name keeps the pattern unambiguous. Added 2026-04-27 per approved Tier 2 single (E) alongside the Tier 1 four-pack. *(Enforced — validate_itinerary.py WEEKLY CLOSURES — VALIDATOR-COVERAGE BACKFILL — E; 2026-04-27.)*

256. **🏨 Title-page hotel name + address contain no placeholder tokens (`{...}` / `[...]` / `TBD` / `TODO` / `FIXME` / `???`).** Per `Brain/CORE RULES/Rules for Claude.html` § *Don't fabricate*: *"No invented hotelsaddressesURLs ... If it's not live-verified this buildit does not ship. Inference and guessing are fabrication."* The rule previously lived in `brain_core.md` (archived 2026-05-07 → `CLAUDE.md`) but surfaces specifically on the `.title-hotel` and `.title-address` content — anchored adjacent to `On Demand/Hotels & Rentals - On Demand.html` (which governs how hotels are picked → name + address ends up on the title page). Pre-existing checks cover (a) title-card scope (Rule 154 — currency / dates / room types / loyalty / amenities banned)(b) title-page children list (Rule 199 — exactly `[title-citytitle-hoteltitle-address]`)(c) `.title-city` uppercase (Rule 198)(d) hotel-name uniqueness across the document (line ~6675)(e) title-address Maps-anchor format (Rule 187). None of those catch a placeholder token. A guide shipping `<div class="title-hotel">{Hotel Name}</div>` (curly placeholder) or `<div class="title-address">[Street · Neighborhood]</div>` (square brackets) or `Hotel TBD` or `Address ???` slipped through every existing check despite being clear fabrication / incomplete-research drift. The new check walks every `.title-hotel` and `.title-address`strips HTMLand hard-FAILs on six placeholder patterns: `\{[^}]+\}` (curly)`\[[^\]]+\]` (square)`\bTBD\b``\bTODO\b``\bFIXME\b``\?{2,}` (two-or-more consecutive question marks). Drift class: a future Claude builds the guide before the owner has confirmed the hotel from her on-demand research outputleaves a placeholder in the title page intending to fill it in laterships the half-built guide. Closes the fabrication-class gap on the title-page surface. The Hotels & Rentals.html validator-coverage pass otherwise legitimately found NO new gaps because the Doc governs an on-demand research doc (rating thresholds / brand priorities / search orders) — not in-guide content. The shipped-guide hotel surface is already comprehensively covered by Hotel Banner / Rule 154 / hotel-uniqueness; this rule closes the one residual fabrication-class catch adjacent to that surface. Added 2026-04-27 per request *"agree with you!"* approval of the Path B (single Tier 1) recommendation in the Hotels & Rentals validator-coverage audit. *(Enforced — validate_itinerary.py HOTELS & RENTALS — VALIDATOR-COVERAGE BACKFILL — T1; 2026-04-27.)*

257. **✈️ Delta — no `✈` glyph or `delta.com` URLs in the shipped guide.** Per `On Demand/Delta - On Demand.html` + `Brain/CORE RULES/Rules for Claude.html`: *"the ✈️ icon is used only in flight researchnever in the itinerary."* Delta.html governs the on-demand FLIGHT research outputNOT in-guide content — same on-demand-doc-leak pattern as Weather (Rules 243 + 244). Pre-existing checks don't cover ✈️ glyph leaks or delta.com URL leaks. Drift class: a future Claude pastes flight research into the shipped guide as `✈️ Delta One SEA → LIS` or links to delta.combaking on-demand research output into a static guide that used to plan the trip from. The new check strips HTML to plain text and hard-FAILs on either (a) the `✈` glyph anywhere in rendered text (the variation-selector form `✈️` is a superset of the bare `✈` U+2708 codepointso the regex catches both)or (b) any `<a href>` URL containing `delta.com`. Mirror of the Weather Rule 243/244 pattern — same shapesame drift classapplied to the flight-research surface. The Delta.html validator-coverage pass otherwise legitimately found NO Delta-specific in-guide gaps because the Doc governs an on-demand research doc (origin/airline/class/routing/lounge/booking/price-check) — not in-guide content. Added 2026-04-27 per approved combined-slice for the Delta v2 content edits + the T1 flight-research-leak validator catch. *(Enforced — validate_itinerary.py DELTA — VALIDATOR-COVERAGE BACKFILL — T1; 2026-04-27.)*

258. **🚗 Car Rentals — no `🚗` glyph or car-rental-host URLs (`hertz.com` / `enterprise.com` / `avis.com` / `budget.com` / `europcar.com` / `sixt.com`) in the shipped guide.** Per `On Demand/Car Rentals - On Demand.html` + `CLAUDE.md` § 📚 Quick Reference (Glossary) (`🚗` = Car Rentals section header) + § *On-demand documents* (Car Rentals is one of the four on-demand-only docs alongside Weather / Flights / Hotels research / Rentals — none ship with the guide). Car Rentals.html governs the on-demand CAR RENTAL research outputNOT in-guide content — same on-demand-doc-leak pattern as Weather (Rules 243 + 244) and Delta (Rule 257). Pre-existing checks don't cover `🚗` glyph leaks or car-rental-host URL leaks. Drift class: a future Claude pastes car-rental research into the shipped guide as `🚗 Hertz pickup at LIS airport` or links to hertz.combaking on-demand research output into a static guide. The new check strips HTML to plain text and hard-FAILs on either (a) the `🚗` glyph anywhere in rendered textor (b) any `<a href>` URL containing one of the six car-rental-host domains (Hertz + the five fallback brands per Car Rentals.html brand priority). Mirror of the Weather + Delta pattern. The Car Rentals.html validator-coverage pass otherwise legitimately found NO Car Rentals-specific in-guide gaps because the Doc governs an on-demand research doc (just two bullets: Hertz default + fallback brand ladder) — not in-guide content. Added 2026-04-27 per approved combined-slice for the Car Rentals v2 content edits + the T1 car-rental-research-leak validator catch. *(Enforced — validate_itinerary.py CAR RENTALS — VALIDATOR-COVERAGE BACKFILL — T1; 2026-04-27.)*

259. **🚄 Day Trips by Train — full per-entry format (Train: + Why: + Book via) OR the literal negative-finding line when empty.** Per `Brain/CORE RULES/Day Trips by Train - Extra Section.html` (2026-04-27 cleanup slice) + `CLAUDE.md` § 📚 Quick Reference (Day Trips by Train index). Pre-2026-04-27 the validator (line ~4125) had two drift problems against the canonical PDF: (P6a) it scanned for `id="more-train"` while the canonical id per `CLAUDE.md` § 📚 Quick Reference cross-link list is `day-trips-by-train` — guides using the canonical id would silently skip the per-entry shape check; (P6b) the entry-shape check only required `.extras-sub` + `.ride-apps` with ≥20 chars of prose ("simplified shape" from a 2026-04-22 PDF iteration)but the v6 PDF restored the full format with explicit `Train:` / `Why:` / `Book via` lines per entry. The 2026-04-27 cleanup slice fixed both: section detection accepts the canonical `day-trips-by-train` id (legacy `more-train` kept as a one-version transition path until in-flight guides rebuild); per-entry shape check enforces `Train:` line + `Why:` label + `Book via` line OR allows the `Book via` to live in the optional section-level intro (a `.ride-apps` appearing before the first `.extras-sub` — shipped when every entry shares one rail operator). When the section is empty (no `.extras-sub` entriesonly `.extras-empty`)the literal phrase *"No qualifying day trips by train from {City}."* must appear. Drift class: rule simplification cycles in 2026-04-22 left the validator stuck in a transitional simplified shape; the v6 PDF rebuild + this validator catch-up restored both the format and the validator-PDF alignment. Pairs with Rule 21 (the cleanliness-side description of the section's two surfaces). Resolves the P6a + P6b findings surfaced during the 2026-04-27 Day Trips by Train content audit. *(Enforced — validate_itinerary.py DAY TRIPS BY TRAIN entry-shape + negative-finding check; 2026-04-27.)*

260. **☕ Cappuccino — top-3 entries each within ≤15 min walk of the hotel.** Per `Brain/CORE RULES/Cappuccino - Extra Section.html` § How to build the list: *"Top **3** cafés ≤15 min walk from the hotel · ordered by walk time (closest first)."* Pre-existing coverage on `id="cappuccino"` was section-presence + ordering only — nothing checked the per-entry walk-time ceiling. The new check walks every `.extras-sub` entry inside the Cappuccino sectionextracts the walk-time integer via `🚶\s*~?(\d+)\s*min`and hard-FAILs on any entry exceeding 15 minutes. Direct mirror of the RNH Rule 193 pattern (hotel-scopedtop-3walk-time ceiling) — Cappuccino is structurally parallel to Restaurants Near Hotel. Drift class: a future Claude widens the radius to 20-25 min walk because the closest cafés have weak espresso reviews — but the locked rule is "best within 15 min," not "best in the city" (the radius caps friction-from-hotel). If nothing qualifies in 15 minthe negative-finding line (Rule 265) ships instead. Added 2026-04-27 per request *"lets go ahead with your recommendation"* approval of the Cappuccino v2 combined slice (T1·T2·T3·T4·T5·T6 six-pack). *(Enforced — validate_itinerary.py CAPPUCCINO — VALIDATOR-COVERAGE BACKFILL — T1; 2026-04-27.)* **🔄 SUPERSEDED 2026-05-13:** cap raised to ≤25 min walk + top-5 cap (was top-3). Café name now links to top review (Yelp / Google Reviews / Google Maps) — not café own site. New chain-blocklist check added.

261. **☕ Cappuccino — top-3 ordered by walk timeclosest first.** Per `Brain/CORE RULES/Cappuccino - Extra Section.html` § How to build the list: *"ordered by walk time (closest first)."* Pre-existing presence check verified the section exists but didn't validate ordering. The new check collects walk-time integers in document order via `🚶\s*~?(\d+)\s*min` and hard-FAILs unless the sequence equals `sorted()` — strict ascending. Direct mirror of the RNH Rule 194 pattern. Drift class: a future Claude orders by espresso-quality / café fame / proximity-to-Maps-stop instead of by walk time — but the closest-first ordering is what makes the section scannable as "where do I go right now if I want a cappuccino with the least friction." Quality is already filtered upstream (the Bar imperative); ordering by walk time gives the second axis. Added 2026-04-27 per request *"lets go ahead with your recommendation"* approval of the Cappuccino v2 combined slice. *(Enforced — validate_itinerary.py CAPPUCCINO — VALIDATOR-COVERAGE BACKFILL — T2; 2026-04-27.)*

262. **☕ Cappuccino — every entry has 📍 address row + 🚶 walk row + 🏛 hours row.** Per `Brain/CORE RULES/Cappuccino - Extra Section.html` § What each entry looks like: *"`{Café Name}` (linked to café's site) · `📍 {Address}` (Google Maps anchor) · `🚶 ~{N} min walk from hotel` · `🏛️ Opens {time} · closed {day}`."* Pre-existing presence check didn't validate per-entry shape. The new check walks every `.extras-sub` entrystrips HTMLand hard-FAILs unless the entry body contains all three glyphs — `📍` (address)`🚶` (walk)`🏛` (hours/closed-day; the `🏛️` variation-selector form is a superset of the bare `🏛` codepoint). Direct mirror of the RNH Rule 195 entry-shape pattern. Drift class: a future Claude drops the hours row because "espresso bars are open early anyway" or drops the address row when the café name is a well-known chain — but the closed-day signal saves a wasted walk on the wrong morningand the address row carries the Maps anchor that makes the entry actionable. All three rows are load-bearing. Added 2026-04-27 per request *"lets go ahead with your recommendation"* approval of the Cappuccino v2 combined slice. *(Enforced — validate_itinerary.py CAPPUCCINO — VALIDATOR-COVERAGE BACKFILL — T3; 2026-04-27.)*

263. **☕ Cappuccino — every entry heading is linked to the café's site (not Mapsnot Wikipedia).** Per `Brain/CORE RULES/Cappuccino - Extra Section.html` § What each entry looks like: *"`{Café Name}` (linked to café's site)."* Pre-existing checks don't verify the heading anchor target. The new check walks every `.extras-sub` headingcollects all `<a href>` URLs in the headingfilters out `google.com/maps` / `maps.google.com` / `goo.gl/maps` / `wikipedia.org`and hard-FAILs unless at least one non-Mapsnon-Wikipedia URL remains. Direct mirror of the RNH Rule 196 heading-link pattern. Drift class: a future Claude links the café name to the Maps pin (since the address row already carries that linkthis is duplicative) or to a Wikipedia article (which describes the café's history rather than its current hours / menu / contact info) — but the heading link is the user's "open the café's homepage to confirm it's open today / look at the espresso menu / see Instagram for atmosphere" path. The Maps link belongs on `📍` (Rule 262) and Wikipedia is never a destination homepage. Added 2026-04-27 per request *"lets go ahead with your recommendation"* approval of the Cappuccino v2 combined slice. *(Enforced — validate_itinerary.py CAPPUCCINO — VALIDATOR-COVERAGE BACKFILL — T4; 2026-04-27.)*

264. **☕ Cappuccino — at most 3 entries.** Per `Brain/CORE RULES/Cappuccino - Extra Section.html` § How to build the list: *"Top **3** cafés."* Pre-existing presence check didn't enforce the top-3 cap. The new check counts `.extras-sub` entries inside `id="cappuccino"` and hard-FAILs if the count exceeds 3. Direct mirror of the DR Rule 229 cap pattern. Drift class: a future Claude expands to 4-5 entries because "there are several great cafés near this hotel" — but the locked top-3 rule is what makes the section a quick at-glance picknot a directory. If more than 3 qualifydrop the longest-walk ones until 3 remain (which compounds with Rule 261 ascending order — the cut always falls on the most distant). Added 2026-04-27 per request *"lets go ahead with your recommendation"* approval of the Cappuccino v2 combined slice. *(Enforced — validate_itinerary.py CAPPUCCINO — VALIDATOR-COVERAGE BACKFILL — T5; 2026-04-27.)* **🔄 SUPERSEDED 2026-05-13:** cap raised to top-5 per `Cappuccino - Extra Section.html` § 3 (companion to Rule 260's walk-radius supersession). Rule retained here for history.

265. **☕ Cappuccino — when zero entries shipthe section carries the literal `"No qualifying cappuccino spots within 15 min walk of the hotel."` negative-finding line.** Per `Brain/CORE RULES/Cappuccino - Extra Section.html` § When no qualifying cafés exist: *"Ship a single negative-finding line: `'No qualifying cappuccino spots within 15 min walk of the hotel.'`"* The line confirms the radius was checked and nothing passed — without ityou can't tell if the section was researched or skipped. Pre-existing extras-empty check (line ~3756) catches sections that are silently empty (no `.extras-sub` and no `.extras-empty`) but doesn't validate the literal phrase. The new check fires when the Cappuccino section ships zero `.extras-sub` entriesstrips HTML to plain textand hard-FAILs unless the phrase `No qualifying cappuccino spots within 15 min walk of the hotel` appears (case-insensitive). Mirror of the negative-finding pattern landed across Local Tastes (Rule 178) / Michelin / DR / Shows / Weekly Closures (Rule 254) / Day Trips by Train (Rule 259). Drift class: future Claude rewords to "No good cappuccino near hotel" / "Skipped — no nearby cafés" / "Nothing notable here" — all break the locked scannable signal. Pairs with Rule 260 (the radius is what defines "qualifying"). Added 2026-04-27 per request *"lets go ahead with your recommendation"* approval of the Cappuccino v2 combined slice. *(Enforced — validate_itinerary.py CAPPUCCINO — VALIDATOR-COVERAGE BACKFILL — T6; 2026-04-27.)* **🔄 SUPERSEDED 2026-05-13:** literal phrase updated to "within 25 min walk" (was 15) — see Cappuccino - Extra Section.html § 4 + Rule 260 supersession.

266. **🗺 Stops Structure — no banned named venue from the Doc's Exclude calibration list appears as a stop subject in any shipped guide.** Per `Brain/CORE RULES/Stops Structure.html` § What to Skip — three explicit named-venue lines (markets / shopping arcades / bookshops) anchor the bar with twelve calibration examples. The PDF is the **canonical home** for these names — they help a future Claude reason about what level of "iconic but excluded" looks like. They're allowed there as anchors. **Nowhere else.** The check walks every `.stop-name``.subject``.tour-name` heading + every anchor text inside `.ticket-box` and `.tour-box`strips HTMLand hard-FAILs (case-insensitive substring match) on any of the twelve banned named venues from the Doc's calibration lists. Drift class: a future Claude builds a Milan guide and proposes the iconic shopping-arcade by its architecture angle — but the Doc explicitly closes that loopholeand the validator backstops it mechanically. Same drift class on the iconic-bookshop angle ("but it's a literary landmark") and the iconic-market angle ("but it's a cultural / food experience"). The PDF's v5 cleanup (2026-04-27) stripped the redundant *"never propose"* / *"the architecture angle is not a loophole"* / *"the literary angle is not a loophole"* parentheticals because the **What to Skip** header already does that work — and this validator catch makes the exhortations even more redundant: the mechanical scrub closes the loopholes whether Claude rationalized past them or not. Pairs with Rule 26 (no real place names in brain prose elsewhere — this PDF is the sole exemption for the calibration anchors). Added 2026-04-27 per request *"yes... we kept as an example for you because these were very helpful to you"* approval of the T1 named-venue scrub. *(Enforced — validate_itinerary.py STOPS STRUCTURE — VALIDATOR-COVERAGE BACKFILL — T1; 2026-04-27.)*

267. **🗺 Stops Structure — no banned tour-type token from the Doc's Exclude lines appears as a stop subjecttour-box product titleor ticket-box anchor text.** Per `Brain/CORE RULES/Stops Structure.html` § What to Skip — three lines enumerate format-level tour bans: (a) Tours: *"private tours · museum-heavy tours · food tours · hop-on hop-off buses · Segway · horse carriage · pub crawls · boat parties"*; (b) Tastings & food: *"wineolive oilcheesetruffle tastings · cooking classes · perfume making · pottery · art workshops · truffle hunting"*; (c) Places & experiences: *"cemetery / ghost tours · street-art / graffiti tours."* Pre-existing checks don't enforce these — the validator surface for this PDF is content-policy enforcementand prior to this slice it was zero. The check walks every shipped subject surface (same surfaces as Rule 266) and matches case-insensitively against ~38 banned tour-type tokens with word-boundary gating. Hard-FAILs on any match. Drift class: future Claude searches Viator/GetYourGuide for the destinationfinds a top-rated *"{City} Food Tour"* or *"{City} Museums Private Tour"* productrationalizes that Local Tastes / Museums coverage justifies the formatand slips it in as a 🎟 ticket-box stop. The validator catches the tour-format token in the product title regardless of how the underlying activity is justified. Drift class also covers the cooking-class / wine-tasting / pub-crawl loopholes — common Viator product framings that don't pass the editorial bar but pass the surface "guided experience" sniff test. Pairs with Rule 266 (named-venue scrub) — together they cover the Doc's full "never propose" surface mechanically. Added 2026-04-27 per request *"ah interesting... sure!"* approval of the T2 banned-tour-type scrub. *(Enforced — validate_itinerary.py STOPS STRUCTURE — VALIDATOR-COVERAGE BACKFILL — T2; 2026-04-27.)*

268. **🗂 Guide Structure — End-of-Itinerary `.extras-section` elements appear in the locked 14-id sequence (`tours` → `weekly-closures` → `cappuccino` → `restaurants` → `downtown` → `local-tastes` → `food-delivery` → `shows` → `getting-around` → `stations-near-hotel` → `day-trips` → `pickleball` → `michelin` → `cities-gotchas`).** Per `Brain/CORE RULES/Guide Structure.html` § Section order: *"End-of-Itinerary Extrasin this order"* with the explicit fourteen-element numbered list. (`tours` prepended 2026-05-20; `food-delivery` was already in the order — both were missing from this entry's earlier text.) Pre-existing checks cover Trip at a Glance position (line ~7649) and TaaG card href↔id matching but DO NOT enforce the EoI sub-order — a guide could ship with sections in arbitrary order (Michelin firstWeekly Closures last) and pass every existing check. The new check walks every `.extras-section` in document orderextracts the `id` attributefilters to canonical-only ids (unknown ids are flagged separately by Rule 269)and verifies the filtered sequence is a non-strict subsequence of the canonical 14-id order. Hard-FAILs on out-of-order. Drift class: future Claude reorders sections by content priority (e.g.puts Michelin first because it's high-endor puts Getting Around first because it's logistics) — but the at-glance scan order depends on the canonical sequence. The order isn't aesthetic; it groups by use-pattern (scheduling info → morning routine → food → entertainment → logistics → special-occasion food). Added 2026-04-27 per request *"great ideasyes"* approval of the T1 four-pack. *(Enforced — validate_itinerary.py GUIDE STRUCTURE — VALIDATOR-COVERAGE BACKFILL — T1; 2026-04-27.)*

269. **🗂 Guide Structure — every `.extras-section` carries an `id` from the canonical 14-id list (`tours` / `weekly-closures` / `cappuccino` / `restaurants` / `downtown` / `local-tastes` / `food-delivery` / `shows` / `getting-around` / `stations-near-hotel` / `day-trips` / `pickleball` / `michelin` / `cities-gotchas`) — no drift forms.** Per `Brain/CORE RULES/Guide Structure.html` § Cross-links / anchors: *"each section gets an `id` for in-page jumping"* with the explicit fourteen-element list. Pre-existing href=id matching check (line ~7647) catches BROKEN cross-links (when an `<a href="#x">` points to a non-existent id) but doesn't catch the case where an `.extras-section` carries a NON-CANONICAL id — e.g.`id="weekly_closures"` (underscore drift) or `id="restaurants-near-hotel"` (descriptive variant) or `id="day_trips_by_train"`. Such drift would pass the href=id check (the section's own internal anchors might be self-consistent) but break cross-links from elsewhere in the brain (TaaG cardsCLAUDE.mdother Docs' references). The new check walks every `.extras-section` and hard-FAILs on any id outside the canonical 14-id set. Special note on `restaurants` vs `restaurants-near-hotel`: the canonical id is the bare `restaurants` (preserved for backward compat per Doc § Cross-links — *"anchor name preserved for backward compatibility with shipped guides linking #restaurants; visible heading is 🍴 Restaurants Near Hotel"*). Drift class: well-meaning Claude renames `id="restaurants"` to `id="restaurants-near-hotel"` to match the renamed Doc — but the anchor name is intentionally frozen. Added 2026-04-27 per request *"great ideasyes"* approval of the T1 four-pack. *(Enforced — validate_itinerary.py GUIDE STRUCTURE — VALIDATOR-COVERAGE BACKFILL — T2; 2026-04-27.)*

270. **🗂 Guide Structure — every `.extras-section` element sits AFTER the last `.day-block` element in the document.** Per `Brain/CORE RULES/Guide Structure.html` § Section order: the top-to-bottom flow is Title Page → Trip at a Glance → Day blocks → End-of-Itinerary Extras → Claude Inspiration. The EoI sections come AFTER all day-blocks. Pre-existing TaaG-position check (line ~7649) covers the same shape on the Trip-at-a-Glance side (`.glance-section` sits between `.title-page` and the first `.day-block`); this rule is the mirror catch on the EoI side. The new check finds the byte offset of the LAST `.day-block` opening tagthen flags any `.extras-section` with a smaller offset (i.e.before/between day-blocks). Hard-FAILs on any misplaced section. Drift class: future Claude inserts a thematically-appropriate EoI section into a day's body — e.g.dropping a "🍮 Local Tastes" section into Day 3 right after a stop that touches local cuisineor floating a "Getting Around" section next to a metro-heavy day. Both feel helpful but break the canonical layout — the EoI sections are at-end-of-itinerary by design (the reference surface lives there). Pairs with Rule 268 (order) and Rule 269 (id validity) — together they enforce the full EoI structural contract. Added 2026-04-27 per request *"great ideasyes"* approval of the T1 four-pack. *(Enforced — validate_itinerary.py GUIDE STRUCTURE — VALIDATOR-COVERAGE BACKFILL — T3; 2026-04-27.)*

271. **🗂 Guide Structure — every shipped guide carries all universal End-of-Itinerary sections (the canonical ids minus the conditional Pickleball + Cities Gotchas; with negative-finding line when empty); never silently skipped.** *(Count note: 12 universal always-required today (Tours rollout complete as of 2026-05-30). This entry's original "10" predated `food-delivery` being counted.)* Per `Brain/CORE RULES/Guide Structure.html` § Universal shipping (added 2026-04-27 in this slice): *"Every guide ships all 10 universal sections. Sections with no qualifying content ship a negative-finding line per their Doc rulenever silently skipped."* Pre-existing checks across the negative-finding pattern (Rules 178 / 254 / 259 / 265 plus extras-empty rules) ensure that *if* a section is shippedit carries either entries or the negative-finding line — but no rule enforced "the section must be shipped." A guide could pass every prior check while silently dropping (e.g.) Day Trips by Train when Claude rationalized "no rail network here." The new check walks every `.extras-section`collects canonical ids presentand hard-FAILs if any of the 10 canonical ids is missing. The check gates on the document containing at least one `.day-block` (so isolated test fixtures / brain snippets don't false-positive). Drift class: future Claude builds a small-city / island / desert-resort guide and silently drops Cappuccino ("no Italian café culture")Day Trips by Train ("no rail network")or Michelin ("no starred restaurants in the country") — but per the universal-shipping rule landed in v6the right move is always to ship the section with the negative-finding line. T4 catches the silent-skip drift mechanically. Pairs with the eight per-section negative-finding rules (Rules 178 / 254 / 259 / 265 etc.) — together they enforce that every guide carries every EoI section in the canonical scannable form. Added 2026-04-27 per request *"yesplease"* approval of the T4 universal-presence checkafter she green-lit C1 (the universal-shipping clarifier in PDF v6). *(Enforced — validate_itinerary.py GUIDE STRUCTURE — VALIDATOR-COVERAGE BACKFILL — T4; 2026-04-27.)*

272. **💡 Claude Inspiration — when shippedit sits as the LAST section of the guide; no `.extras-section``.day-block`or `.glance-section` appears after it.** Per `Brain/CORE RULES/Claude Inspiration - Extra Section.html` § Where it sits in the guide (locked placement landed 2026-04-27): *"Always the last section of the guide — after the End-of-Itinerary Extraswith nothing after it. Locked placement; not flexible."* Pre-v2 the placement rule was *"default at end OR anywhere it fits the content"* — flexible. the owner tightened to LAST in this slice: *"lets say it will be the last section of the guide"*. The cross-surface ripple updated Guide Structure.html → v7 to match. The new check fires only when `.claude-inspiration` exists in the document (the section is OPTIONAL — when not shippedthis rule doesn't fire). When presentthe check finds the section's byte offset and scans for any `.extras-section` / `.day-block` / `.glance-section` element after it. Hard-FAILs on any. Drift class: future Claude places Claude Inspiration mid-guide (between two days as atmospheric framing) or at the top (as opening framing) — natural-feeling placements that match the pre-v2 flexibility but break the locked v2 rule. The lock helps the at-glance scan: the section is always findable in one place. Pairs with Rule 273 (length) + Rule 274 (HTML structure) — together they enforce the full Claude-Inspiration shipping contract. Added 2026-04-27 per request *"yesmake sure it has everything needed to ship if you decide to use"* approval of the T1·T2·T3 three-pack. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — VALIDATOR-COVERAGE BACKFILL — T1; 2026-04-27.)*

273. **💡 Claude Inspiration — when shippedbody is under 6 lines / ≤150 words.** Per `Brain/CORE RULES/Claude Inspiration - Extra Section.html` § What this section is: *"under 6 lines... Free-form prose. No templateno required structureno bullet checklist."* The under-6-lines rule keeps the section pithy by design — it's a small creative-voice spacenot an essay slot. Pre-existing checks didn't enforce this (the validator had zero coverage on this PDF before the slice). Mechanically defining "lines" is fuzzy because visual rendered lines depend on CSS / browser widthso the new check uses TWO complementary metrics: (a) explicit line-break count via `<br>` + `</p>` ≤ 6and (b) word count ≤ 150 (proxy for ~6 visual lines that's stable across rendering surfaces). Hard-FAILs if EITHER metric exceeds. The body content stripped of HTML excludes the `.extras-title` heading line (which doesn't count toward the 6-line body limit). Drift class: future Claude pads the section with multiple paragraphs because "this trip had so many cool moments" — defeats the design. The rule isn't anti-content; it's pro-pithy. If there's a lot to sharepick the best one. Pairs with Rule 272 (placement) + Rule 274 (HTML structure). Added 2026-04-27 per request *"yesmake sure it has everything needed to ship if you decide to use"* approval. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — VALIDATOR-COVERAGE BACKFILL — T2; 2026-04-27.)*

274. **💡 Claude Inspiration — when shippedhas everything needed to render (`.extras-title` heading + non-empty body content); when nothing to sharethe section is skipped entirely (no placeholder).** Per `Brain/CORE RULES/Claude Inspiration - Extra Section.html` § HTML structure when shipped: *"the section carries everything needed to render cleanly — same shape as the End-of-Itinerary sectionsjust with its own class: `<div class="claude-inspiration" id="claude-inspiration">` with `<div class="extras-title">{Icon} {Title}</div>` heading + body prose."* Per § How it works: *"Optional. Only ships when something genuinely worth sharing came up during research. If nothing surfacedskip the section entirely — no negative-finding lineno placeholder."* The new check fires when `.claude-inspiration` exists; verifies the section contains an `.extras-title` heading AND has substantive body content (>20 non-tag characters after stripping HTMLaccounting for whitespace). Hard-FAILs on missing heading OR empty body. Drift class: future Claude ships an empty `<div class="claude-inspiration"></div>` shell because "the section was supposed to be there" (confusing the optional Claude-Inspiration with the universal-shipping rule for the 10 universal EoI sections). Or ships a placeholder like *"Nothing notable this trip."* — exactly what the Doc prohibits. The rule: ship with real contentOR don't ship the div at all. Half-empty isn't a state. Pairs with Rule 272 (placement) + Rule 273 (length). Per request framing of the locked rules: *"yesmake sure it has everything needed to ship if you decide to use"* — the section is opt-in; opting in carries an implicit promise of complete content. Added 2026-04-27. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — VALIDATOR-COVERAGE BACKFILL — T3; 2026-04-27.)*


275. **🏓 Pickleball section heading uses the 🏓 Unicode emoji — NOT an `<img>` tag or any custom image file.** All section icons are Unicode emoji. The check (1) locates the `.extras-section[id="pickleball"]` div(2) finds every `.extras-title` heading inside it(3) verifies the heading text contains 🏓 and does NOT contain any `<img>` tag. Hard-FAILs if 🏓 is missing or if an `<img>` tag is present. Drift class: early builds shipped pickleball with a custom image instead of emoji — emoji is the correct and only format. *(Enforced — validate_itinerary.py PICKLEBALL HEADING icon check; updated 2026-05-10.)*

276. **🥒 Pickleball entries do NOT lead with 🏟️ indoor marker.** Per request 2026-05-03: the 🏟️ indoor-venue marker on Pickleball entries is retired — the 🏓 heading emoji is the visual identity; having a second indoor marker on every entry is double-marking the same signal. The check finds every `.extras-sub` entry inside `id="pickleball"` and flags any entry whose heading text starts with or leads with 🏟️. Hard-FAILs on any match. Drift class: future Claude adds 🏟️ to every Pickleball entry because the venue IS an indoor facility — but the locked rule is that the section heading handles the indoor signal. Pairs with Rule 275 (icon). *(Enforced — validate_itinerary.py PICKLEBALL ENTRY indoor-marker check; added 2026-05-03.)*

277. **Universal EoI row order — info (🏛/🎟) → map (📍) → walk (🚶) — applies to every `.entry-body` and `.shows-box` across all EoI sections.** Per request 2026-05-03: *"everything there is a mapthe walk is below; walk is below map; map is below hours."* Every EoI section box (`.entry-body``.shows-box`) must follow the same row order: info/hours (🏛 or 🎟) comes firstthen the map anchor (📍)then the walk time (🚶) when present. This is the universal version of per-section checks that already existed for Cappuccino + Restaurants Near Hotel entries — the universal pass catches the same drift in PickleballTrain Stations Near HotelDowntown RestaurantsShowsand Michelin entry boxes. The new check walks every `.entry-body` and `.shows-box` in the guidefinds the string positions of 🏛/🎟 (info)📍 (map)and 🚶 (walk)and hard-FAILs on two violations: (a) info row AFTER 📍(b) 🚶 walk BEFORE 📍. Drift class: Pasadena v1 build 2026-05-03 shipped Cappuccino + Stations + Pickleball entries with 🚶 walk row BEFORE 📍 map row — the per-section validators had been silently passing the old row order. The universal check is the safety net for any future EoI surface that picks up the same drift. *(Enforced — validate_itinerary.py UNIVERSAL EoI ROW-ORDER CHECK; added 2026-05-03.)*

278. **🆓 stop flag only on non-obvious free venues — not on stops whose name contains a public-space term (park / street / plaza / promenade / pier / district / square / boulevard / avenue / walk of fame / campus / etc.).** Per request 2026-05-03: *"free is to be used conservatively — when it's a surprise to be freenot streetetc."* and *"in doubtdon't put free."* The 🆓 flag is for venues where free admission is NOT obvious — a museuman indoor venuesomewhere that looks like it should charge but doesn't. Public streetsplazasparkspierspromenadesdistrictssquaresboulevardspublic courtyardsand transit hubs have obviously-zero admission; the 🆓 marker is noise on top of the obvious. The rule lives in `Brain/CORE RULES/Stops Structure.html` § 5 but the Doc's *"🆓 Free — admission cost is zero"* definition doesn't yet explicitly exclude obvious-free spaces; this validator heuristic enforces the spirit until the Doc is updated. The check walks every `.stop-block` that contains 🆓extracts the `.stop-name` textand hard-FAILs (case-insensitive substring) if the name contains any of ~25 public-space terms. Drift class: Pasadena v1 build 2026-05-03 shipped 🆓 on Old Pasadena (district)Pasadena City Hall (public courtyard)Rose Bowl exterior (public path)Union Station (transit hub)Olvera Street (public plaza)Hollywood Walk of Fame (public street)Santa Monica Pier (public pier)Third Street Promenade (pedestrian mall). *(Enforced — validate_itinerary.py 🆓 FLAG MISUSE check; added 2026-05-03.)*

279. **🥒 Guide Structure — Pickleball section (#10) ships ONLY for CA + AZ trips; silently omitted for all other destinations — no section headerno negative-finding line.** Per `Brain/CORE RULES/Guide Structure.html` § Section order — Pickleball is a conditional EoI sectiongreen-lit 2026-05-02. The validator detects the trip state from `.title-address` text (Pattern A: state code before US ZIP; Pattern B: state code after comma). Three-branch logic: (a) state ∈ {CAAZ} → `id="pickleball"` MUST be presenthard-FAIL if absent; (b) state ∉ {CAAZ} → pickleball MUST be absenthard-FAIL if present; (c) state undetectable (non-US guide or unusual address shape) + pickleball section present → hard-FAIL (can't prove eligibility). Unlike the 10 universal EoI sectionspickleball silently omits when the eligibility condition isn't met — no negative-finding lineno placeholder. Pre-existing T1/T2/T3/T4 Guide Structure checks cover all universally-required sections but skip the two conditional ones; T5 fills the gap for Pickleball. Pairs with Rule 280 (T6 — Cities Gotchas) — together they enforce the two conditional EoI ship-gates. Added 2026-05-02 per approval for the T5/T6 pair. *(Enforced — validate_itinerary.py GUIDE STRUCTURE — T5 Pickleball ship-gate; added 2026-05-02.)*

280. **⚠️ Guide Structure — Cities Gotchas section (#12) ships ONLY when `Brain/mds/Cities Gotchas.md` has a `## {CityName}` heading matching the trip city; silently omitted otherwise — no section headerno negative-finding line.** Per `Brain/CORE RULES/Guide Structure.html` § Section order — Cities Gotchas is a conditional EoI sectiongreen-lit 2026-05-02. The validator parses `Brain/mds/Cities Gotchas.md` at ship timeextracts all `## {City}` headingsand checks whether the trip city (lowercased `.title-card` text) matches any. Two-branch logic: (a) match found → `id="cities-gotchas"` MUST be present; (b) no match → section MUST be absent. If Cities Gotchas.md is missing or unreadableT1/T2/T3/T4 still rail the id validity and ordering of any cities-gotchas section that does ship — they just can't enforce the gate direction. Drift class: future Claude ships a Cities Gotchas section for a city with no gotcha entries yet (the section is empty)or silently drops it for a city that DOES have entries (reminder is lost). The gate enforces the right direction mechanically for any city that has at least one entry logged. Pairs with Rule 279 (T5 — Pickleball). Added 2026-05-02. *(Enforced — validate_itinerary.py GUIDE STRUCTURE — T6 Cities Gotchas ship-gate; added 2026-05-02.)*

281. **🗂 Guide Structure — EoI section order updated to 12-element sequence (2026-05-03); canonical order includes `stations-near-hotel` (#8) which pushed conditional sections to `pickleball` #10 and `cities-gotchas` #12.** Pre-2026-05-02 the canonical EoI order was 9 sections (weekly-closures → cappuccino → restaurants → downtown → local-tastes → shows → getting-around → day-trips → michelin). 2026-05-02 extended it to 11 by adding pickleball (#9) and cities-gotchas (#11). Subsequently `stations-near-hotel` was inserted at position #8 making the sequence 12: `weekly-closures → cappuccino → restaurants → downtown → local-tastes → shows → getting-around → stations-near-hotel → day-trips → pickleball → michelin → cities-gotchas`. The T1 order check filters the in-document ids against the canonical sequence; both conditional ids are in the canonical list so the order check applies to them when present. The T4 universal-presence check covers only the 10 universal sections — `pickleball` and `cities-gotchas` are excluded from T4 because they're conditional; T5/T6 (Rules 279/280) gate them instead. The `day-trips-by-train` id was also renamed to `day-trips` in the 2026-05-02 slice (B17); the validator accepts both for backward compat on in-flight guides. **2026-05-20 update:** `food-delivery` (between `local-tastes` and `shows`) and `tours` (prepended as the first EoI sectionbefore `weekly-closures`) bring the canonical sequence to **14 ids**. `tours` is universal-but-gated by `TOURS_EXCLUDED_GUIDES` during its guide-by-guide rollout. So wherever earlier entries in this file say "12-id" / "10 universal," read them as historical snapshots — the current canonical count is 14 ids (Rules 268/269 carry the up-to-date list). *(Context entry — see validate_itinerary.py GUIDE STRUCTURE — VALIDATOR-COVERAGE BACKFILL header comment; updated 2026-05-20.)*

282. **📍 address rows — link text must be the address itselfnever the word "Maps".** Per `Brain/CORE RULES/Icon Order and Format.html` row 9 (updated 2026-05-17): the old format `📍 Address [Maps]` — where "Maps" is the anchor text — is retired. New format: `📍 [Address]` as a single clickable linkwith the address text itself as the anchor. The guide-wide check scans every `<a>` element in the rendered HTML and hard-FAILs if any anchor's visible text is exactly "Maps" (case-insensitive) — a sure sign the old `[Maps]` template survived a copy-paste. The word "Maps" as link text is now always wrong in a shipped guideregardless of context. Drift class: caught across multiple guides on 2026-05-17 when the Michelin 📍 row format was updated in CORE RULES and old guides carried the legacy `[Maps]` form. *(Enforced — validate_itinerary.py `── 📍 'Maps' LINK TEXT DRIFT ──`; added 2026-05-17.)*

283. **⭐ icon permitted in `.extras-sub` headings for cappuccinorestaurants (Restaurants Near Hotel)and downtown sections.** The `_SECTION_ICON_ALLOWLISTS` in `validate_itinerary.py` was extended 2026-05-17 to permit `⭐` and `★` in cappuccino / restaurants / downtown section heading icon slots. Restaurant and café rating stars appear naturally in these headings (e.g. `⭐ 4.7 · Café Name`); previously these sections had no star icon in their allowlistscausing false-positive FAIL on any heading that opened with a rating star. The allowlist fix is surgical — star icons remain disallowed in other EoI sections (showslocal-tastesgetting-aroundetc.) where a leading ⭐ would be a genuine build error. Drift class: future builds of restaurant or cappuccino sections that include star ratings in the subheading will no longer trigger a spurious icon-allowlist failure. *(Enforced — validate_itinerary.py extras-sub icon allowlist `_SECTION_ICON_ALLOWLISTS`; updated 2026-05-17.)*

284. **`.next-tram` always follows `.next` — never precedes or appears without the main motion banner.** Per `Brain/CORE RULES/Motion Rule.html` §1 Stops pattern: the 🚎 tram sub-line (`.next-tram` div) is an add-on to the main ride-app hop (`.next`)not a standalone opener. A `.next-tram` that appears before the first `.next` in its day-block means the tram sub-line has been detached from its paired main banner — a structural error. The new check scans every `.day-block` for `.next` and `.next-tram` events in document order; for each `.next-tram`it verifies at least one `.next` appears earlier in the same day. Hard-FAILs when a `.next-tram` has no preceding `.next` (one violation surfaced per day). Drift class: a future Claude builds a day where the tram-only variation is accidentally placed before the main walk/ride-app hop — the tram sub-line renders before the reader has seen the transport mode it supplements. Added 2026-05-17 per Motion Rule.html §1 gap audit — GAP A finding. *(Enforced — validate_itinerary.py `── .next-tram ORDERING — must follow .next ──`; added 2026-05-17.)*

285. **⚠️ Cities Gotchas — no 🚶 walk or 🚕 ride rows inside entries.** Per `Brain/CORE RULES/Motion Rule.html` §2 Extra sections — Cities Gotchas falls in the "without motion rows" list alongside DowntownLocal Tastesand Michelin. Gotcha entries are contextual warnings (❗ venue + gotcha description + Workaround)not navigation surfaces — the reader uses this section to avoid mistakesnot to plan a route. Walk/ride rows are either hotel-scoped (Cappuccino/Restaurants Near Hotel) or district-scoped (Downtown); neither role applies inside a gotcha body. The new check walks every entry body inside `id="cities-gotchas"` and hard-FAILs on any row containing 🚶 or 🚕. Mirror of the Downtown Restaurants no-walk check at line ~7264 (Downtown was coveredCities Gotchas was the remaining gap). Drift class: a future Claude pastes a gotcha from a navigation-heavy source and includes a "🚶 5 min from hotel" row thinking it's helpful context — but the section's contract is warnings-only. Added 2026-05-17 per Motion Rule.html §2 gap audit — GAP B finding. *(Enforced — validate_itinerary.py Cities Gotchas no-motion-row check inside `if cities_gotchas_present:` block; added 2026-05-17.)*

286. **🍽️ Downtown Restaurants — 📍 row must NOT include a postal code — postal codes are globally banned.** Per `Brain/CORE RULES/Links.html` §6 and `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` §5 updated format: `📍 Address` — street + neighborhood onlyno postal codeno city nameno country. Drift class: future Claude appends a ZIP/postcode to the address rowadding noise the reader cannot act on. **🔄 REVERSED 2026-05-19 (supersedes original 2026-05-17 "must include" rule):** postal codes removed from all address display text globally. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — A3 postal-code check REMOVED; global postal-code ban check active.)*

287. **🍽️ Downtown Restaurants — 🏛 row must contain hours text (non-empty after the emoji).** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` §5 entry format: `🏛 [Hours]` — the hours field is required; a bare 🏛 with no text is a build stub. Drift class: future Claude ships `🏛` as a placeholder row without filling in the actual hoursleaving the owner with no information about when to visit. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — A2 hours-text check; added 2026-05-17.)*

288. **🍽️ Downtown Restaurants — restaurant heading must be a hyperlink (no bare plain-text names).** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` §5: the restaurant name links to a review host. The heading must contain at least one `<a>` element — a plain-text name with no link is a build stub. Distinct from Rule 227 (which validates the link destination) — this rule catches the case where no `<a>` tag exists at all. Drift class: future Claude lists the restaurant name as a plain label thinking the 📍 map link is sufficient — but the heading link is the primary review surface. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — B1 heading-link-present check; added 2026-05-17.)*

289. **🍽️ Downtown Restaurants — heading must include ⭐ star rating in N.N⭐ decimal format AND review count (NNN+ reviews).** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` §5: `[Name] N.N⭐ · NNN+ reviews` — three components are required together: (a) ⭐ present(b) decimal number before the ⭐ (e.g. `4.2⭐`not bare `⭐`)(c) review count in `NNN+` format. Three separate sub-checks fire: B3 (⭐ present)B4 (decimal format)B5 (review count). Drift class: future Claude ships `Café Name ⭐` without a rating numberor `4.2⭐` without the review count — both lose the ranking signal used to compare picks. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — B3/B4/B5 star-rating and review-count checks; added 2026-05-17.)*

290. **🍽️ Downtown Restaurants — no overlap with Restaurants Near Hotel (🦫) section.** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` §4: *"No overlap with Restaurants Near Hotel."* The two sections serve different scopes — Restaurants Near Hotel is walking-distance; Downtown is historic-core best-of. The same restaurant in both sections double-counts and defeats the section split. Case-insensitive set intersection of plain-text restaurant names across both sections. Drift class: a future Claude recommends the same restaurant in both sections because it happens to be both near the hotel AND in the historic downtown. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — G RNH-overlap cross-check; added 2026-05-17.)*

291. **🍽️ Downtown Restaurants — no duplicate restaurant names within the Downtown section.** The same restaurant name appearing twice inside `#downtown` is always a build error — either a copy-paste duplicate or two entries for the same place under slightly different names. Case-insensitive; short-circuits on first collision. Drift class: future Claude adds the same restaurant twice with slightly different heading text (`Café Cento` vs `Cafe Cento`). *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — H duplicate-names check; added 2026-05-17.)*

292. **🍽️ Downtown Restaurants — no seafood-focused restaurants (sushi permitted; all other seafood-centric concepts excluded).** Per `Brain/CORE RULES/Downtown Restaurants - Extra Section.html` §4 seafood exclusion: fish/shellfish/crustacean-centric venues are excluded from the Downtown top-4 picks. Sushi is explicitly exempted. The check scans entry cuisine descriptions for seafood-focused terms (lobstercraboysteretc.). Drift class: a future Claude includes a well-reviewed fish restaurant in the top picks without noticing the seafood exclusion rule. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — I seafood-exclusion check; added 2026-05-17.)*

293. **🍽️ Downtown Restaurants — no wrong box class inside section (transit-box / shows-box / station-box are format drift; entry-body is the correct wrapper).** The DR entry format uses `.entry-body` wrappers — the same structure as Michelin entries. Transit/shows/station-box classes belong to other sections. Presence of a wrong box class indicates a copy-paste from a different section. Drift class: future Claude pastes a Shows entry into DR without stripping the `.shows-box` class. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — J box-class drift check; added 2026-05-17.)*

294. **🍽️ Downtown Restaurants — no build annotations in entries ([UPDATE]/[CHECK]/🔴/[TBD] etc. must be stripped before shipping).** Same annotation-leakage pattern as Local TastesFood Deliveryand Stop Names checks. The check scans every entry's heading and body rows for `[UPDATE]``[CHECK]``[TBD]``[TODO]``[FIXME]``[VERIFY]``[RESEARCH]``[PLACEHOLDER]`🔴🟡🟢. Drift class: a future Claude ships an entry with `[UPDATE hours]` still in the 🏛 row because the hours weren't confirmed before shipping. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — L annotation-leakage check; added 2026-05-17.)*

295. **🍽️ Downtown Restaurants — no `<p>` tags inside section (entries are `.entry-body` divs; `<p>` is structural drift).** DR entries use the same div-based structure as Michelin entries. A `<p>` element inside the DR section signals prose that was typed outside a boxor a copy-paste from a text document. Drift class: future Claude writes a narrative intro paragraph before the entries. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — M p-tag check; added 2026-05-17.)*

296. **🍽️ Downtown Restaurants — no intro prose before first entry (section goes directly from `.extras-title` to entries; no narrative preamble).** The section opens with the title div and then immediately the first entry box — no connecting proseno context paragraph. Any plain-text node appearing before the first `.extras-sub` is structural drift. Drift class: future Claude writes a sentence like "The historic core of this city has some great options:" before listing the entries. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — N intro-prose check; added 2026-05-17.)*

297. **🍽️ Downtown Restaurants — no chain or tourist-trap restaurants (per §4: "Chains and tourist-trap caricatures are excluded even when geographically downtown").** A hard-coded list of ~22 global chain and tourist-trap brand names (McDonald'sBurger KingHard Rock CafeCheesecake Factoryetc.) is checked against every entry heading. Case-insensitive substring match. Drift class: a future Claude includes a globally recognizable chain because the specific location is highly rated — the spec excludes chains regardless of rating. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — O chain/tourist-trap check; added 2026-05-17.)*

298. **🍽️ Downtown Restaurants — entry name must be a proper restaurant namenot a bare category word ("Restaurant""Café""Bar""Bistro" etc.).** Same guard as Pickleball and Train Stations Near Hotel. A regex matches entry headings that are just a bare category word with no proper name — these are placeholder or unfilled build stubs. Drift class: future Claude builds an entry shell with `Café` as the name because the actual venue name wasn't confirmed yet. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — P generic-name guard; added 2026-05-17.)*

299. **🍽️ Downtown Restaurants — `Downtown Restaurants - Extra Section.html` calibration anchors intact (10 spec anchors checked).** The drift-protection block reads the CORE RULES spec and verifies 10 key anchor strings still exist: top-4 capchains exclusionseafood exclusionno RNH overlapcuisine ≤80 charsno walk/ride rowsNNN+ reviews formatpostal codeclosed row formatsection icon 🍽️. A missing anchor means the spec was trimmed or rewritten and the validator may now misread the rules. *(Enforced — validate_itinerary.py DOWNTOWN RESTAURANTS — Q spec drift-protection; added 2026-05-17.)*

300. **💡 Claude Inspiration — body content uses `<p>` prose blocks; at least one non-empty `<p>` required.** Per `Brain/CORE RULES/Claude Inspiration - Extra Section.html` HTML structure when shipped: *"each [insight] as its own `<p>` block."* Free-form prose but the container must be `<p>` — not a bare `<div>`not flat text. Check T4 fires when the section exists and finds zero non-empty `<p>` elements in the body (title excluded). Drift class: future Claude writes prose directly in the outer div without wrapping in `<p>` tags. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — T4 p-block check; added 2026-05-17.)*

301. **💡 Claude Inspiration — no bullet or numbered lists (`<ul>`/`<ol>`/`<li>` tags are format drift; prose only).** Per spec: *"Free-form prose. No templateno required structureno bullet checklist."* If a bullet list appears inside the section it means the content was written as a list rather than prose — the section's design intent is violated. Drift class: future Claude writes "Things I found interesting: • item1 • item2" using an `<ul>`. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — T5 list-tags check; added 2026-05-17.)*

302. **💡 Claude Inspiration — no logistics containers inside (`.entry-body``.transit-box``.shows-box``.extras-sub` are format drift).** The Claude Inspiration section is pure prose — no EoI entry boxesno navigation containers. Any structured-entry div class from another section landing here means a copy-paste from the wrong source. Drift class: future Claude pastes a Michelin entry into the inspiration section thinking the restaurant belongs as a creative note. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — T6 logistics-container check; added 2026-05-17.)*

303. **💡 Claude Inspiration — outer `<div>` must carry `id="claude-inspiration"`.** Per spec §4: the outer element is `<div class="claude-inspiration" id="claude-inspiration">`. The `id=` is required for in-page cross-linking and validator detection. A section without the id attribute still renders visually but breaks any href that targets it. Drift class: future Claude ships the section without the id attribute because the class alone seemed sufficient. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — id-attribute check; added 2026-05-17.)*

304. **💡 Claude Inspiration — `.extras-title` heading must contain at least one emoji/iconand must have substantive text after that icon.** Per spec: the section title carries an icon (e.g. `💡 Claude's Inspiration`) — a bare text heading with no iconor an icon-only heading with no label textis incomplete. Two sub-checks: (a) icon present in title(b) text after the icon is non-empty. Drift class: future Claude ships `<div class="extras-title">Claude's Inspiration</div>` without the iconor ships `<div class="extras-title">💡</div>` with only the emoji. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — extras-title icon + text checks; added 2026-05-17.)*

305. **💡 Claude Inspiration — outer `<div>` class must include a `theme-{color}` modifier.** Per spec §4: `class="claude-inspiration theme-{color}"` — the theme modifier applies the section's visual color treatment. Without it the section renders in the default stylelosing the intended visual distinction. The check scans the outer div's class attribute for the `theme-` prefix. Drift class: future Claude ships the section without a theme class because it wasn't in the copy-paste template. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — theme-class check; added 2026-05-17.)*

306. **💡 Claude Inspiration — no build annotations in prose ([UPDATE]/🔴/[TBD] etc. must be stripped before shipping).** Same annotation-leakage guard as other EoI sections. Scans the full section inner HTML for `[UPDATE]``[CHECK]``[TBD]``[TODO]``[FIXME]``[VERIFY]``[RESEARCH]``[PLACEHOLDER]`🔴🟡🟢. Any annotation surviving into a shipped guide is a build error. Drift class: a future Claude ships `[RESEARCH — was this confirmed?]` inside a prose paragraph without noticing it during final review. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — annotation-leakage check; added 2026-05-17.)*

307. **💡 Claude Inspiration — no empty `<p>` blocks; no `<br>` tags inside section.** Empty `<p>` elements are spacer stubs with no prose value — any `<p>` must contain substantive text (≥10 chars after stripping HTML). `<br>` tags are also banned: paragraph spacing comes from the `<p>`/`</p>` structurenot line breaks. Two sub-checks: (a) each `<p>` has ≥10 non-tag characters(b) zero `<br>` tags. Drift class: future Claude uses `<br><br>` between prose paragraphs instead of separate `<p>` blocks. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — empty-p and br-tag checks; added 2026-05-17.)*

308. **💡 Claude Inspiration — `theme-{color}` must use a valid color name (one of the locked color token set).** The theme modifier is `theme-{color}` where `{color}` must be one of the guide's locked color tokens. An arbitrary or misspelled color name (`theme-blueish``theme-colour1`) produces a broken class that the stylesheet ignores. The check extracts the `theme-` value and tests it against the allowed set. Drift class: future Claude invents `theme-slate` or `theme-warm` without checking the locked token list. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — theme-color-valid check; added 2026-05-17.)*

309. **💡 Claude Inspiration — no `<h2>`–`<h6>` heading tags inside section (flat prose; heading markup is format drift).** The section is free-form prose inside `<p>` blocks — HTML heading elements are structural markup from a document modelnot appropriate inside a small creative-voice section. Any `<h>` tag indicates a copy-paste from a structured document. Drift class: future Claude writes `<h3>What stood out</h3>` as an internal subheader. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — no-h-tags check; added 2026-05-17.)*

310. **💡 Claude Inspiration — no inline `style=` attributes inside section.** Inline styles bypass the theme CSS and hard-code visual properties that may break on different renderers. Drift class: future Claude copies text from an external source that carries `style="color: red"` or `style="font-size: 14px"` inline. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — no-inline-style check; added 2026-05-17.)*

311. **💡 Claude Inspiration — prose must not duplicate headings already used in the guide (e.g. day namesstop names).** The section is creative context — it should not restate information already visible elsewhere in the guide. The check extracts `.stop-name` and `.day-title` texts from the full guidethen scans the inspiration prose for exact matches (case-insensitive). A match means the prose section is narrating content the reader can already find in the itinerary. Drift class: future Claude writes "Day 2 at the Colosseum was…" using the exact stop namemaking the section read like a schedule recap rather than an insight. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — prose-no-duplicate-headings check; added 2026-05-17.)*

312. **💡 Claude Inspiration — no nested `<div>` elements other than `.extras-title` (flat prose structure; nested divs are format drift).** The section structure is: outer `claude-inspiration` div → `.extras-title` div + `<p>` blocks. Any additional nested `<div>` indicates a logistics container or box-structure copy-pasted from another section. Drift class: future Claude pastes a `.entry-body` or `.transit-box` fragment inside the inspiration section. *(Enforced — validate_itinerary.py CLAUDE INSPIRATION — no-nested-div check; added 2026-05-17.)*

313. **❗ Cities Gotchas — `.extras-title` must be exactly `"❗ Cities Gotchas"` (icon + exact label; no drift variants).** Per `Brain/CORE RULES/Cities Gotchas - Extra Section.html` § Icon Order and Format.html: the title is locked. Variants like "❗ Gotchas""❗ City Gotchas"or "❗ Cities Gotcha" (missing the s) are drift that breaks visual scanning across guides. The check strips HTML from the `.extras-title` div and compares exact string. Drift class: future Claude titles the section "❗ City Gotchas" or "❗ Watch Out" thinking the label is flexible. *(Enforced — validate_itinerary.py Cities Gotchas — extras-title exact check; added 2026-05-17.)*

314. **❗ Cities Gotchas — no box wrappers inside section (`.transit-box``.entry-body``.shows-box` are format drift; entries use flat divs).** CG entries are three flat divs (heading + gotcha + workaround) — not wrapped in a box container. A box class inside the section means an entry was structured as a Michelin or transit entry. Drift class: future Claude wraps a CG entry in `.entry-body` because it looked like the right container for a named-venue entry. *(Enforced — validate_itinerary.py Cities Gotchas — no-box-wrappers check; added 2026-05-17.)*

315. **❗ Cities Gotchas — three-row entry format enforced: heading (`❗ Venue — Short Title`)gotcha body (≤150 charsno emojino links)workaround (`Workaround:` capital Wnon-empty text after colonno emoji).** Per spec §2 entry shape. Six sub-checks consolidated into one pass: (a) heading starts `❗ ` + contains ` — `  + text on both sidesno `<a>` links; (b) heading has no extra emoji after the leading ❗; (c) no duplicate heading across entries; (d) gotcha non-empty≤150 charsno emojino linksdoes not start with "Workaround:"; (e) Workaround row presentcapital Wnon-empty bodyno emoji; (f) no extra rows beyond 2 body rows per entry. Each sub-check accumulates to its own list and the combined result fires as one logical check group. *(Enforced — validate_itinerary.py Cities Gotchas — entry-format checks; added 2026-05-17.)*

316. **❗ Cities Gotchas — no intro prose before first ❗ entry (section goes directly from `.extras-title` to entries).** Any plain div row appearing in the section content before the first `❗`-prefixed entry is narrative drift — the section has no preambleno explanationno context paragraph. Drift class: future Claude writes "Below are some gotchas to watch out for in this city:" before the first entry. *(Enforced — validate_itinerary.py Cities Gotchas — no-intro-prose check; added 2026-05-17.)*

317. **❗ Cities Gotchas — no build annotations in gotcha/workaround rows AND no build annotations in heading row (`[UPDATE]`/🔴/`[TBD]` etc. stripped before shipping).** Two separate checks: (a) body rows (gotcha + workaround) scanned per entry; (b) heading row scanned separately. The heading annotation check was added after the body check because the heading scan was missing from the original annotation-leakage block. Drift class: a future Claude ships `❗ Café Name — [CHECK name]` in the headingor `[UPDATE — confirm this]` inside the gotcha text. *(Enforced — validate_itinerary.py Cities Gotchas — body annotation check + heading annotation check; added 2026-05-17.)*

318. **❗ Cities Gotchas — structural drift checks: no `<p>` tagsno `<h2>`–`<h6>` tagsno `<br>` tagsno inline `style=` attributesno nested `<div>` inside entry rows.** Five structural checks mirroring the same guards on Day TripsFood DeliveryTrain Stations Near Hoteland other flat-div sections. All five fire independently; the combined count covers copy-paste drift from document-style content. Drift class: future Claude copies a gotcha from a rich-text source that carries `<strong>``<h3>`or `<br>` markup. *(Enforced — validate_itinerary.py Cities Gotchas — p-tag / h-tag / br-tag / inline-style / nested-div checks; added 2026-05-17.)*

319. **❗ Cities Gotchas — heading separator must be em-dash `—` (U+2014); venue name 2–60 chars; venue name is not a generic category word; venue name and short title must differ.** Four heading-quality checks: (a) em-dash `—` required (not hyphen `-` or en-dash `–`); (b) venue name length 2–60 chars; (c) venue is a specific namenot a generic word like "Restaurant""Hotel""Museum"; (d) venue name ≠ short title (same text on both sides of ` — ` is copy-paste drift). Drift class: future Claude writes `❗ Restaurant — Restaurant` (venue equals title)uses a hyphen as separatoror writes a single-character venue code. *(Enforced — validate_itinerary.py Cities Gotchas — em-dash / venue-length / generic-venue / venue-eq-title checks; added 2026-05-17.)*

320. **❗ Cities Gotchas — gotcha text ≥5 chars and starts with capital letter; workaround text ≥10 chars after colonstarts with capital letterand differs from gotcha text.** Four content-quality checks on the body rows: (a) gotcha ≥5 chars (not a stub); (b) gotcha starts with capital (prose consistency); (c) workaround body ≥10 chars (actionablenot a stub); (d) workaround starts with capital; (e) workaround text ≠ gotcha text (copy-paste drift where both rows contain the same sentence). Drift class: future Claude writes a one-word gotcha ("Crowds.") and copies it verbatim as the workaround body. *(Enforced — validate_itinerary.py Cities Gotchas — gotcha/workaround min-length and capital checks; added 2026-05-17.)*

321. **❗ Cities Gotchas — short title (after ` — `) must be ≤40 chars.** The short title is a concise labelnot a sentence. More than 40 characters means the "title" has become a descriptionwhich belongs in the gotcha row instead. Drift class: future Claude writes `❗ Eiffel Tower — The lifts stop working frequently without warning` (40+ chars) as the short title instead of `Lift Outages`. *(Enforced — validate_itinerary.py Cities Gotchas — short-title-length check; added 2026-05-17.)*

322. **❗ Cities Gotchas — no tilde (`~`) in gotcha or workaround rows (tilde is globally banned outside ⏰ context; gotcha entries never have a ⏰ distance row).** Same global tilde-ban enforced on every other EoI body row. Gotcha and workaround rows are never distance-or-time estimatesso any tilde is always authoring drift. Drift class: future Claude writes "~20 min queue" inside a gotcha row. *(Enforced — validate_itinerary.py Cities Gotchas — no-tilde-in-body check; added 2026-05-17.)*

323. **❗ Cities Gotchas — gotcha row and workaround text must each end with sentence-ending punctuation (`.` or `!`).** Both the gotcha description and the workaround body are complete sentences by spec — they should end cleanly. A fragment that ends mid-word or with a comma is an unfinished build entry. Drift class: future Claude writes `Closes without notice` (no period) or `Try the side entranceit's usually open` (ends with comma-free). *(Enforced — validate_itinerary.py Cities Gotchas — trailing-punctuation checks; added 2026-05-17.)*

324. **❗ Cities Gotchas — entry count < 8 (≥8 entries signals section may be overcrowded; review and curate).** Per spec §1: the section must stay tightly curated — it is not a dumping ground for every quirk. ≥8 entries is not a hard format error but a signal that the section has grown unwieldy. The check fires a FAIL when count ≥ 8 to force a review-and-trim pass before shipping. Drift class: future Claude adds every gotcha discovered during research without curatingleaving 10–12 entries that the reader cannot scan quickly. *(Enforced — validate_itinerary.py Cities Gotchas — entry-count check; added 2026-05-17.)*

325. **❗ Cities Gotchas — `Cities Gotchas - Extra Section.html` calibration anchors intact (8 spec anchors checked).** The drift-protection block reads the CORE RULES spec and verifies 8 key anchor strings: `Cities Gotchas.md` source ref`Silence means clean``❗` icon`[Venue]` heading format`Short Title` label`the surprise` (gotcha description)`150` char limit`Workaround:` label. A missing anchor means the spec was trimmed and the validator may now misread the section rules. *(Enforced — validate_itinerary.py Cities Gotchas — spec drift-protection; added 2026-05-17.)*

326. **❗ Cities Gotchas — no tilde (`~`) in heading row (`❗ Venue — Short Title` is never a distance estimate).** The body-row tilde ban (Rule 322) covers gotcha/workaround rows. This separate check covers the heading row — a tilde there (e.g. `❗ Venue — ~5 min walk`) is also banned. The heading is a labelnot a distance estimate. *(Enforced — validate_itinerary.py Cities Gotchas — no-tilde-in-heading check; added 2026-05-17.)*

327. **❗ Cities Gotchas — venue name (before ` — `) starts with a capital letter; short title (after ` — `) starts with a capital letter.** Proper-noun venue names and their short-title labels both follow standard capitalisation. A lowercase opener (`mcdonald's``the tower`) is authoring drift. Two sub-checks fire independently. Drift class: future Claude writes `❗ the eiffel tower — Lift Issues` with a lowercase venue name. *(Enforced — validate_itinerary.py Cities Gotchas — venue-capital and short-title-capital checks; added 2026-05-17.)*

328. **❗ Cities Gotchas — heading row must contain exactly one ` — ` separator (two or more em-dashes = format drift).** The three-part format is `❗ [Venue] — [Short Title]` — exactly one separator. A heading like `❗ Venue — A — B` has two separators and produces an ambiguous split. Drift class: future Claude writes `❗ Eiffel Tower — Lifts — Queue Issues` (two dashes) thinking the extra dash adds clarity. *(Enforced — validate_itinerary.py Cities Gotchas — exactly-one-em-dash check; added 2026-05-17.)*

329. **❗ Cities Gotchas — venue name must not end with a period; short title must not be wrapped in quote marks.** Two cosmetic drift checks: (a) `Eiffel Tower.` — labels are not sentencestrailing period is drift; (b) `"Cash Only"` or `'Cash only'` — short titles are plain labelsnot quoted phrases. Both are copy-paste residue from prose sources. *(Enforced — validate_itinerary.py Cities Gotchas — no-trailing-period / no-quote-marks checks; added 2026-05-17.)*

330. **❗ Cities Gotchas — same venue name must not appear more than once (different short titles for the same venue = split-entry drift).** This is distinct from Rule 315's full-heading duplicate check. Here the venue name (the part before ` — `) is extracted and compared case-insensitively across all entries. Two entries for `Starbucks — Cash Only` and `Starbucks — No WiFi` pass the full-heading dupe check but are caught here. Drift class: future Claude logs two separate gotchas for the same venue and ships them as independent entries instead of consolidating. *(Enforced — validate_itinerary.py Cities Gotchas — no-duplicate-venue-names check; added 2026-05-17.)*

331. **❗ Cities Gotchas — no `<script>` or `<style>` tags inside section; no HTML comments (`<!-- -->`).** Structural/script markup and authoring comments do not belong in shipped entry content. `<script>` and `<style>` are always wrong in EoI entry content; HTML comments are build notes that must be stripped. Three sub-checks: (a) `<script>`/`<style>` presence(b) `<!--` presence. Drift class: future Claude includes a `<!-- TODO: confirm this gotcha -->` comment that survives into the shipped guide. *(Enforced — validate_itinerary.py Cities Gotchas — no-script/style and no-comments checks; added 2026-05-17.)*

332. **❗ Cities Gotchas — no `<strong>` or `<em>` tags in gotcha/workaround rows; no `<ul>` or `<ol>` tags inside section.** Entry body rows are plain text — bolditalicand list markup are format drift imported from rich-text sources. Same guards as Pickleball and Food Delivery body rows. Four sub-checks: (a) `<strong>`/`<em>` in any body row(b) `<ul>`/`<ol>` anywhere in the section. Drift class: future Claude **bolds** a key word in the gotcha row or lists multiple workaround steps as a `<ul>`. *(Enforced — validate_itinerary.py Cities Gotchas — no-strong/em and no-list-tags checks; added 2026-05-17.)*


333. **Shows — no `<p>` tags inside `.shows-box` entry bodies.** Per the cross-section pattern enforced in Cappuccino T10 (Rule ~182) and Local Tastes: EoI section content is built from `<div>` rowsnever prose `<p>` elements. A `<p>` tag inside a `.shows-box` entry is a copy-paste bleed from a non-EoI surface (blog excerptdescription blocketc.) or a Claude-generated paragraph that should have been a `<div>` row. The check scans every `.extras-sub` entry body in the Shows section and hard-FAILs if any entry's HTML contains a `<p` open tag. Drift class: future Claude generates a show description as a `<p>` paragraph instead of a 🗒 `<div>` rowor pastes venue prose from a web source into the entry body. *(Enforced — validate_itinerary.py Shows O check: `shows_p_tags`; added 2026-05-17.)*

334. **Shows — no intro prose before the first `.extras-sub` entry.** The Shows section body must begin directly with the first entry heading — no lead-in paragraphscontext sentencesor description text before the first `.extras-sub`. Mirrors the same rule enforced for Local Tastes and Day Trips sections: EoI sections are scannable entry listsnot editorial prose. The check extracts the section bodystrips any `.extras-title` heading div from the pre-entry contentand hard-FAILs if any non-whitespace text remains before the first entry. Drift class: future Claude opens the Shows section with "Here are the best shows in town…" or similar preamble that should instead be omitted or placed in the guide intro. *(Enforced — validate_itinerary.py Shows P check: `shows_preamble_bad`; added 2026-05-17.)*

335. **Shows — 🗒 description must be at least 10 characters.** Every `.shows-box` entry may carry a 🗒 description row; when presentthe plain-text content after stripping all tags must be ≥10 characters. A single wordan ellipsisa placeholder dashor any stub shorter than 10 chars is a build placeholdernot a real description. Mirrors the minimum-length enforcement in Local Tastes and Michelin entries. The check locates the 🗒 row inside each entry body and hard-FAILs if the stripped text is non-empty but under 10 chars. Drift class: a future Claude writes `🗒 TBD` or `🗒 –` as a skeleton placeholder that slips through a ship. *(Enforced — validate_itinerary.py Shows Q check: `shows_desc_too_short`; added 2026-05-17.)*

336. **Shows — 🗒 description must be plain textno embedded `<a>` links.** The 🗒 description row is a plain prose summary of the show/venue. Embedding a clickable `<a>` link inside the description is a format violation — links belong in 🎟 ticket rows or 📅 tour boxesnot in prose description text. The check scans each entry's 🗒 row window (up to the next closing `</div>`) and hard-FAILs if any `<a` open tag appears. Drift class: future Claude auto-links venue names or show titles inside the descriptionimporting link markup from a scraped source or web snippet. *(Enforced — validate_itinerary.py Shows R check: `shows_desc_has_link`; added 2026-05-17.)*

337. **Shows — 🎟 link text must be non-empty.** Every 🎟 ticket anchor in a `.shows-box` must carry visible link text — the anchor cannot be a bare `<a href="…"></a>` with no readable content. Mirrors the near-identical check enforced in Cappuccino and Restaurants Near Hotel sections. The check finds the first `<a href>` element within 400 chars of each 🎟 glyphstrips all tagsnormalises whitespaceand hard-FAILs if the result is empty. Drift class: future Claude emits `🎟 <a href="URL"></a>` with the text omitted (copy-paste from a template or a forgotten second pass). *(Enforced — validate_itinerary.py Shows S check: `shows_ticket_no_text`; added 2026-05-17.)*

338. **Shows — no ⭐ star ratings in venue headings.** The `.extras-sub` venue heading (e.g. "🎭 Show Name") is a title only. Star ratings (⭐ with a numeric rating) belong in 🎟 ticket rows where the rating source and score are properly attributable. A ⭐ in the heading mixes metadata into a title field and is unsupported by the spec. The check inspects each entry's heading text and hard-FAILs if it contains `⭐` or a `[0-9]·?[0-9]?⭐` pattern. Drift class: future Claude copies a "Show Name ⭐ 4.8" line from a listing site directly into the heading without stripping the rating. *(Enforced — validate_itinerary.py Shows T check: `shows_star_in_heading`; added 2026-05-17.)*

339. **Shows — combined 🚶/🚕 motion row must use · separator.** When both a walk time (🚶) and a ride-app estimate (🚕) appear in the same `.shows-box` entrythey must be joined by a `·` dot separator on the same row — matching the universal motion-row format. A missing separator means the two times are either on separate lines or run together without punctuation. The checkwhen both glyphs are present in an entryinspects the plain-text segment between them (plus 20 chars after the later glyph) and hard-FAILs if `·` is absent. Drift class: a future Claude writes `🚶 8 min  🚕 $12 (~5 min)` on a single line but forgets the middot separatoror accidentally splits them onto two separate rows. *(Enforced — validate_itinerary.py Shows U check: `shows_motion_no_dot`; added 2026-05-17.)*

340. **Shows — 🚶 walk time must be a parseable integer (`\d+ min`).** When a Shows entry includes a 🚶 walk glyphthe immediately-following content must match `🚶 N min` where N is a numeric integer. Prose phrases like "🚶 about 10 min"missing-unit entries like "🚶 ~8"or placeholder text like "🚶 TBD min" all fail. Mirrors the walk-time format enforcement already applied to other EoI sections. The check applies the regex `🚶\s*(\d+)\s*min` to the plain-text entry body; if the glyph is present but the pattern does not matchit hard-FAILs. Drift class: future Claude writes the walk time as a range ("🚶 8–10 min")adds a tilde ("🚶 ~8 min")or forgets the `min` suffix. *(Enforced — validate_itinerary.py Shows V check: `shows_walk_not_int`; added 2026-05-17.)*

341. **Shows — when entries are presentthe negative-finding phrase must NOT appear.** The `id="shows"` section ships either a list of entries OR a single negative-finding line ("No exceptional showsperformancesor concerts in…") — never both. A guide that has real entries AND the negative-finding line is in a contradictory state: the negative line survives from a skeleton and was never removed when real content was added. Mirrors the inverse-negative check enforced in the Day Trips section. The check is the reverse of the empty-section check: when the entry count is > 0it scans the section body for the negative-finding pattern and hard-FAILs if found. Drift class: future Claude adds entries to a Shows section that started from the skeleton template but forgets to delete the negative-finding line. *(Enforced — validate_itinerary.py Shows W check: `shows_inv_neg_bad`; added 2026-05-17.)*

342. **Stop titles (`.stop-name`) must not end with trailing punctuation (`.``!``?``:`).** Per `Brain/CORE RULES/Stops Structure.html` §3: every stop is named after the venue. Venue names do not end with sentence-final punctuation — trailing `.!?:` signals that a description sentence or a placeholder label has leaked into the title field. The check uses `re.finditer` over all `.stop-name` spansstrips tagsstrips the guided/self modifier suffixand hard-FAILs if the plain name ends with any of `.!?:` (after whitespace normalisation). Drift class: future Claude writes "Visit the Eiffel Tower." as a stop name (description bleed)or uses a label like "Option 1:" as the stop title. *(Enforced — validate_itinerary.py Stop Titles trailing-punctuation check; added 2026-05-17.)*

343. **Stop titles (`.stop-name`) must not exceed 90 characters.** Venue names are proper nouns — concise by definition. A `.stop-name` exceeding 90 characters is virtually always a description sentencea taglineor a compound name-plus-qualifier that should have been split into a name + annotation. The 90-char cap is generous enough to accommodate long hyphenated venue names (e.g. "Palais de la Cité – Conciergerie and Sainte-Chapelle") while reliably catching description drift. The check strips tags from each `.stop-name`normalises whitespaceand hard-FAILs if length > 90. Drift class: future Claude pastes a venue's marketing tagline ("The world-famous Moulin Rouge cabaret show featuring can-can dancers") as the stop name instead of the venue name alone. *(Enforced — validate_itinerary.py Stop Titles length-cap check; added 2026-05-17.)*

344. **Stop titles (`.stop-name`) must not contain ⭐ star ratings.** The `.stop-name` span is the venue's proper name. A ⭐ rating belongs in the 🎟 ticket box (as part of the review score) — not in the stop heading. A star rating in the name means a listing-style "Venue Name ⭐ 4.7" line has been used as the title instead of the plain venue name. The check scans each `.stop-name`'s plain text and hard-FAILs on any `⭐` character. Drift class: future Claude copies a venue name from a rating-annotated source (TripAdvisor snippetGoogle Maps listing) and pastes the rating alongside the name into the `.stop-name` field. *(Enforced — validate_itinerary.py Stop Titles no-star-rating check; added 2026-05-17.)*

345. **Stop titles (`.stop-name`) must not end with an orphaned `·` separator.** The middot `·` is a separator between two adjacent fields. A trailing `·` at the end of a stop name means one side of the separator pair is missing — the second field was deleted or never written. This always signals an incomplete build. The check strips tags from each `.stop-name`normalises whitespaceand hard-FAILs if the resulting string ends with `·` (with or without trailing spaces). Drift class: future Claude deletes the subtitle or rating that followed a `·` in the name field but leaves the orphaned separator behind. *(Enforced — validate_itinerary.py Stop Titles orphaned-dot check; added 2026-05-17.)*

346. **⚠️ warning text must be at least 15 characters.** Per `Brain/CORE RULES/Icon Order and Format.html` §3: the ⚠️ flag carries non-empty text. Rule 15 (existing) already enforces non-empty. Rule 299 tightens the floor: text shorter than 15 characters (after stripping tags and normalising whitespace) is a single-word or stub placeholder that communicates nothing useful to the reader. "⚠️ Careful" or "⚠️ Note" are not valid warnings. The check measures the length of the stripped text following the ⚠️ glyph and hard-FAILs if it is between 1 and 14 characters inclusive. Drift class: future Claude writes a one-word warning as a build placeholderor abbreviates a warning so aggressively the reader has no actionable information. *(Enforced — validate_itinerary.py ⚠️ ROW FORMAT min-length check: `_warn_short_violations`; added 2026-05-17.)*

347. **⚠️ warning text must not contain build annotations (`[TBD]``[UPDATE]``[CHECK]``[FIXME]``[TODO]``🔴``[PLACEHOLDER]`).** A build annotation inside warning text means the stop was shipped before the warning was finalised. These tokens are internal scaffolding — they are visible to the reader in the rendered guide and immediately signal an unfinished build. The check applies `_WARN_ANNOT_RE` to the text following each ⚠️ glyph (case-insensitive) and hard-FAILs on any match. Drift class: a future Claude writes "⚠️ [TODO: verify this is still true]" as a placeholder and ships it. The annotation leaks to the reader. *(Enforced — validate_itinerary.py ⚠️ ROW FORMAT annotation-leakage check: `_warn_annot_violations`; added 2026-05-17.)*

348. **🚫 closed-day rows must never be stacked — combine multiple closed days into one row.** Per `Brain/CORE RULES/Icon Order and Format.html` §3: *"Combine multiple closed days into one row — never stack."* A `.tour-box` or `.ticket-box` that contains more than one `<div>🚫` row is always wrong: "🚫 Mondays" on one line and "🚫 Tuesdays" on another should be "🚫 Mondays and Tuesdays" on a single row. Stacking creates visual clutter and violates the canonical flag-row format. The check uses `_walk_balanced_div` to extract each `.tour-box`/`.ticket-box` interiorcounts `<div[^>]*>\s*🚫` occurrencesand hard-FAILs if the count exceeds 1. Drift class: future Claude generates a separate 🚫 row for each closed day of the week instead of combining themor copies two separate closed-day rows from different sources into the same box. *(Enforced — validate_itinerary.py 🚫 STACKING check: `_closed_stacked_hits`; added 2026-05-17.)*

349. **🎭 Shows — negative-finding line must not contain a `[City]` literal placeholder.** When the Shows section ships with zero entriesthe negative-finding line must substitute the actual destination city name — `"No exceptional showsperformancesor concerts in Paris."` not `"…in [City]."`. A `[City]` bracket in the shipped line is a skeleton template that was never filled in. The check fires only when `shows_audit_entry_count == 0` and the negative-finding phrase IS present (D check passed); it then tests whether the phrase contains a `\[` bracket immediately following `in `. Hard-FAILs if matched. Drift class: future Claude copies the skeleton negative-finding line from the spec or a guide template and ships it without substituting the city name. *(Enforced — validate_itinerary.py Shows X check: `shows_neg_placeholder`; added 2026-05-17.)*

350. **🎭 Shows — 🚎 tram row must contain `→` arrows when present.** Per `Brain/CORE RULES/Shows, Performances & Concerts - Extra Section.html` §3: the optional tram row format is `"🚶 X min → [BOARD] · 🚎 [tram] → [ALIGHT] · 🚶 T min"`. The `→` arrows are structural — they mark the board and alight transitions. A 🚎 glyph without any `→` in its row window (120 chars either side) means the tram row was stripped to bare text or copy-pasted without the directional markers. Hard-FAILs per offending entry. Drift class: future Claude writes `🚎 Line 14` with no boarding/alighting structureleaving the reader no directions on where to get on or off. *(Enforced — validate_itinerary.py Shows Y check: `shows_tram_no_arrow`; added 2026-05-17.)*

355. **🍮 Local Tastes — `.extras-title` must be exactly `"🍮 Local Tastes"` (locked label; no drift variants).** Per `Brain/CORE RULES/Icon Order and Format.html`: the title is locked. Variants like "🍮 Local Flavor" or "🍮 Local Food" are drift that breaks visual scanning across guides. *(Enforced — validate_itinerary.py Local Tastes — extras-title exact check; added 2026-05-17.)*

356. **🍮 Local Tastes — description ≥10 chars (not a stub).** A description shorter than 10 characters is an unfilled build placeholder. Drift class: future Claude ships `"Famous here."` (13 chars — passes) vs `"Tasty."` (6 chars — fails). *(Enforced — validate_itinerary.py Local Tastes — description min-length check; added 2026-05-17.)*

357. **🍮 Local Tastes — description starts with a capital letter.** Same prose-consistency rule enforced on all other body-text rows across EoI sections. Drift class: future Claude writes a lowercase opener like `"a rich chocolate cake…"`. *(Enforced — validate_itinerary.py Local Tastes — description capital check; added 2026-05-17.)*

358. **🍮 Local Tastes — description ends with a period.** Descriptions are complete sentences; a fragment ending without punctuation is an unfinished build entry. Drift class: future Claude writes `"A bicerin is a layered coffee and chocolate drink from Turin"` without the closing period. *(Enforced — validate_itinerary.py Local Tastes — description period check; added 2026-05-17.)*

359. **🍮 Local Tastes — description is plain prose; no `<a>` links (links belong in 📍 row only).** The two-row entry format separates content (description) from navigation (📍 row). An `<a>` inside the description row means content and navigation are mixed — the reader can't visually scan the layout. Drift class: future Claude embeds a Wikipedia link inline in the description text. *(Enforced — validate_itinerary.py Local Tastes — description no-link check; added 2026-05-17.)*

360. **🍮 Local Tastes — description is plain text; no `<strong>` or `<em>` markup.** Same guard as Pickleball and Food Delivery body rows. Bold/italic is formatting drift imported from rich-text sources. Drift class: future Claude **bolds** the dish name inside the description paragraph. *(Enforced — validate_itinerary.py Local Tastes — description no-markup check; added 2026-05-17.)*

361. **🍮 Local Tastes — no tilde (`~`) in any entry row (globally banned outside ⏰ context; LT entries never have distance rows).** The tilde global ban applies across all EoI sections. Local Tastes entries are dish name + description + 📍 row — none of these are distance/time estimates. Drift class: future Claude writes `"~30 min wait at peak hours"` inside a description. *(Enforced — validate_itinerary.py Local Tastes — no-tilde check; added 2026-05-17.)*

362. **🍮 Local Tastes — no build annotations in NAME row (`[UPDATE]`/🔴/`[TBD]` etc.).** The existing annotation check (Rule J) only covered description rows. The NAME row (`.extras-sub` heading div) was not scanned. Drift class: future Claude ships `[CHECK name] BICERIN` in the NAME div because the dish spelling was unconfirmed. *(Enforced — validate_itinerary.py Local Tastes — NAME annotation check; added 2026-05-17.)*

363. **🍮 Local Tastes — no build annotations in 📍 row.** Same annotation scan applied to the 📍 establishment row. Drift class: future Claude ships `📍 [UPDATE — confirm Maps link] Café Bicerin` with an unresolved placeholder in the 📍 row. *(Enforced — validate_itinerary.py Local Tastes — 📍 annotation check; added 2026-05-17.)*

364. **🍮 Local Tastes — NAME must be a specific dish or drink namenot a generic category word ("Dessert""Food""Drink""Dish" etc. are stubs).** Same generic-name guard as PickleballStationsDowntown. A bare category word means the entry was never filled in. *(Enforced — validate_itinerary.py Local Tastes — generic-name check; added 2026-05-17.)*

365. **🍮 Local Tastes — NAME ≤50 chars (concise dish labelnot a sentence).** Dish names are labels like `BICERIN` or `FRENCH ONION SOUP` — not descriptive phrases. More than 50 chars means the author wrote a sub-headline rather than a dish name. Drift class: future Claude writes `TRADITIONAL LAYERED COFFEE AND HOT CHOCOLATE DRINK FROM PIEDMONT` (65 chars) as the NAME. *(Enforced — validate_itinerary.py Local Tastes — NAME length check; added 2026-05-17.)*

366. **🍮 Local Tastes — entry count ≤6 (per §1: only genuinely distinctive items; ≥7 signals over-inclusion).** The section is a curated highlight reelnot an exhaustive catalogue. Per spec §1: *"Include only when something genuinely distinctive exists."* More than 6 entries signals the section has become a food guide rather than a curated set of locally iconic items. Drift class: future Claude lists 10 local foods because the destination has a rich food culture — defeats the curation intent. *(Enforced — validate_itinerary.py Local Tastes — entry-count check; added 2026-05-17.)*

367. **🍮 Local Tastes — negative-finding line must be in `<div class="extras-empty">` (bare text without the class is structural drift).** The locked phrase `"No recommended foods to taste in {City}."` must live in the `extras-empty` wrapper div — same enforcement as ShowsStationsWeekly ClosuresFood Delivery. Drift class: future Claude writes the phrase directly in the section without the wrapper classbreaking the visual treatment. *(Enforced — validate_itinerary.py Local Tastes — extras-empty class check; added 2026-05-17.)*

368. **🍮 Local Tastes — no `<br>` tags inside section.** Row breaks come from div structure. `<br>` is line-break markup from a document modelnot appropriate in a flat-div entry section. *(Enforced — validate_itinerary.py Local Tastes — no-br check; added 2026-05-17.)*

369. **🍮 Local Tastes — no `<h2>`–`<h6>` heading tags inside section (format drift).** Heading tags signal content pasted from a structured document. The section is flat div rows only. *(Enforced — validate_itinerary.py Local Tastes — no-h-tags check; added 2026-05-17.)*

370. **🍮 Local Tastes — no `<ul>` or `<ol>` tags inside section (list markup is format drift).** Entry format is div-based. A list element means a bullet list was inserted instead of a proper description row. *(Enforced — validate_itinerary.py Local Tastes — no-list-tags check; added 2026-05-17.)*

371. **🍮 Local Tastes — no inline `style=` attributes inside section.** Inline styles bypass the guide's CSS theme and hard-code visual properties that may break on different renderers. *(Enforced — validate_itinerary.py Local Tastes — no-inline-style check; added 2026-05-17.)*

372. **🍮 Local Tastes — no `<script>` or `<style>` tags inside section.** Structural/script markup does not belong in entry content. *(Enforced — validate_itinerary.py Local Tastes — no-script/style check; added 2026-05-17.)*

351. **🍮 Local Tastes — no HTML comments (`<!-- -->`) inside section (build notes must be stripped before shipping).** Same guard as Cities Gotchas and Claude Inspiration. A surviving `<!-- TODO -->` comment is a build error in a shipped guide. *(Enforced — validate_itinerary.py Local Tastes — no-comments check; added 2026-05-17.)*

352. **🍮 Local Tastes — section div resolved (`id="local-tastes"` must be on the `.extras-section` div).** If the section is in the document but the `id="local-tastes"` attribute is missing or misspelledthe validator can't locate the section and all per-entry checks silently skip. This check fires as a hard-FAIL when the section exists in `eoi_present_set` but `_lt_hdr` is `None`. *(Enforced — validate_itinerary.py Local Tastes — section-div-resolved check; added 2026-05-17.)*

353. **🍮 Local Tastes — `Local Tastes - Extra Section.html` readable at expected CORE RULES path.** Drift-protection: the spec file must be accessible so future Claude can read the rules. A missing or unreadable spec means future validator updates may misread the section rules. *(Enforced — validate_itinerary.py Local Tastes — spec-readable check; added 2026-05-17.)*

354. **🍮 Local Tastes — `Local Tastes - Extra Section.html` calibration anchors intact (6 spec anchors checked).** Six anchor strings verified: `genuinely distinctive` (§1 what ships)`Dish or Drink Name` (§2 NAME format)`240` (description char limit)`1 to 3` (establishment count)`Google Maps` (📍 link type)`No recommended foods to taste in` (§3 negative-finding). A missing anchor means the spec was trimmed and the validator may now misread the section rules. *(Enforced — validate_itinerary.py Local Tastes — spec drift-protection; added 2026-05-17.)*


373. **🎭 Shows — `.extras-title` must be exactly `"🎭 ShowsPerformances & Concerts"` (locked label; no drift variants).** Per spec: the title is locked. Variants like "🎭 Shows & Concerts" or "ShowsPerformances" are drift that breaks visual scanning across guides. *(Enforced — validate_itinerary.py Shows — extras-title exact check; added 2026-05-18.)*

374. **🎭 Shows — 🗒 description must start with a capital letter.** Prose consistency rule applied to every show description row. A lowercase start is a build stub or copy-paste error. *(Enforced — validate_itinerary.py Shows — description capital check; added 2026-05-18.)*

375. **🎭 Shows — 🗒 description must end with a period (.).** Sentence-ending punctuation required. Matches the standard applied to all EoI description rows. *(Enforced — validate_itinerary.py Shows — description period check; added 2026-05-18.)*

376. **🎭 Shows — no tilde (`~`) in 🗒 description rows.** Tilde is a build placeholder universally banned in shipped content. A tilde in a description means a distance estimate or stub text was left in. *(Enforced — validate_itinerary.py Shows — description tilde check; added 2026-05-18.)*

377. **🎭 Shows — no `<strong>` or `<em>` markup in 🗒 description rows.** Description rows are plain prose. Bold or italic markup is format drift — emphasis belongs in the heading labelnot the description. *(Enforced — validate_itinerary.py Shows — description markup check; added 2026-05-18.)*

378. **🎭 Shows — entry count ≤ 6 (high-calibration section).** Per § 2: calibration is high — only destination-level performances qualify. More than 6 entries signals over-inclusion of touring or non-flagship shows. *(Enforced — validate_itinerary.py Shows — entry count cap check; added 2026-05-18.)*

379. **🎭 Shows — negative-finding must be inside `<div class="extras-empty">`.** When no shows qualifythe negative-finding phrase `"No exceptional showsperformancesor concerts"` must use the `extras-empty` class — not a bare `<div>` or `<p>`. *(Enforced — validate_itinerary.py Shows — extras-empty class check; added 2026-05-18.)*

380. **🎭 Shows — no `<br>` tags inside section.** All line breaks are `<div>` row boundaries. A `<br>` is structural drift from a prose-editing context. *(Enforced — validate_itinerary.py Shows — no br tags check; added 2026-05-18.)*

381. **🎭 Shows — no `<h2>`–`<h6>` heading tags inside section (format drift).** EoI sections use `<div>` rows; heading tags are for page-level structure only. *(Enforced — validate_itinerary.py Shows — no h tags check; added 2026-05-18.)*

382. **🎭 Shows — no `<ul>` or `<ol>` tags inside section (list markup is format drift).** Show entries are `<div>` rowsnot list items. List tags mean content was pasted from a non-EoI surface. *(Enforced — validate_itinerary.py Shows — no ul/ol tags check; added 2026-05-18.)*

383. **🎭 Shows — no inline `style=` attributes inside section.** All styling is class-driven. An inline style is a one-off override that bypasses the design system. *(Enforced — validate_itinerary.py Shows — no inline style check; added 2026-05-18.)*

384. **🎭 Shows — no `<script>` or `<style>` tags inside section.** Script and style blocks are forbidden inside the guide body. *(Enforced — validate_itinerary.py Shows — no script/style tags check; added 2026-05-18.)*

385. **🎭 Shows — no HTML comments (`<!-- -->`) inside section.** Build notes and temporary comments must not ship with the guide. *(Enforced — validate_itinerary.py Shows — no HTML comments check; added 2026-05-18.)*

386. **🎭 Shows — no `<table>` tags inside section (table markup is format drift).** Content is `<div>`-row based; table tags indicate content pasted from a non-EoI source. *(Enforced — validate_itinerary.py Shows — no table tags check; added 2026-05-18.)*

387. **🎭 Shows — section div resolved (`id="shows"` on `.extras-section` container).** The section regex must find the `id="shows"` div. If it doesn't resolveall per-entry checks silently pass — the gate exists to catch container renaming or removal. *(Enforced — validate_itinerary.py Shows — section div resolved check; added 2026-05-18.)*

388. **🎭 Shows — `Shows, Performances & Concerts - Extra Section.html` must be readable at expected CORE RULES path.** The spec file must exist and be readable so calibration anchors can be verified. A missing spec file means drift can't be detected. *(Enforced — validate_itinerary.py Shows — spec readable check; added 2026-05-18.)*

389. **🎭 Shows — calibration anchors intact (5 spec anchors checked).** Five anchor strings verified: `destination-level` (§2 what ships)`🗒` (§3 row format)`160` (description char limit)`🎟` (booking row)`No exceptional shows` (§4 negative-finding). A missing anchor means the spec was trimmed and the validator may now misread section rules. *(Enforced — validate_itinerary.py Shows — spec drift-protection anchors check; added 2026-05-18.)*

390. **🚗 Food Delivery — no intro prose before first platform entry (preamble banned).** The section opens directly with `.extras-sub` platform headings — no explanatory paragraphsno lead-in text. Any visible text before the first `.extras-sub` div (after stripping the `.extras-title` wrapper) is structural drift imported from a document or written as a build note. Hard-FAILs when `_fd_preamble_bad` is `True`. Drift class: future Claude writes "Here are the main delivery platforms available in this city:" before the first entry. *(Enforced — validate_itinerary.py Food Delivery P check: `_fd_preamble_bad`; added 2026-05-18.)*

391. **🚗 Food Delivery — description ≥10 chars (stub floor).** A description shorter than 10 characters is an unfilled build placeholder. The existing ≤80 char check already enforces the ceiling; this floor ensures no entry ships with `"Delivery."` (9 chars) or similar one-word stubs. Hard-FAILs per offending entry. Drift class: future Claude fills in `"Fast."` as a placeholder description and ships it. *(Enforced — validate_itinerary.py Food Delivery Q check: `_fd_desc_short`; added 2026-05-18.)*

392. **🚗 Food Delivery — URL link text must be non-empty.** The second child div of each `.transit-box` entry must contain an `<a>` with non-empty visible text. An `<a href="…"></a>` with no inner text renders as an invisible link — the reader sees nothing to tap. Hard-FAILs per offending entry. Drift class: future Claude pastes a bare URL into an `<a>` tag and leaves the inner content empty. *(Enforced — validate_itinerary.py Food Delivery S check: `_fd_link_no_text`; added 2026-05-18.)*

393. **🚗 Food Delivery — no ⭐ star rating in platform heading.** Food Delivery platforms are named (e.g. "Uber Eats")not rated. A ⭐ in the `.extras-sub` heading div means a star-rated venue format was mixed into a platform entry. Hard-FAILs per offending heading. Drift class: future Claude adds "(4.2 ⭐)" after the platform name by analogy with attraction entries. *(Enforced — validate_itinerary.py Food Delivery T check: `_fd_heading_star`; added 2026-05-18.)*

394. **🚗 Food Delivery — platform name must start with a capital letter.** Platform names are proper nouns or branded names; a lowercase opener is formatting drift from a prose sentence or copy-paste error. Hard-FAILs per offending heading. Drift class: future Claude writes "uber eats" or "deliveroo (main app)" with a lowercase first character. *(Enforced — validate_itinerary.py Food Delivery — heading capital check: `_fd_heading_no_cap`; added 2026-05-18.)*

395. **🚗 Food Delivery — platform name ≤60 chars.** A platform name over 60 characters is not a name — it is a sentence or description in the heading div. The name must be a label only; description belongs in the first child of the `.transit-box`. Hard-FAILs per offending heading. Drift class: future Claude writes "Uber Eats — the main delivery platform operating in this city" (61 chars) as the heading. *(Enforced — validate_itinerary.py Food Delivery — heading length check: `_fd_heading_long`; added 2026-05-18.)*

396. **🚗 Food Delivery — entries present → no negative-finding phrase.** When the section contains at least one `.extras-sub` platform entrythe negative-finding phrase `"No delivery services available"` must be absent. Its presence alongside real entries means the negative line was not removed after entries were added. Hard-FAILs when `_fd_inv_neg_bad` is `True`. Drift class: future Claude adds platform entries to a guide that already had the negative phrase and forgets to remove it. *(Enforced — validate_itinerary.py Food Delivery W check: `_fd_inv_neg_bad`; added 2026-05-18.)*

397. **🚗 Food Delivery — no `[City]` literal placeholder in negative-finding line.** When the section ships with no entriesthe negative phrase must use the real destination city name — `"No delivery services available in Paris."` not `"…in [City]."`. A bracket placeholder is an unfilled template skeleton. The check fires only when the section is confirmed negative and the phrase is present. Hard-FAILs when `_fd_neg_placeholder` is `True`. Drift class: future Claude copies the skeleton negative line from the spec and ships it without substituting the city name. *(Enforced — validate_itinerary.py Food Delivery X check: `_fd_neg_placeholder`; added 2026-05-18.)*

398. **🚝 Getting Around — every `.transit-box` row ≤80 chars (tram template rows exempt).** Per `Brain/CORE RULES/Getting Around - Extra Section.html` — transit-box rows are designed to be scannable at a glance; wall-of-text rows break the compact layout. Every `<div>` inside a Getting Around `.transit-box` is stripped of HTML tags and measured as plain text; any row over 80 chars is a hard-FAIL. Exemption: tram template rows (containing `"has a tram system"``"No tram available"`or `"does not have a tram system"`) are allowed to exceed 80 chars because the three locked tram template sentences can run up to ~91 chars and cannot be shortened without breaking the locked spec. Drift class: future Claude writes a long descriptive sentence about a metro line's historycoverageor route in a single transit-box row instead of splitting into name row + one-line description row. Added 2026-05-18 after the London Getting Around audit exposed wall-of-text rows that visually broke the section while passing all existing checks. *(Enforced — validate_itinerary.py GETTING AROUND — TRANSIT-BOX ROW LENGTH ≤80 CHARS; added 2026-05-18.)*

399. **🚝 Getting Around — every 🚝 Metro subsection must contain a `"Metro planned: …"` locked template row.** Per `Brain/CORE RULES/Getting Around - Extra Section.html` § Metro: the metro subsection's `.transit-box` must include a row beginning `"Metro planned: {City} metro — {Operator}. Lines: {N}. Pay with {Method}."` — this row carries the machine-readable metro summary and is how future validators and guide consumers identify the metro system. An `.extras-sub` headed with 🚝 that has a `.transit-box` but no `"Metro planned:"` row hard-FAILs. Detection: find every `<div class="extras-sub">` in the Getting Around section whose text starts with `🚝`locate the following `.transit-box`strip HTML to plain textand scan for the substring `"Metro planned:"` (case-insensitive). Drift class: future Claude writes a metro transit-box with name row + description row but forgets the locked template summary rowmaking the section visually adequate but machine-unreadable. Added 2026-05-18 alongside Rule 398 after the London Getting Around fix. *(Enforced — validate_itinerary.py GETTING AROUND — METRO LOCKED TEMPLATE; added 2026-05-18.)* **🔄 SUPERSEDED 2026-05-19:** The `"Metro planned:"` template row was removed from the Getting Around format spec when rules were updated. The `"Metro planned:"` substring check was retired from `validate_itinerary.py` on 2026-05-19. **What runs now (Check Dvalidate_itinerary.py lines ~21315–21375):** every real 🚝 Metro `.transit-box` must have EXACTLY 3 rows in order: (1) plain-language description ≤80 charsno `<a href>`starts uppercase; (2) `"{City} has a metro — operated by …"` locked template sentence; (3) operator link row containing `<a href>`. Wrong row countdescription over 80 charsmissing templateor missing link all hard-FAIL. Negative-finding entries (section present but city has no metro) are excluded via `_is_real_metro_fmt`. *(Previously enforced as "Metro planned:" scan — retired 2026-05-19; replaced by validate_itinerary.py GETTING AROUND — METRO FORMAT — CHECK D; retirement note added 2026-05-25.)*


390. **☕🫕🍽️ Heading link must be a Google Maps PLACE pagenot a search URL (`maps.google.com/?q=...` is banned as a review link).** The allowed-review-host check already blocks non-Google URLs. This separate check catches the case where the domain IS `maps.google.com` or `google.com/maps` but the URL contains `?q=` or `/search/` — making it a search URLnot a place/review page. A search URL delivers no user reviews; the reader lands on a search results list rather than the venue's review page. Enforced in Cappuccino (T4b)Restaurants Near Hoteland Downtown Restaurants (B2b). Drift class: future Claude pastes a Google Maps directions or search link into the heading instead of the venue's place page. *(Enforced — validate_itinerary.py T4b / RNH / DR B2b; added 2026-05-18.)*

391. **☕🫕🍽️ No plain text before the venue link in an `extras-sub` heading (`"no reviews [Venue](...)"` is a build note and must not ship).** The heading div must open directly with the `<a>` tag. Any non-whitespace text before the first `<a>` element is a build annotation or draft note — text like `"no reviews"``"[CHECK]"`or `"see Yelp"` before the venue link means the entry was not cleaned before shipping. Enforced in Cappuccino (T4c)Restaurants Near Hoteland Downtown Restaurants (B2b). Drift class: future Claude adds `"no reviews"` or a similar qualifier before the link to note that the venue has no online reviews — that note must be removednot shipped. *(Enforced — validate_itinerary.py T4c / RNH / DR B2b; added 2026-05-18.)*

392. **🎭 Shows — 📒 (contextual note) row is allowed in the Shows section and must follow the exact format `"📒 [City/cities]: no performances meet the bar for this guide."` A 📒 row that uses any other phrase structure fails.** The 📒 icon is added to the Shows icon allowlist alongside 🗒 🎟 📍 ⚠️ 🚶 🚕 🚎. This row type is used as a city-specific negative-finding when the destination has no qualifying performances — it explains the absence by naming the city/cities rather than using the generic `extras-empty` phrase. The colon + exact phrase is required; the sentence must end with a period. Drift class: future Claude uses 📒 with an improvised phrase like `"📒 No shows in Sintra."` or copies the phrase but omits the colon. *(Enforced — validate_itinerary.py Shows — 📒 allowlist entry + format check; added 2026-05-18.)*


401. **🚶🚕 Motion times ≥60 min must use `Xh Ymin` format — never bare `N min`.** Any transit time ≥60 minutes displayed in a guide (walk row `🚶`ride row `🚕`or day-trip distance `· N min`) must be written as `Xh Ymin` (e.g. `1h 30min``2h 20min`). For exact hours with no remainderdrop the minute part: `60 min → 1h``120 min → 2h`. Bare `N min` for N ≥ 60 is forbidden — it makes long times visually indistinguishable from short ones and was found across 9 guides (41 lines fixed 2026-05-25). Detection: validator scans every visible `🚶 N min``🚕 N min`and `· N min` pattern in non-comment HTML; any N ≥ 60 that is NOT preceded by an `Xh` prefix is a hard-FAIL. The autofix script (`autofix_itinerary.py`) converts bare N-min values ≥ 60 to Xh-Ymin on sight. HTML comments (e.g. `<!-- walk-over-40: ... 90 min walk -->`) are exempt — they are build notesnot rendered content. *(Enforced — validate_itinerary.py MOTION FORMAT — ≥60 min → Xh Ymin; added 2026-05-25.)*

402. **🏨 `FROM HOTEL:` line must contain only transit info — no departure time.** The `.hotel-first` div format is locked as `🏨 FROM HOTEL: [🚶 N min ·] 🚕 N min → {Destination}`. A departure time like `8:00am` or `7:30am` after the colon is forbidden drift — the line answers "how far" not "what time." Including a time leaks day-plan scheduling logic into the transit layeris inconsistent with all non-train-day hotel-first linesand was found across 8 guides (12 lines fixed 2026-05-25: Paris 3Munich 4Sydney 1Lisbon 1Turin 1London 1SF 1). Detection: validator scans every `.hotel-first` div; if the text following `FROM HOTEL:` matches `\d+:\d+[ap]m`it is a hard-FAIL. Departure times belong in the day-plan prose or train-day headernot in the FROM HOTEL transit line. *(Enforced — validate_itinerary.py FROM HOTEL — no departure time; added 2026-05-25.)*

400. **🚫 No Section Snippets file — permanently prohibited.** `Section Snippets` (any file with that nameor any auxiliary template / copy-paste reference file for guide section content) is permanently retired and must never be recreated. the owner deleted it on 2026-05-24 and explicitly prohibited it on 2026-05-25. The CORE RULES Extra Section HTML files (`Brain/CORE RULES/[Section] - Extra Section.html`) are the only source of format for every guide section — read those directly. Detection: `brain_check.py` (session-start) scans for any file whose name contains "Section Snippets" in the Travel workspace and flags it for archiving. No validator enforcement needed in guide HTML — this is a workspace-level prohibition. Drift class: a future Claude sessionfinding no copy-paste templatecreates one to "help future builds." That file becomes the next source of drift. The pattern has already caused the Getting Around ride-app description loop (six repeated failures 2026-04-24 to 2026-05-25). The fix is to read the rule filenot to create an intermediary. *(Human-enforced — archive any file matching "Section Snippets" on sight; added 2026-05-25.)*

---

**Why this exists.** Before this listClaude could say "I'll keep the Brain clean" but the only proof was Claude's memory between sessionswhich evaporates. That's how Rule 00a0cand four feedback files were silently deleted in one pass on 2026-04-20. Every cleanliness action now lives in `brain_check.py` (session-start)`guide_tools.py audit` (weekly)or `validate_itinerary.py` + `verify_urls.py` (ship-gate). No session-to-session memory required.

**This list grows; it never shrinks.** New categories of drift will surface. When they doadd a check here and wire it into the corresponding script. Per §Permissioning in the profileadditions are green-lit.
